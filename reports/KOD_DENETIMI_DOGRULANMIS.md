# Kod/Notebook Denetimi — Doğrulanmış Bulgular (2026-09-06)

120 ajanlı iş akışı, 12 boyut. **108 ham bulgu → 57'si karşı-doğrulamada çürütüldü → 51'i doğrulandı.**
Her bulgu, onu bulan ajandan bağımsız bir ajan tarafından *çürütülmeye çalışılarak* sınandı.
LaTeX ve reports/ kapsam dışı.

| # | Ağırlık | Boyut | Dosya | Özet |
|---|---|---|---|---|
| 1 | blocker | leakage | `notebooks/en/04_runtime_prediction_models.ipynb` | Confirmed and slightly broader than stated. Every training cell in notebooks/en/04_runtime_prediction_models.ipynb (cd07, cd08, cd09, cd14, cd15, cd16… |
| 2 | blocker | notebook-pipeline-consistency | `notebooks/en/04_runtime_prediction_models.ipynb` | Accurate as stated, but the scope is broader than "every ML metric". All 20 model checkpoints plus exp_ablation_features.json, exp_fc06_rolling_origin… |
| 3 | blocker | training-and-metrics | `notebooks/en/04_runtime_prediction_models.ipynb` | Core claim confirmed; two refinements to the wording, neither weakening it. (1) Cell count: 26 code cells print the "[Checkpoint] WARNING: ... provena… |
| 4 | major | config-and-dead-code | `src/feature_engineering.py:611` | Confirmed, with two corrections to scope. CONFIRMED CORE: src/feature_engineering.py:611 `with_categorical` is a bare copy with no category restrictio… |
| 5 | major | config-and-dead-code | `notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ip` | Both notebook-05 variants are committed only partially executed (en/32-GPU: 12 of 28 code cells executed at HEAD, 0 stored figures; en/256-GPU: 14 of … |
| 6 | major | en-tr-parity | `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipyn` | The defect is real as to mechanism and consequence, but one evidentiary detail is wrong. The claim states "The checkpoints on disk are currently in th… |
| 7 | major | en-tr-parity | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | Confirmed as stated, with two additional errors: both language versions of the 32-GPU cell name policies ('SJF-XGBoost (Categorical)', 'SJF-LSTM (Cate… |
| 8 | major | export-and-artifacts | `scripts/export_thesis_results.py` | extract_from_nb_dict() in scripts/export_thesis_results.py never inspects out["output_type"] (grep count 0), so a code cell that raised still contribu… |
| 9 | major | export-and-artifacts | `scripts/export_thesis_results.py` | export_thesis_results.py counts and exports any stored cell output as a figure produced by the current run; it never consults execution_count, so an o… |
| 10 | major | feature-engineering | `notebooks/en/04_runtime_prediction_models.ipynb` | Real, with two corrections to the claim's framing. (1) The measured effect is larger than stated and the claimant's ad-hoc n_estimators=200 refit unde… |
| 11 | major | leakage | `notebooks/en/04_runtime_prediction_models.ipynb, cel` | The Per-Group Median baseline is not "provably leakage-free" as cell grpmed01 claims: whether a test job receives an informative train-only group medi… |
| 12 | major | leakage | `notebooks/en/04_runtime_prediction_models.ipynb, cel` | The leakage-6 mitigation in prepare_features_for_model's "with_categorical_native" branch is bypassed by every path that produces a reported number: c… |
| 13 | major | leakage | `notebooks/en/04_runtime_prediction_models.ipynb, cel` | Real, and stronger than stated. In notebooks/en/04_runtime_prediction_models.ipynb cell 'fc06cell01' the three F-C06 rolling-origin windows are not me… |
| 14 | major | leakage | `src/tuning.py:305-363` | All 31 tree- and DL-model checkpoints in results/checkpoints/ record src_sha256["src/feature_engineering.py"] = 605f75d2... , the CURRENT post-fix has… |
| 15 | major | notebook-pipeline-consistency | `notebooks/en/04_runtime_prediction_models.ipynb cell` | The leakage-6 mitigation is dead code for every model the study reports. src/feature_engineering.py applies train-only category derivation (remove_unu… |
| 16 | major | notebook-pipeline-consistency | `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipyn` | Scope is narrower than stated and status is stronger. Only three TR save_checkpoint cells differ from their EN counterparts -- nbcm02 (exp_b_constant_… |
| 17 | major | regression-this-session | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | Correct statement: notebook 05 (both cluster sizes, EN and TR) is left in a partially executed state in which none of its 7 figure cells carry stored … |
| 18 | major | regression-this-session | `notebooks/en/04_runtime_prediction_models.ipynb` | The new Per-Group Median baseline was wired into both results tables without the None-guard and without the checkpoint fallback that every other entry… |
| 19 | major | reproducibility | `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb c` | notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell r4cell (identical in 05_scheduler_evaluation_32_gpu.ipynb and notebooks/tr/05_gorev_zamanlayic… |
| 20 | major | reproducibility | `src/tuning.py:155-166` | The provenance guard hashes the wrong file in both directions. `_compute_provenance` (src/tuning.py:155-166) records `data_sha256` of data/processed/1… |
| 21 | major | reproducibility | `src/tuning.py:109` | The provenance/refit guard hashes only src/feature_engineering.py and src/tuning.py, and model_artifact_is_current (src/tuning.py:396) compares nothin… |
| 22 | major | simulator | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | The shipped checkpoint results/models/cnn_lstm_numeric_seq.pth emits a single constant value (4128.124023; n_unique=1, spearman=nan, MAE=6702.11233273… |
| 23 | major | simulator | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | Cell cd13 of all four notebook 05 variants re-sorts sim_jobs with pandas' default non-stable quicksort on arrival_time, a column where 30.8% of the 16… |
| 24 | major | simulator | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | The multi-seed DL robustness check (robustness-4) in cell r4cell of all four 05 notebooks is permanently dead due to a seed-naming mismatch: it tests … |
| 25 | major | statistics | `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb` | Confirmed as stated, with two precisions. In all four notebook 05 variants (notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb and notebooks/tr/0… |
| 26 | major | statistics | `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb` | In cell 'stats1cell' (identical in notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, notebooks/en/05_scheduler_evaluation_32_gpu.ipynb, notebooks/tr… |
| 27 | major | statistics | `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb` | Confirmed, and slightly broader than stated. In notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (and the identical cell ids in notebooks/tr/05_gore… |
| 28 | major | tests | `tests/test_feature_engineering.py` | This session's change to prepare_features_for_model (build the job table with include_cpu_only=True, run the sweep-line over all jobs, restrict to gpu… |
| 29 | major | tests | `tests/test_tuning.py` | tests/test_tuning.py:67-103 (TestFinalizeMlModelDataBudget) is named and documented as the regression guard for code_bugs-6 ("XGB/LGBM final refits mu… |
| 30 | major | tests | `src/tuning.py` | Confirmed core: model_artifact_is_current (src/tuning.py:366) and record_model_artifact (src/tuning.py:399), added in 4e4f5b5, have zero tests — grep … |
| 31 | major | tests | `src/tuning.py` | Real, and understated in scope. Neither half of commit 4e4f5b5's reproducibility-4 fix has any test. (a) save_checkpoint's re-save branch (src/tuning.… |
| 32 | major | tests | `src/tuning.py` | Correct in substance; two count details are off and one consequence needs narrowing. Measured under ./venv/bin/python at HEAD 897524a, the data_sha256… |
| 33 | major | training-and-metrics | `src/tuning.py:1161-1162, 1225-1234, 1371-1373` | make_narrow_grid()'s _clip_05_10 floors subsample / colsample_bytree / colsample_bylevel / colsample_bynode at 0.5. configs/models.yaml offers colsamp… |
| 34 | major | training-and-metrics | `notebooks/en/04_runtime_prediction_models.ipynb` | Real, but under-scoped: all SEVEN result-interpretation markdown cells in notebook 04 are stale, not five. Beyond fc720756, 0cb81788, ee775324, 271641… |
| 35 | minor | config-and-dead-code | `notebooks/en/00_data_preparation.ipynb cell 5dd6c695` | After this session's change to `include_cpu_only=True`, the cells that build the canonical job table no longer drop the 17,816 CPU-only rows -- the re… |
| 36 | minor | en-tr-parity | `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipyn` | In notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb cell 'cd28' (section "20. LSTM (One-Hot)"), the closing print line is a verbatim copy of cell… |
| 37 | minor | en-tr-parity | `notebooks/tr/05_gorev_zamanlayici_degerlendirme_32_g` | Both TR notebook-05 files carry a TR-only 'Simülasyon Kurulum Parametreleri' block in cell md14 whose 'Küme Mimarisi' line misdescribes the cluster th… |
| 38 | minor | export-and-artifacts | `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` | The rank-correlation cells (notebooks/en/05_scheduler_evaluation_32_gpu.ipynb cell 7a843cee lines 149-154; 05_scheduler_evaluation_256_gpu.ipynb cell … |
| 39 | minor | feature-engineering | `notebooks/en/00_data_preparation.ipynb` | Cell md05 in notebooks/en/00_data_preparation.ipynb and notebooks/tr/00_veri_hazirlama.ipynb contains three separate inaccuracies about the temporal s… |
| 40 | minor | feature-engineering | `notebooks/en/00_data_preparation.ipynb` | Confirmed as filed, with one addition and one sharpening. (1) Stale placement — confirmed. In notebooks/en/00_data_preparation.ipynb and notebooks/tr/… |
| 41 | minor | feature-engineering | `notebooks/en/03_feature_engineering.ipynb` | The annotation on the shape-check cell claims 17,816 defective rows were purged; the cell it annotates prints a frame that still has all 100,000 rows,… |
| 42 | minor | feature-engineering | `notebooks/en/01_data_overview.ipynb` | In notebooks/en/01_data_overview.ipynb, two committed markdown captions misdescribe the temporal axis. Cell 35d12bbd states "the hour values in the he… |
| 43 | minor | notebook-pipeline-consistency | `src/tuning.py:155-169` | _compute_provenance (src/tuning.py:155-169) fingerprints data/processed/100k_job_with_utilization_full.csv, which no modelling code reads: every prepa… |
| 44 | minor | notebook-pipeline-consistency | `notebooks/en/04_runtime_prediction_models.ipynb cell` | Confirmed and correctly diagnosed, with one clarification of impact. In notebooks/en/04_runtime_prediction_models.ipynb (and its tr/ mirror), `group_m… |
| 45 | minor | notebook-pipeline-consistency | `notebooks/en/00_data_preparation.ipynb markdown cell` | Notebook 00 md05 (EN and TR) contains three separate factual errors about the temporal step. (a) It names `arrival_hour`, `arrival_dayofweek`, `arriva… |
| 46 | minor | regression-this-session | `notebooks/en/04_runtime_prediction_models.ipynb` | Real but latent and partly misstated. Nothing currently reported is affected: all 20 sidecars match the current src_sha256, and the only realized skew… |
| 47 | minor | reproducibility | `src/tuning.py:347-355` | save_checkpoint's provenance carry-forward (src/tuning.py:347-350) is gated on best_params equality, but best_params for the non-learned baselines is … |
| 48 | minor | reproducibility | `tests/test_tuning.py` | The provenance machinery in src/tuning.py (_compute_provenance, the save_checkpoint carry-forward branch at lines ~327-357, model_artifact_is_current … |
| 49 | minor | simulator | `src/simulation/multi_node_simulator.py:566-598; note` | The run loop never records a utilization snapshot at the makespan (snapshot precedes the heappop; the `while events or not pending_df.empty` guard the… |
| 50 | minor | tests | `tests/test_tuning.py` | TestFinalizeDlModelSaveAllSeeds (tests/test_tuning.py:190-229) does not test the regression named in its own docstring. It asserts only that three fil… |
| 51 | minor | training-and-metrics | `notebooks/en/04_runtime_prediction_models.ipynb` | Notebook 04 applies the project's own non-negative clipping policy (src/tuning.py:1021, code_bugs-5) inconsistently across its two diagnostic cells, s… |

---

## 1. [BLOCKER] leakage — regression

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells cd07, cd08, cd09, cd14, cd15, cd16, cd17, ebfc04ef); mirrored in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb`

**Ne:** Confirmed and slightly broader than stated. Every training cell in notebooks/en/04_runtime_prediction_models.ipynb (cd07, cd08, cd09, cd14, cd15, cd16, cd17, ebfc04ef, plus the DL cells) short-circuits on `ckpt = load_checkpoint(name); if ckpt: metrics = ckpt['metrics']`, and load_checkpoint is explicitly non-enforcing, so no checkpoint is ever recomputed. Verified by execution: exp_a_xgb 6968.351273116791/n_est 7, exp_a_lgbm 5810.996001399282/1 and exp_b_lgbm_nat 4973.163012087947/63 reproduce the PRE-fix pipeline (include_cpu_only=False) exactly, while the current pipeline gives 6461.4905/71 for XGB; the .joblib artifacts on disk are post-fix (xgb_numeric.joblib scores 6461.4905 with 71 rounds, lgbm_categorical_native.joblib has 95 trees). The pre-fix numbers are in the exported thesis tables NB04_Table01/07/12, and notebook 04 contradicts itself in one run (cd11 6968.35 vs rob7cell 6461.49 / 6787.83 / 4857.24), as does notebook 05 (Model MAE 6,461.49 / 6,787.83 / 4,857.24). Two corrections: (1) scope is wider than the eight named cells - every trained-model checkpoint including DL Experiments C-F carries the same restamped provenance (2026-09-06T09:23, 55a571f) despite holding pre-09cf225 numbers, so DL rows are stale too; only the later-added baselines are current. (2) load_checkpoint does print a provenance warning, it just does not gate the load, and that warning is itself blinded for feature_engineering.py by the pre-4e4f5b5 restamping bug that commit 4e4f5b5 fixed only on the artifact side.

**Kanıt:** Ran `finalize_ml_model` with each checkpoint's own `best_params` on a pipeline built with `build_job_table_from_sample(..., include_cpu_only=False)` (the pre-fix order) and on the current `prepare_features_for_model`: exp_a_xgb : OLD mae 6968.3513, n_est_eff 7 == checkpoint 6968.3513, n_est_eff 7 | NEW mae 6461.4905, n_est_eff 71 exp_a_lgbm : OLD mae 5810.996 , n_est_eff 1 == checkpoint 5810.996 , n_est_eff 1 | NEW mae 5811.1018 exp_b_lgbm_nat: OLD mae 4973.163, n_est_eff 63 == checkpoint 4973.163012087947, n_est_eff 63 | NEW mae 4857.2432, n_est_eff 95 The saved artefacts score the NEW numbers: `joblib.load('results/models/xgb_numeric.joblib').predict(X_test)` -> MAE 6461.4905 (checkpoint says 6968.3513); `lgbm_categorical_native.joblib` -> MAE 4857.2432 with n_estimators==95 (checkpoint says 4973.163 / 63). Cell cd17: "if ckpt:\n lgb_nat_final = None\n lgb_nat_final_metrics = ckpt['metrics']". Cells cd19 and cd31 build the results tables straight from these checkpoints via `load_all_checkpoints()`.

**Sonucu:** Every MAE/MdAE/RMSE/MAPE/R2 in notebook 04's Experiment A/B tables, the comparison figure and the champion tables is a pre-fix number computed with cluster_load_cpu/active_job_count measured over GPU jobs only -- i.e. the leakage-4 defect the session's change was meant to remove is still embodied in every reported result. XGBoost (Numeric) is misreported by 507 s of MAE (6968 vs 6461, 7.3%) and LightGBM (Native Categorical) by 116 s. Worse, notebook 05's scheduling simulation loads the post-fix .joblib models, so the JCT results and the "Model MAE" column beside them describe a model that no reported metric corresponds to, and the freshly computed Per-Group Median row (3369.14) is compared against pre-fix model rows in the same table.

**Düzeltme:** Add a checkpoint-currency check mirroring `model_artifact_is_current` (e.g. treat `load_checkpoint` as a miss when `_provenance_mismatches(stored, current)` names any file in `_PROVENANCE_SRC_FILES`), then re-run notebook 04 end to end so checkpoints, .joblib/.pth artefacts and tables all come from one pipeline.

---

## 2. [BLOCKER] notebook-pipeline-consistency — regression

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells cd07/cd08/cd09/cd14/cd15/cd16/cd17/ebfc04ef -> tables cd19, cd31); mirrored in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb; backing files results/checkpoints/exp_a_*.json, exp_b_{rf,xgb,lgbm}_*.json`

**Ne:** Accurate as stated, but the scope is broader than "every ML metric". All 20 model checkpoints plus exp_ablation_features.json, exp_fc06_rolling_origin.json and exp_robustness9_groupwise_cv.json carry provenance git_commit=55a571f with the 2026-09-06T09:23 timestamp and reproduce only against the pre-fix pipeline -- this includes the deep-learning Experiments C-F (exp_c/d/e/f_{cnn,lstm,hybrid}), which consume the same three cluster-load columns (cluster_load_cpu / cluster_load_gpu / active_job_count) and are therefore stale for the same reason. So the master summary (cd31) and the comparison figure (cd32) are affected across every experiment track, not only the ML rows. Two of the claim's illustrative deltas are also negligible rather than material (exp_a_lgbm +0.11 s, exp_b_rf_oh -2.89 s), so the defect is uneven in size: the material distortions are XGBoost (Native Categorical) 6600.07->5920.48 s with R2 0.12->0.22, XGBoost (Numeric) 6968.35->6461.49 s, XGBoost (One-Hot) 6053.96->5651.60 s, and LightGBM (Native Categorical) 4973.16->4857.24 s. Severity remains blocker: notebook 04's reported accuracy comes from models fit on cluster-load features that no longer exist, while notebook 05's scheduling simulation loads the post-fix .joblib/.pth artifacts, so the accuracy tables and the scheduling results describe two different models.

**Kanıt:** I re-fitted every checkpoint's best_params twice, from the repo root. (a) With the PRE-FIX pipeline (build_job_table_from_sample(..., include_cpu_only=False) -> add_temporal -> add_categorical -> add_cluster_utilization -> sort -> chronological 80/20) every checkpoint reproduces to full precision: exp_a_rf ckpt=6796.983268 prefix_repro=6796.983268 MATCH=True exp_a_xgb ckpt=6968.351273 prefix_repro=6968.351273 MATCH=True exp_a_lgbm ckpt=5810.996001 prefix_repro=5810.996001 MATCH=True exp_b_rf_oh ckpt=6271.008543 prefix_repro=6271.008543 MATCH=True exp_b_xgb_oh ckpt=6053.956835 prefix_repro=6053.956835 MATCH=True exp_b_lgbm_oh ckpt=5305.430224 prefix_repro=5305.430224 MATCH=True exp_b_lgbm_nat ckpt=4973.163012 prefix_repro=4973.163012 MATCH=True exp_b_xgb_nat ckpt=6600.065582 prefix_repro=6600.065582 MATCH=True (b) With the CURRENT prepare_features_for_model the same best_params give different numbers: exp_a_rf reported=6796.98 current=6787.83 (-9.15) exp_a_xgb reported=6968.35 current=6461.49 (-506.86) exp_b_xgb_oh reported=6053.96 current=5651.60 (-402.36) exp_b_lgbm_nat reported=4973.16 current=4857.24 (-115.92) exp_b_xgb_nat reported=6600.07 current=5920.48 (-679.59) (c) The artifact on disk is the post-fix fit: joblib.load('results/models/rf_numeric.joblib').predict(X_test) on the CURRENT test set gives mae=6787.828971716485, i.e. exactly the current-pipeline value, not the 6796.983267743382 printed in notebook 04's Experiment A table. (d) The saved notebook output for cell cd17 is `[Exp B / LGB+Native] Loaded previous results from full checkpoint.` and cd19 displays `LightGBM (Native Categorical) 4973.16`, while cell ckpt_b_lgb_nat prints `[SKIP] lgbm_categorical_native.joblib already on disk and current.` -- the metric and the artifact come from two different fits. (e) results/checkpoints/exp_b_lgbm_nat.json records "src/feature_engineering.py": "605f75d2...", which is the sha256 of the CURRENT (post-fix) file -- the provenance is wrong, a leftover of the pre-4e4f5b5 restamp behaviour that save_checkpoint now carries forward unchanged, so load_checkpoint no longer even warns about feature_engineering.py.

**Sonucu:** Experiment A, Experiment B, the master summary (cd31), the comparison figure (cd32), the tail-sliced/unseen-user/ablation/rolling-origin analyses that print `lgb_nat_final_metrics['mae']` for comparison, and every table scraped from notebook 04 report accuracy for models fit on cluster-load features that no longer exist, while notebook 05's scheduling results come from models fit on the corrected features. The Experiment B table also mixes the two: the baseline rows (Per-User/Profile/Constant/Per-Group Median, Alibaba estimates) are unaffected by the sweep-line fix and are current, so the ML-vs-baseline gap it reports is measured across two different feature pipelines. The largest single distortion is XGBoost (Native Categorical): reported 6600.07 s MAE / R2 0.12 versus 5920.48 s / R2 0.22 with the features it was actually fit on.

**Düzeltme:** Delete results/checkpoints/exp_a_rf.json, exp_a_xgb.json, exp_a_lgbm.json, exp_b_rf_oh.json, exp_b_xgb_oh.json, exp_b_lgbm_oh.json, exp_b_lgbm_nat.json, exp_b_xgb_nat.json (and the exp_c..exp_f DL checkpoints, whose provenance carries the same falsified stamp) and re-run notebook 04 so metrics and artifacts come from one pass over the current pipeline. Separately, make the checkpoint mechanism fail closed: have load_checkpoint treat a src_sha256/data mismatch as invalid rather than printing a warning and returning the payload, and make the `[RECOVER]` branch in each ckpt_* cell write the metrics its own finalize_ml_model call returns instead of discarding them into `_` while save_checkpoint above it re-writes the loaded ones.

---

## 3. [BLOCKER] training-and-metrics — reproducibility

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells cd07, cd08, cd09, cd17, ebfc04ef, ckpt_a_rf, ckpt_a_xgb, ckpt_a_lgb, ckpt_b_lgb_nat, cc712f54 …, 20 save cells total); src/tuning.py:411-436`

**Ne:** Core claim confirmed; two refinements to the wording, neither weakening it. (1) Cell count: 26 code cells print the "[Checkpoint] WARNING: ... provenance differs" banner and then report the loaded numbers anyway; 29 is the count of "[Checkpoint] Loaded" lines, not of warning cells. (2) The claim says the artifacts "were refit under the current source tree". That is exactly true for rf_numeric and xgb_numeric, which I proved by refitting from the checkpoints' own best_params on the current pipeline and getting predictions identical to the on-disk models (max abs diff 0.0; 71 effective trees vs the checkpoint's recorded n_estimators_effective=7). It is NOT true for xgb_categorical_native: that file holds a 190-tree booster, while the current pipeline's early stopping yields 19 trees and MAE 6565.69. So that artifact is stale on a third axis - it matches neither its checkpoint (6600.07) nor a clean rerun - which makes the inconsistency worse, not better. Two consequences are understated. First, the shipped notebook contradicts itself in its own stored outputs: cd31 (master summary, fed by load_all_checkpoints()) prints LightGBM-Native 4973.16 s and XGBoost-Native 6600.07 s, while l5r9cell01, which joblib.loads those same two saved models, prints 4857.41 s and 5930.37 s. Two different MAEs for one model in one notebook. Second, the staleness produces an outright ranking inversion in the numeric track, not merely shifted magnitudes: reported RF 6796.98 s beats XGB 6968.35 s, whereas the models actually on disk give XGB 6461.49 s beating RF 6787.83 s.

**Kanıt:** Sidecar results/models/lgbm_categorical_native.joblib.provenance.json records src_sha256['src/tuning.py']='06da3e7ae70badd121327539b962d8fa6477cc8ba402554baa233ea4f1040796' (= current file), while results/checkpoints/exp_b_lgbm_nat.json records '4cdece86315a9612567ade8a1010002fb0a989cded6c0d1a972c9cbed7e8e257'. Executed (python3 from repo root, current pipeline, prepare_features_for_model(feature_mode='with_categorical_native')): lgbm_categorical_native.joblib -> MAE_clipped=4857.24 vs checkpoint=4973.16; xgb_categorical_native.joblib -> MAE_clipped=5919.79 (unclipped 5929.68) vs checkpoint=6600.07; xgb_numeric.joblib -> MAE=6461.49 vs checkpoint=6968.35; rf_numeric.joblib -> MAE=6787.83 vs checkpoint=6796.98. 29 code cells in the shipped notebook print '[Checkpoint] WARNING: <name>.json provenance differs from the current environment: … src_sha256[src/tuning.py]: 4cdece86… -> 06da3e7a…' and then '[Checkpoint] Loaded ←' and report those numbers anyway. Cell ckpt_b_lgb_nat: `save_checkpoint('exp_b_lgbm_nat', {'metrics': lgb_nat_final_metrics, …})` runs first with the stale metrics, then `lgb_nat_final, _ = finalize_ml_model('lgbm', _ckpt['best_params'], X_train_nat, y_train_nat, X_test_nat, y_test_nat, random_state=42, categorical_feature=list(cat_cols_nat))` discards the metrics of the model it is about to write to disk.

**Sonucu:** Every accuracy number in Tables 1-5, the master summary, and the DL summary of notebook 04 describes a model that no longer exists on disk, and notebook 05's scheduling simulation (which loads the .joblib/.pth files) runs the other model. The gap reaches 680 s of MAE (XGBoost Native Categorical: 6600.07 s reported vs 5919.79 s actual) and flips R² for LightGBM-Native from 0.0388 to 0.0777. Ranking claims built on these tables are not reproducible from the shipped code.

**Düzeltme:** Add a checkpoint_is_current() companion to model_artifact_is_current() that compares the stored provenance's src_sha256 against the current one, and change every `if ckpt:` guard to `if ckpt and checkpoint_is_current(name):` so a stale result is recomputed rather than reported. In the [RECOVER] branches, capture the metrics finalize_ml_model / finalize_dl_model return (`model, metrics = …`) and call save_checkpoint with them instead of dropping them into `_`. Then re-run notebook 04 end to end so checkpoints and artifacts come from one coherent run.

---

## 4. [MAJOR] config-and-dead-code — leakage

**Dosya:** `src/feature_engineering.py:611 (branch), notebooks/en/04_runtime_prediction_models.ipynb cell cd13, notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb cell cd08, tests/test_feature_engineering.py:19-44`

**Ne:** Confirmed, with two corrections to scope. CONFIRMED CORE: src/feature_engineering.py:611 `with_categorical` is a bare copy with no category restriction; the leakage-6 fix lives only in `with_categorical_native` (line 567). Every model that consumes native categoricals uses the unguarded branch — nb04 cd13 feeds X_train_nat/X_test_nat to cd17 (LightGBM-Native) and ebfc04ef (XGBoost-Native); nb05 cd08 (both GPU sizes, both languages) builds X_test_cat_base the same way. `git show 55a571f6` (the commit in the checkpoint provenance) confirms cd13 already used `with_categorical` when the results were produced. Runtime numbers reproduce exactly: with_categorical -> 681 train-dtype categories, 578 present, 103 phantom, 0 NaN in test; with_categorical_native -> 578/578, 312 NaN; 58 test-only users over 312 test rows. `pytest -k native` passes on the branch no model runs. DECISIVE MATERIALITY EVIDENCE (deterministic, 2 reps, checkpointed champion params): - LightGBM-Native: bit-identical under both branches (MAE 4857.24, R2 0.0777, 95 trees). - XGBoost-Native: with_categorical -> MAE 5920.478618315597, RMSE 13724.38, R2 0.2225, MdAE 2938.15, 190 trees; with_categorical_native -> MAE 6565.688, RMSE 14602.90, R2 0.1197, MdAE 3730.96, 19 trees. results/analysis/rank_correlation_32gpu.csv and rank_correlation_256gpu.csv report `SJF-XGBoost (Native Categorical)` with MAE 5920.478618315597 — a bit-for-bit match to the unguarded branch, proving the reported figure bypassed the fix. The unguarded branch is the optimistic one (R2 0.2225 vs 0.1197). CORRECTION 1 (claim misstated): `with_categorical_native` has 4 references, not 2, and is NOT used only by tests — nb04 en cell abc999f5 and nb04 tr cell c7d62c96 call it. However those cells use only the returned job_df for the per-user-median baseline and discard X_train/X_test, so no model features pass through the fixed branch. The claim's substance holds. CORRECTION 2 (claim overstated): "Every Experiment B native categorical number (LightGBM-Native, XGBoost-Native)" is wrong. LightGBM-Native is provably unaffected — metrics are bit-identical under both branches. Only XGBoost-Native is affected. ADDITION (claim understated): the provenance actively mislabels. nb04's save cells hard-code 'feature_mode': 'with_categorical_native', and src/tuning.py:200 _FEATURE_MODE_BY_CHECKPOINT_NAME maps exp_b_lgbm_nat -> "with_categorical_native", so results/checkpoints/exp_b_{lgbm,xgb}_nat.json record a feature_mode that was never executed. SEVERITY: major, not blocker — only one reported model's numbers change, and the fix makes XGBoost-Native worse, reinforcing rather than reversing the thesis's headline that non-learned baselines beat the trained models.

**Kanıt:** src/feature_engineering.py:611-616 -- `elif feature_mode == "with_categorical":` / `feature_cols = numeric_cols + categorical_cols` / `X_train = X_train_full[feature_cols].copy()` (no category handling at all). nb04 cd13: `prepare_features_for_model(..., feature_mode="with_categorical",)` then `for col in cat_cols_nat: X_train_nat[col] = X_train_nat[col].astype("category")` -- the exact no-op the leakage-6 comment at line 571-580 describes as broken. nb05 cd08 (both GPU variants, both languages) also uses `feature_mode="with_categorical"`. Counting `feature_mode=` across all notebooks at HEAD: Counter({'numeric_only': 10, 'with_categorical_onehot': 10, 'with_categorical': 6, 'with_categorical_native': 2}) -- and the only 2 `with_categorical_native` hits are inside tests, not notebooks. I executed both branches on the real trace: [with_categorical] user: n_categories_on_train_dtype=681, actually_present_in_train=578, test-only values still in train dtype=103, test values unseen in train=58, NaNs in test after mapping: 0. [with_categorical_native] user: n_categories_on_train_dtype=578, present_in_train=578, NaNs in test=312. tests/test_feature_engineering.py asserts `self.assertTrue(test_categories <= train_categories)` and `assertGreater(int(X_test["user"].isna().sum()), 0)` -- both pass, on the branch no notebook runs.

**Sonucu:** Every Experiment B 'native categorical' number in the thesis (LightGBM-Native, XGBoost-Native) and every notebook-05 simulation replaying those models was produced with a train-side categorical dtype whose 681 user categories include 103 users that appear in no training row, and with 58 test-only users encoded as known categories instead of missing. The test suite reports the leakage-6 fix as verified while the reported results never went through it.

**Düzeltme:** Change `feature_mode="with_categorical"` to `"with_categorical_native"` in nb04 cd13 and nb05 cd08 (en and tr), delete the now-redundant manual `.astype("category")` loop in cd13, and remove the `with_categorical` branch (and its Literal entry at line 407) so the unguarded path cannot be selected again. Re-run Experiment B and notebook 05.

---

## 5. [MAJOR] config-and-dead-code — reproducibility

**Dosya:** `notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb, notebooks/tr/05_*.ipynb, scripts/export_thesis_results.py:99-128`

**Ne:** Both notebook-05 variants are committed only partially executed (en/32-GPU: 12 of 28 code cells executed at HEAD, 0 stored figures; en/256-GPU: 14 of 28, 1 figure; the tr/ pair likewise, with 0 figures), stopping before every figure cell from section 11b onward. Because produced != EXPECTED_FIGURE_COUNT (7), _sync_thesis_figures (scripts/export_thesis_results.py:283-315) refuses all 14 NB05 entries in THESIS_FIGURE_MAP, so the default pipeline (bash scripts/run_all_experiments.sh) can never refresh them — and the 'run'/--execute mode cannot fix it either, since its auto-execute gate requires 'pngs == 0 and htmls == 0' and these notebooks have partial outputs. Consequence: the 10 nb05-fig01..fig05 files actually included by thesis/latex/chapters/6.results_and_discussion.tex are frozen at commit 6b04232 and are demonstrably stale — nb05-fig05-wait-percentile_32gpu.png still shows 20 policies under the pre-rename labels ('SJF-LGBM (Categorical)', 'SJF-LSTM (Categorical)') and the removed '— Lower is Better' titles, whereas the current notebook defines 27 policies with renamed labels and the added non-learned baselines. This violates AGENTS.md:13. Corrections to the original evidence: NB05 tables are 1 (32-GPU) and 2 (256-GPU), not 0, and three NB05_*_Table*.html files do exist in the export directory; _clean_stale_exports deletes and then re-writes NB05_256GPU-Figure01.png rather than destroying it; and regeneration is possible via --force-execute (data CSVs and all 56 models are git-tracked), so the accurate statement is that the documented default and 'run' pipelines cannot refresh these figures, not that regeneration is impossible. Two of the 14 mapped entries (nb05-fig06-load-backfill-sensitivity_{32,256}gpu.png) have never existed and are referenced by no LaTeX source.

**Kanıt:** Execution state across the listed commits for notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (code cells / executed / stored PNG outputs): e4c1b7c 28/11/0, acce262 28/11/0, 4107ebd 28/11/0, ea30507 28/11/0, 8891bc8 28/11/0, 37c8701 28/11/0, b4b7fa8 28/19/0, 4e4f5b5 28/15/1, 0570471 28/15/1, 55a571f 28/15/1. Counting `text/html` outputs containing `<table>` at HEAD: 05_scheduler_evaluation_32_gpu.ipynb tables=0, 05_scheduler_evaluation_256_gpu.ipynb tables=0, while NOTEBOOKS_EN marks both with extract_html=True (export_thesis_results.py:54-55). EXPECTED_FIGURE_COUNT = {..., "NB05_32GPU": 7, "NB05_256GPU": 7}. Current export directory contents: results/figures/thesis_export/png/ holds exactly one NB05 file (NB05_256GPU-Figure01.png) out of 14 mapped, and results/figures/thesis_export/html/ holds 14 NB04 tables and zero NB05 tables.

**Sonucu:** The 14 NB05 figures and all NB05 tables that the thesis shows came from a run that no longer exists in the repository. `bash scripts/run_all_experiments.sh` prints the 'tez şekilleri GÜNCELLENMEDİ' warning and leaves them untouched, so nobody can verify or regenerate them; `_clean_stale_exports("NB05_*")` additionally deletes the one surviving NB05 export on every run.

**Düzeltme:** Run both notebook-05 variants end to end (all 28 code cells), commit the executed notebooks with their outputs, and re-run scripts/export_thesis_results.py so the NB05 figures and tables in the export directory and the thesis come from the committed notebook state.

---

## 6. [MAJOR] en-tr-parity — reproducibility

**Dosya:** `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb (cells 'nbpm02', 'nbcm02', 'nbcz02') vs notebooks/en/04_runtime_prediction_models.ipynb (same ids); consumed by src/tuning.py:save_checkpoint`

**Ne:** The defect is real as to mechanism and consequence, but one evidentiary detail is wrong. The claim states "The checkpoints on disk are currently in the Turkish variant, i.e. written by the TR notebook" with exp_b_profile_median.json holding the Turkish estimator string and a timestamp of 2026-09-06T11:22:58. In fact the on-disk file — and the committed HEAD state, `git diff HEAD` on it is empty — holds the ENGLISH variant, `"estimator": "median lookup over profile columns ['user', 'num_inst', 'num_cpu', 'gpu_demand', 'gpu_type']"`, with timestamp 2026-09-06T11:55:49.303713+00:00; exp_b_constant_median.json holds 'single global training median, no grouping' at 11:55:49.388451 and exp_b_constant_zero.json holds 'always predicts 0' at 11:55:49.470305. So the three files are currently language-stamped by the EN notebook, not the TR one. This does not weaken the finding — if anything it is consistent with a restamp having already happened, since these three files' timestamps are detached from the 09:23:2x-39 stamp shared by the other 25 checkpoints, including their language-neutral sibling exp_b_user_median.json. Also worth stating precisely: in an environment whose provenance already equals the stored provenance, only the timestamp changes; the provenance overwrite (and the resulting permanent suppression of load_checkpoint's mismatch warning) occurs when the stored provenance is stale relative to the current source/data, which is exactly the situation the guard was added to protect.

**Kanıt:** EN 'nbpm02': `save_checkpoint('exp_b_profile_median', {..., 'best_params': {'estimator': f'median lookup over profile columns {_profile_cols}'}, ...})`. TR 'nbpm02': `save_checkpoint('exp_b_profile_median', {..., 'best_params': {'estimator': f'profil kolonları {_profile_cols} üzerinden medyan lookup'}, ...})`. Same pattern in 'nbcm02' ('single global training median, no grouping' vs 'gruplamasız tek global eğitim medyanı') and 'nbcz02' ('always predicts 0' vs 'her zaman 0 tahmin eder'). src/tuning.py:346-349 gates the carry-forward on `previous.get("best_params") == clean.get("best_params")`. The checkpoints on disk are currently in the Turkish variant, i.e. written by the TR notebook: results/checkpoints/exp_b_profile_median.json holds `{"estimator": "profil kolonları ['user', 'num_inst', 'num_cpu', 'gpu_demand', 'gpu_type'] üzerinden medyan lookup"}`. I executed the EN cell's exact payload against that file (metrics taken verbatim from the file so nothing was recomputed) and it printed: `metrics identical : True`, `timestamp CHANGED : True` (2026-09-06T11:22:58 -> 2026-09-06T11:42:39), `provenance CHANGED: True`. I restored the file afterwards.

**Sonucu:** This is exactly the reproducibility-4 failure the guard was added to prevent, reintroduced through the translation. Alternating between the English and Turkish notebooks restamps exp_b_profile_median, exp_b_constant_median and exp_b_constant_zero as freshly produced on every switch, so a stale result carried across a source or data change is relabelled current and load_checkpoint's provenance-mismatch warning is suppressed from that point on. It also means the three checkpoint files in the repository are language-stamped artefacts rather than language-neutral records of the computation.

**Düzeltme:** Keep the estimator descriptions untranslated (identical English strings in both notebooks) and move the Turkish wording into the surrounding comment, exactly as 'nbae02' and '14bc1952' already do — or give the payload a language-neutral machine key (e.g. `{'estimator': 'profile_median_lookup', 'cols': _profile_cols}`) and render the human-readable label at display time.

---

## 7. [MAJOR] en-tr-parity — consistency

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (markdown cell 'da4b8662') vs notebooks/tr/05_gorev_zamanlayici_degerlendirme_32_gpu.ipynb ('da4b8662')`

**Ne:** Confirmed as stated, with two additional errors: both language versions of the 32-GPU cell name policies ('SJF-XGBoost (Categorical)', 'SJF-LSTM (Categorical)') that do not exist in the results table, and both claim SJF-LSTM (Numeric Sequence) degrades JCT by -45.5% below FIFO when the executed table shows it improving by +51.86% — a sign reversal, not just a magnitude error.

**Kanıt:** EN 'da4b8662': "reducing mean JCT to **92,064 seconds**, which represents an astonishing **81.6% improvement** relative to the FIFO baseline (499,951s)" and "**SJF-XGBoost (Categorical)** and **SJF-LSTM (Categorical)** emerge as the absolute state-of-the-art leaders with **56.2%** and **55.2%** improvements respectively". TR 'da4b8662': "ortalama JCT'yi **15.225 saniyeye** indirmiş ve FIFO taban çizgisine (82.906 sn) kıyasla **%81.6'lık bir iyileşme** sağlamıştır" and "**SJF-XGBoost (Categorical)**, **%69.3'lük bir JCT iyileşmesi** ile en iyi performansı sergilemiş". Neither matches the notebook's own executed results table, which is byte-identical in both languages: I parsed the stored HTML output of code cell 'cd19' and it gives SJF-Oracle Mean JCT 252353.75 / 81.63%, FIFO Mean JCT 1373886.81, SJF-XGBoost (One-Hot) 61.62%, SJF-LSTM (One-Hot) 61.21%. (The 256-GPU pair agrees: both languages say 14,507 s / 50,306 s / 71.2% / 57.2% / 53.9% / -45.4%.)

**Sonucu:** The two published notebooks give a reader two different quantitative answers to the same 32-GPU experiment — the absolute Oracle and FIFO job-completion times differ by more than 6x, and the headline predictive-policy gain differs by 13 percentage points (56.2% vs 69.3%). The English text also credits two leading models while the Turkish text credits one, so the two versions support different conclusions about which architecture wins at 32 GPUs.

**Düzeltme:** Regenerate both 'da4b8662' cells from the executed 'cd19' table of the current run (Oracle 252,353.75 s, FIFO 1,373,886.81 s, 81.63%; best predictive SJF-XGBoost (One-Hot) 61.62%, SJF-LSTM (One-Hot) 61.21%) so both languages quote the same numbers and name the same models, and use the canonical model names from the standardisation pass rather than '(Categorical)'.

---

## 8. [MAJOR] export-and-artifacts — correctness

**Dosya:** `scripts/export_thesis_results.py`

**Ne:** extract_from_nb_dict() in scripts/export_thesis_results.py never inspects out["output_type"] (grep count 0), so a code cell that raised still contributes the figure matplotlib flushes after the traceback and any table already displayed before it — into results/figures/thesis_export/ and, whenever the notebook's total figure count still matches EXPECTED_FIGURE_COUNT, into thesis/latex/figures/ under the exact filename LaTeX includes — printing only [PNG]/[HTML] success lines and exiting 0. Verified end-to-end: injecting one error output into NB03's first figure cell of the real notebook published nb03-fig01-cluster-load.png to the thesis figure directory with no warning and exit status 0. The EXPECTED_FIGURE_COUNT gate is not a backstop for this: it catches only a truncated run (it correctly skipped NB05 at 0/7 and 1/7), and is blind whenever the erroring cell still emits its figure so the count is unchanged. The defect is a latent hole in the sole thesis-figure publication mechanism rather than a currently-corrupt artifact: the concrete LinAlgError instance in NB05 cells 8d1a4e10 / 7a843cee has since been fixed in the notebook (np.isfinite mask before np.polyfit, lines 81-94) and the shipped mae_spearman_vs_jct_gain_*.png no longer match the corrupt md5 the claim cites, so no thesis number is presently known-wrong — but any future crashed run republishes partial output silently. Fix: skip a cell (or fail loudly) when any of its outputs has output_type == "error".

**Kanıt:** scripts/export_thesis_results.py:155-159 `for out in cell.get("outputs", []): / data = out.get("data", {}) / if extract_png and "image/png" in data:` — there is no `if out.get("output_type") == "error"` guard anywhere in the file (`grep -c output_type scripts/export_thesis_results.py` = 0). EXECUTED: I built a notebook dict whose two figure cells each contain `{"output_type":"error","ename":"LinAlgError"}` followed by the inline `display_data` PNG that matplotlib flushes after the traceback, and called extract_from_nb_dict + _sync_thesis_figures with THESIS_FIG_DIR redirected to a scratch dir. Output: `produced: 2 tables: 1 thesis files written: 2`, and `nb03-fig01-cluster-load.png -> b'PNG-HALF-DRAWN-1'`, `nb03-fig02-correlation.png -> b'PNG-HALF-DRAWN-2'` — no message mentions that both cells raised. This is not hypothetical: at 14:51 today the working tree had notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell 8d1a4e10 and notebooks/en/05_scheduler_evaluation_32_gpu.ipynb cell 7a843cee (plus both TR twins) with outputs `['stream','display_data','stream','error','display_data']`, ename `LinAlgError`, evalue `SVD did not converge in Linear Least Squares`, raised at the cell's line 87 `slope, intercept = np.polyfit(rank_df[x_col], rank_df["JCT Improvement %"], deg=1)`; running the real run_pipeline() with the output dirs redirected to a scratch dir wrote `NB05_256GPU-Figure01.png` (md5 a2e4fc24c2e90f78883c511ef59997c3), byte-identical to that errored cell's inline PNG, plus `NB05_256GPU_Table03.html` from the same cell. The cause was visible in results/analysis/rank_correlation_32gpu.csv, whose last row `SJF-CNN-LSTM (Numeric Sequence),,,,,6702.112332730178,0.0` carries NaN rho/tau — and rank_df.to_csv() runs at cell line 60, before the crash at line 87, so the CSV artifact was published too.

**Sonucu:** A figure printed in the thesis, and an HTML table the thesis numbers are read off, can be the partial output of a cell that crashed — a scatter plot with no regression line and no Pearson r, or a table computed from an incomplete frame — while the export prints only success lines. Nothing downstream can tell it apart from a valid result.

**Düzeltme:** In extract_from_nb_dict, scan each cell's outputs first; if any has output_type == 'error', skip that cell entirely, count it, and make run_pipeline raise (or exit non-zero) naming the cell id and ename, exactly as _sync_thesis_figures already refuses to copy on a count mismatch.

---

## 9. [MAJOR] export-and-artifacts — reproducibility

**Dosya:** `scripts/export_thesis_results.py`

**Ne:** export_thesis_results.py counts and exports any stored cell output as a figure produced by the current run; it never consults execution_count, so an output left behind by a cell that was not executed (execution_count: null) occupies a figure position, is written to results/figures/thesis_export/png/, counts toward EXPECTED_FIGURE_COUNT, and — once the notebook's total matches — is copied into thesis/latex/figures under the THESIS_FIGURE_MAP filename. Because a stale output inflates the total exactly like a fresh one, it helps the count gate pass rather than trip it, defeating the guard documented at lines 73-77 and 286-297 as protecting against "a partial or stale run." Live instance: notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell d7a6e287 has execution_count: None with one image/png output, and that exact image (sha1 743b93f5…) is already sitting in results/figures/thesis_export/png/NB05_256GPU-Figure01.png from the Sep 6 export run. Corrections to the original claim: (a) today that cell occupies figure position 1, not 2 — position 2 arises only after cell 8d1a4e10 is also executed; (b) the thesis-directory copy is currently BLOCKED by the count gate (produced=1 vs expected=7), so nothing wrong is in thesis/latex/figures yet, and nb05-fig06-load-backfill-sensitivity_256gpu.png is in fact absent from thesis/latex/figures and unreferenced anywhere under thesis/. The thesis-level republication is therefore latent rather than realized; what is realized today is the mislabeled export artifact. The mechanism is general and applies to any position in any of the six mapped notebooks, and the default no-flag pipeline path will not re-execute a notebook that already holds outputs.

**Kanıt:** The extraction loop (scripts/export_thesis_results.py:151-176) reads only `cell.get("outputs", [])`; `execution_count` appears nowhere in the file. Live in the working tree right now: notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell `d7a6e287` has `execution_count: None` and one `display_data` output containing an `image/png` (verified by scanning the .ipynb JSON). THESIS_FIGURE_MAP assigns ('NB05_256GPU', 2) -> nb05-fig06-load-backfill-sensitivity_256gpu.png, so that never-executed cell's leftover image is exactly what position 2 would deliver to the thesis once the notebook's total reaches 7.

**Sonucu:** A figure carried over from a previous revision of a notebook is republished as a current result. The count gate cannot see it, because a stale output counts toward the total exactly like a fresh one — so it actually helps the gate pass.

**Düzeltme:** Reject a notebook whose code cells' execution_count values are not a strictly increasing, gap-free-from-1 sequence with no nulls among cells that carry outputs; report it the same way as a count mismatch.

---

## 10. [MAJOR] feature-engineering — leakage

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb`

**Ne:** Real, with two corrections to the claim's framing. (1) The measured effect is larger than stated and the claimant's ad-hoc n_estimators=200 refit understates it: through the actual finalize_ml_model path with the stored best_params, XGBoost-Native scores MAE 5920.48 s / 190 effective trees on the frame the notebook uses versus 6565.69 s / 19 effective trees with the with_categorical_native guard applied (not 5929.24 vs 6043.42). LightGBM is unaffected (4857.243211 on both frames). (2) The Experiment B headline value (6600.07 s in results/checkpoints/exp_b_xgb_nat.json, which feeds the Table07/Table12/NB05 model-MAE columns) is a stale checkpoint that reproduces neither path under current code, and its n_estimators_effective=19 matches the guarded path's fingerprint — so that particular headline number is not itself the leaky one. What is provably produced on the unguarded frame is: results/models/xgb_categorical_native.joblib (190 rounds) and lgbm_categorical_native.joblib (681-category dtype), notebook 04's unseen-user and burst-deduplication tables (XGB-Native MAE 5930.37), the LightGBM feature-ablation table's inputs, and both scheduler notebooks — results/analysis/rank_correlation_32gpu.csv carries exactly 5920.478618315597 for SJF-XGBoost (Native Categorical), the bit-exact unguarded value, with its rho and JCT improvement derived from those same predictions. Notebook 05 independently rebuilds the frame with feature_mode="with_categorical" (cell cd08), so switching cd13 alone would not fix the simulation path.

**Kanıt:** notebooks/en/04_runtime_prediction_models.ipynb cell `cd13`: ``` (_, X_train_nat, X_test_nat, y_train_nat, y_test_nat, _, cat_cols_nat) = prepare_features_for_model( dataset="main", ..., feature_mode="with_categorical", ) for col in cat_cols_nat: X_train_nat[col] = X_train_nat[col].astype("category") X_test_nat[col] = X_test_nat[col].astype("category") ``` `.astype("category")` on an already-categorical column is a no-op. Executed check: with_categorical: user dtype categories = 681, observed in train = 578, test-only values = 58, test NaN = 0 with_categorical_native: user dtype categories = 578, test NaN = 312 after the notebook's `.astype("category")`: train_cats == test_cats == the full 681-item list (True). Measured effect, refitting with the stored best_params from results/checkpoints/exp_b_xgb_nat.json (n_estimators reduced to 200, enable_categorical=True): predictions differ on 16,436 of 16,437 test rows; MAE 5929.24 (as used) vs 6043.42 (with the guard). For LightGBM (exp_b_lgbm_nat params, n_estimators=300) I measured no difference (0/16,437 rows), so the impact is XGBoost-Native specific, not universal. X_train_nat/X_test_nat are the inputs for `exp_b_lgbm_nat` (cell cd17), `exp_b_xgb_nat` (cell ebfc04ef), the feature ablations (cell ablcell01) and the unseen-user analysis (cell l5r9cell01). Provenance is also mislabelled: results/checkpoints/exp_b_xgb_nat.json and exp_b_lgbm_nat.json both record `"feature_mode": "with_categorical_native"`, but the data came from `with_categorical`.

**Sonucu:** The XGBoost-Native result reported in Experiment B depends on test-partition category codes being present in the training frame's dtype, and its metric changes when the documented safeguard is applied -- i.e. the guard the module and the thesis claim is in force is not in force on the reported path. tests/test_feature_engineering.py::test_native_categorical_categories_come_from_train_only passes while asserting this property of a mode the notebook does not use for its model inputs, so the gap is invisible to the test suite.

**Düzeltme:** Change cell cd13 to `feature_mode="with_categorical_native"` and delete the now-redundant `.astype("category")` loop, then re-run the native-categorical experiments and the checkpoints that depend on them. Either delete the `with_categorical` branch (lines 611-616) or make it apply the same `remove_unused_categories()` / `set_categories(train_categories)` treatment. Extend the regression test to parametrise over both mode names so a future switch back cannot pass silently.

---

## 11. [MAJOR] leakage — leakage

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb, cell 'grpmed01' (join key defined in cell 'nbae01'); mirrored in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb`

**Ne:** The Per-Group Median baseline is not "provably leakage-free" as cell grpmed01 claims: whether a test job receives an informative train-only group median or the global-median fallback is decided by a record-linkage key that contains that test job's own label (`duration`). The predicted VALUES are not themselves contaminated -- the group medians are computed from the training partition only, and the recovered group labels are the true ones (0 disagreements against the label-free key) -- so the defect is label-dependent coverage selection, not a poisoned lookup table. The consequence is nonetheless that MAE 3369.14 / R2 0.3341 is unreproducible without y_test: the best label-free linkage, (user, submit_time), yields MAE 4410.74 / R2 0.1193 (+31% MAE), and no alternative label-free key (adding num_gpu, num_cpu, num_pod, gpu_type) recovers the coverage. Two of the claim's subset counts are slightly off -- matched-by-both is n=7,316 (MAE 1,335.3) and matched-only-via-the-duration-key is n=2,270 (MAE 2,166.5), not 7,422/2,164 -- but the fallback figure (n=6,851, MAE 5,939.6) and the overall metrics are exact. Affected reported output: the "Per-Group Median (baseline)" rows in results/figures/thesis_export/html/NB04_Table07.html and NB04_Table12.html (and notebook cells cd19/cd31), plus the checkpoint results/checkpoints/exp_b_group_median.json. Fix is either to drop `duration` from the key for this baseline and report 4410.74, or to keep the 3-col key for parity with SJG/SJGG but delete the "provably leakage-free" claim and disclose that coverage is label-keyed.

**Kanıt:** Cell grpmed01: `_grp_lookup = (_raw_main.loc[~_main_dup, ["job_id"] + _join_cols].merge(_est_raw.loc[~_est_dup, _join_cols + ["group"]], on=_join_cols, how="inner").set_index("job_id")["group"])` with `_join_cols = ["user", "submit_time", "duration"]` from nbae01. Executed both keyings on the real data: join on (user, submit_time, duration): lookup 61,203 job_ids, 9,586/16,437 test jobs matched, MAE 3369.14, R2 0.3341 (reproduces checkpoint exp_b_group_median exactly) join on (user, submit_time) only : lookup 49,113 job_ids, 7,316/16,437 test jobs matched, MAE 4410.74, R2 0.1193 Where both keys produce a label the labels agree exactly (0 disagreements), so the target in the key buys nothing but coverage: 12,090 extra job_ids, 2,164 extra test jobs. Error breakdown of the reported run: matched-by-both n=7,422 MAE 1,358.9; matched-only-via-the-duration-key n=2,164 MAE 2,126.0; fallback n=6,851 MAE 5,939.6.

**Sonucu:** The headline Per-Group Median MAE of 3369.14 s (checkpoint exp_b_group_median, and rows in cd19 and cd31) is not a held-out number: dropping the target from the linkage key moves it to 4410.74 s (+31%) and R2 from 0.334 to 0.119. Since this baseline was added specifically to be the auditable, leakage-free counterpart to the un-auditable SJG/SJGG columns and now leads or matches every model in the study, a contaminated value here misstates the study's central comparison. The same join underlies alibaba_estimate_*_pred in nbae01.

**Düzeltme:** Recover `group` with a key that excludes `duration` (e.g. (user, submit_time), or job_id where it is verifiable), or keep the current key but report the metric only over the coverage-matched subset together with the coverage-restricted numbers for the models it is compared against; and delete the "provably leakage-free" claim unless the key is target-free.

---

## 12. [MAJOR] leakage — leakage

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb, cell 'cd13' (and notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb, cell 'cd13'); guard in tests/test_feature_engineering.py:18-43`

**Ne:** The leakage-6 mitigation in prepare_features_for_model's "with_categorical_native" branch is bypassed by every path that produces a reported number: cd13 (EN and TR notebook 04) and cd08 (both notebook 05 variants) build the native-categorical matrices with feature_mode="with_categorical", whose branch applies no restriction, and the following .astype("category") is a no-op on an already-categorical column. The resulting X_train['user'] carries all 681 dataset categories (578 actually occurring in train, 58 test-only) instead of the 578 the model is fit on. The checkpoints exp_b_lgbm_nat.json / exp_b_xgb_nat.json nonetheless record 'feature_mode': 'with_categorical_native', the l5r9cell01 comment asserts the train-only restriction was applied, and the regression test at tests/test_feature_engineering.py:18-43 passes while exercising only the unused branch -- so nothing in the repo surfaces the discrepancy. Applying the restriction moves XGBoost (Native Categorical) from MAE 5920.48 / R2 0.2225 to MAE 6565.69 / R2 0.1197 and its Spearman rho from 0.5126 to 0.4713, values recorded verbatim in results/analysis/rank_correlation_32gpu.csv and rank_correlation_256gpu.csv and fed into the exported figure thesis/latex/figures/mae_spearman_vs_jct_gain_{32,256}gpu.png. Three corrections to the claim as filed. (1) The with_categorical_native branch pre-existed; commit f599aff modified it rather than adding it. (2) The 645 s MAE gap is not the leakage acting on the affected rows -- on the 312 train-unseen test rows the unfixed model is actually better (MAE 6495.66 vs 5943.88); the gap arises on the 16,125 train-seen rows because remove_unused_categories() renumbers the category codes, changing XGBoost's categorical split search and hence the fitted model. So the fix changes the number, but not because the unfixed number was leakage-inflated on the affected rows. (3) The thesis's current Experiment B table (tab:predresults / appendices.tex:74) does not move, because its only native row is LightGBM (Native Cat.), which is bit-identical under both modes; the affected model appears only in the results/analysis rank-correlation outputs and the exported scatter figure.

**Kanıt:** cd13: `prepare_features_for_model(..., feature_mode="with_categorical")` then `for col in cat_cols_nat: X_train_nat[col] = X_train_nat[col].astype("category")`. Executed both modes: with_categorical -> X_train['user'] carries 681 categories, only 578 occur in train, 58 occur only in test, 0 test rows NaN with_categorical_native -> 578 categories, 312 test rows mapped to NaN Refitting with the checkpoint's own best_params: XGBoost-Native MAE 5920.48 / R2 0.2225 under the mode the notebook uses vs MAE 6565.69 / R2 0.1197 under the fixed mode (LightGBM-Native is insensitive: 4857.2432 either way). l5r9cell01 comment: "those two were categorical-restricted to TRAIN-only categories for the chronological split (leakage-6) specifically". src/tuning.py:200 `_FEATURE_MODE_BY_CHECKPOINT_NAME = {"exp_b_lgbm_nat": "with_categorical_native"}`.

**Sonucu:** The leakage-6 mitigation is inert: no reported model was ever fit with train-only categories, and the checkpoints and the in-notebook comment both state otherwise, so a reader (and the test suite) cannot tell. The number is materially sensitive to it -- applying the fix moves XGBoost (Native Categorical) by 645 s of MAE and nearly halves its R2 (0.22 -> 0.12), which changes where that model sits in the Experiment B ranking.

**Düzeltme:** Change cd13's native call to `feature_mode="with_categorical_native"` (dropping the notebook-side `.astype("category")`, which would undo the restriction), re-run Experiment B, and extend tests/test_feature_engineering.py to assert on the mode the notebooks actually call rather than only on the mode that carries the fix.

---

## 13. [MAJOR] leakage — leakage

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb, cell 'fc06cell01'`

**Ne:** Real, and stronger than stated. In notebooks/en/04_runtime_prediction_models.ipynb cell 'fc06cell01' the three F-C06 rolling-origin windows are not merely contained in the tuning partition -- they are (to one row) the hyperparameter-search CV validation folds themselves. configs/models.yaml sets tuning.common.cv: 3 and src/tuning.py:538-554 _make_cv returns TimeSeriesSplit(n_splits=3) over the data passed in, so searching on the 65,747-row chronological training partition (cd07 RF on X_train; ebfc04ef XGB-native and cd17 LGBM-native on X_train_nat) produced validation folds [16439,32874], [32875,49310], [49311,65746]. The F-C06 folds, from TimeSeriesSplit(n_splits=4) over the 82,184-row _cv_X = concat(_tr_rows, _te_rows), are [16440,32875], [32876,49311], [49312,65747] -- 16,435/16,436 = 99.9939% overlap in every window, with matching train prefixes. Since cv=3 selects params by minimising mean MAE across exactly those three folds, and finalize_ml_model (src/tuning.py:881) only refits (no re-search), the reported window MAEs (LGBM-Native 4964.36 / 4273.91 / 3456.07 in results/checkpoints/exp_fc06_rolling_origin.json) are a re-scoring of the model-selection folds, not an independent walk-forward check. One refinement to the original wording: within each window the arrow of time is respected (train on a strict prefix, test on the following segment), so the defect is hyperparameter-selection optimism / CV-score reuse rather than target or temporal leakage. It still defeats the cell's stated purpose, the optimism is not equal across the three families (different search spaces and candidate counts), and no caveat is documented anywhere -- the cell instead calls these "additional origins" and implies the excluded 4th fold is the only non-independent one.

**Kanıt:** `_tscv = TimeSeriesSplit(n_splits=4); _ro_splits = list(_tscv.split(_cv_X))` over `_cv_X = pd.concat([_tr_rows, _te_rows])` (82,184 rows). Computed the fold ranges: window 1 test = rows [16440, 32875], window 2 test = [32876, 49311], window 3 test = [49312, 65747]; the main chronological training partition is rows [0, 65747). So windows 1 and 2 are 100% inside it and window 3 is 16,435/16,436 inside it. Hyperparameter search ran on that partition: cd17 `run_randomsearch_lgbm(X_train_nat, y_train_nat, ...)` / `run_gridsearch_iterative("lgbm", ..., X_train_nat, y_train_nat, ...)`, and src/tuning.py:538 `_make_cv` returns `TimeSeriesSplit(n_splits=common["cv"])` over the data handed in.

**Sonucu:** The rolling-origin MAEs (checkpoint exp_fc06_rolling_origin, e.g. LightGBM-Native 4964.36 in window 1, 4273.91 in window 2) are optimistically biased and are not an independent check. The cell's stated purpose -- deciding whether the single-split model ranking 'is not stable across the trace, only representative of its last slice' -- cannot be answered from windows whose rows steered hyperparameter selection, and the bias is not equal across the three model families.

**Düzeltme:** Re-run the search inside each window's own training prefix (nested walk-forward), or state in the cell and the table caption that windows 1-3 fall inside the hyperparameter-selection data and are therefore not out-of-sample with respect to tuning.

---

## 14. [MAJOR] leakage — reproducibility

**Dosya:** `src/tuning.py:305-363 (save_checkpoint) and results/checkpoints/exp_b_lgbm_nat.json`

**Ne:** All 31 tree- and DL-model checkpoints in results/checkpoints/ record src_sha256["src/feature_engineering.py"] = 605f75d2... , the CURRENT post-fix hash, even though their metrics were produced by the pre-09cf225 sweep line. Verified for exp_b_lgbm_nat: its stored metrics (mae 4973.163012087947, rmse 15259.649130329884, r2 0.038788364148422794, mape 379.0944089701942, mdae 785.6718295594128, n_est_eff 63) reproduce bit-identically only with include_cpu_only=False; the current pipeline gives mae 4857.243, r2 0.0777, n_est_eff 95. The wrong hash was originally stamped by the PRE-4e4f5b5 restamping bug (the checkpoint was last written at 2026-09-06T09:23:30Z, after 09cf225 landed, so _compute_provenance() read the already-fixed file); save_checkpoint:347-355 now preserves rather than creates it, freezing it until something is genuinely recomputed. The consequence is narrower but not weaker than claimed: load_checkpoint's warning DOES still fire (git_commit, data_sha256, src_sha256[src/tuning.py]), so the signal is not fully suppressed -- but it never names the one file whose change actually moved the numbers, and the fields it does name (commit hash, processed-CSV hash that use_processed=False means the model pipeline never reads) change on every commit and are therefore no evidence of a stale result. The reproducibility-4 record thus certifies a reported number -- LightGBM (Native Categorical), MAE 4973.16 s / R2 0.039 in notebook 04 cells 48 and 92 -- as having been produced by the committed feature pipeline, when the committed pipeline actually yields 4857.24 s / R2 0.078.

**Kanıt:** `_provenance_mismatches(json.load(open('results/checkpoints/exp_b_lgbm_nat.json'))['provenance'], _compute_provenance())` returns only ["git_commit: '55a571f...' -> 'e4c1b7c...'", "data_sha256: ...", "src_sha256[src/tuning.py]: ..."] -- src/feature_engineering.py is absent because the stored hash 605f75d280da53a2fd5fa9e15b7beda56e3b90d749d7cd6614c808dcbf1d08e6 equals `shasum -a 256 src/feature_engineering.py` today, while the same checkpoint's metrics (4973.163012087947, n_est_eff 63) reproduce only under `include_cpu_only=False`. `git merge-base --is-ancestor 09cf225 55a571f` confirms the recorded commit already contains the sweep-line fix. save_checkpoint:347-355 `same_result = (previous.get("metrics") == clean.get("metrics") and previous.get("best_params") == clean.get("best_params")) ... clean["provenance"] = previous["provenance"]`.

**Sonucu:** The provenance record certifies stale metrics as having been produced by the current feature pipeline, which is the exact failure the reproducibility-4 machinery was added to prevent, and it suppresses the only signal a reader or a re-runner would get that the Experiment A/B numbers do not correspond to the committed code.

**Düzeltme:** Do not carry provenance forward when the metrics were never recomputed under it -- either recompute the affected checkpoints (see the stale-checkpoint finding) or mark the payload `status: "stale"` and have `_provenance_mismatches` report the feature-engineering hash from the run that actually produced the numbers.

---

## 15. [MAJOR] notebook-pipeline-consistency — leakage

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb cell cd13 and notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb cell cd08; src/feature_engineering.py:611-616 vs 567-583; tests/test_feature_engineering.py:18`

**Ne:** The leakage-6 mitigation is dead code for every model the study reports. src/feature_engineering.py applies train-only category derivation (remove_unused_categories + pd.Categorical(..., categories=train_categories), L567-583) exclusively inside the `with_categorical_native` branch, but notebooks/en/04_runtime_prediction_models.ipynb cell cd13 and notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb cell cd08 both request `feature_mode="with_categorical"` (L611-616), whose only post-processing is a no-op `.astype("category")` in the notebook. The single `with_categorical_native` call in any notebook is the Per-User-Median cell, which discards the returned X matrices. Verified by execution: with_categorical yields user ncat=681 / test NaN=0 / gpu_type ncat=5, while with_categorical_native yields 578 / 312 / 4. The saved artifact confirms the leaky path was the one that ran under current source: results/models/lgbm_categorical_native.joblib booster_.pandas_categorical = [681 users, ['CPU','MISC','P100','T4','V100']], and its .provenance.json records the current src hashes. Every native checkpoint (results/checkpoints/exp_b_{lgbm,xgb}_nat.json and the baseline checkpoints) nonetheless records 'feature_mode': 'with_categorical_native', a label that is factually wrong. tests/test_feature_engineering.py:18 asserts the fix on `with_categorical_native` and passes, while no test anywhere exercises `with_categorical` — the regression guard protects a path no reported model uses. Commit f599aff ("fix(leakage-6)") touched only src/feature_engineering.py and the test, never the notebooks, and pre-fix the two branches were behaviourally identical, which is how the fix landed on the unused branch unnoticed. Two corrections to the reporter's framing. (1) The material metric change is XGBoost-Native only: refitting with the checkpoints' own best_params gives XGB-Native MAE 5920.48 s / R2 0.2225 under `with_categorical` vs MAE 6565.69 s / R2 0.1197 under `with_categorical_native` (an 11% MAE swing, R2 nearly halved), while LGBM-Native is bit-identical at MAE 4857.24 s / R2 0.0777 in both modes. Calling LightGBM materially affected is not supported; it is affected only in that its stored category vocabulary misdescribes what it was fit on. (2) The 5920.48 figure is a fresh rerun of the notebook's path, not the number the thesis pipeline currently prints: results/checkpoints/exp_b_xgb_nat.json reports MAE 6600.07 / R2 0.1210 and is itself stale (its provenance warning fires on git_commit, data_sha256 and tuning.py hash), so the currently reported native metrics are attributable to neither mode. Author's own commit message also notes no target information leaks (a test-only user's rows never appear in train); this is a protocol/metadata deviation plus a broken-fix-and-false-test-assurance defect, not target leakage.

**Kanıt:** grep over notebooks/en/*.ipynb shows `feature_mode="with_categorical_native"` appears exactly once (line 1499, the Per-User Median cell, which uses only job_df), while the models come from line 917 `feature_mode="with_categorical"`. Executed check: with_categorical user train ncat=681 test NaN=0 gpu_type ncat=5 with_categorical_nat user train ncat=578 test NaN=312 gpu_type ncat=4 The saved artifact confirms which one was used: joblib.load('results/models/lgbm_categorical_native.joblib').booster_.pandas_categorical gives n_categories=681 for `user` and ['CPU','MISC','P100','T4','V100'] (5) for `gpu_type` -- the full train+test category set, not the 578/4 the train partition contains. The regression test tests/test_feature_engineering.py::test_native_categorical_categories_come_from_train_only asserts on `feature_mode="with_categorical_native"`, so it passes on a code path no reported model uses. The one-hot path is unaffected (encoder sees only observed train values: 578 users + 4 gpu_types + 9 numeric = 591, matching the notebook's printed `X_train_oh : (65747, 591)`), which is why only the native models are affected.

**Sonucu:** The two 'Native Categorical' models -- one of which (LightGBM) is the thesis's best trained predictor -- are fit with a category vocabulary that includes 103 users and one gpu_type appearing only in the test partition, i.e. the fix the code and its regression test claim is in force is not in force for any reported number. Re-running XGBoost (Native Categorical) with the leak-free mode changes its result materially: MAE 5920.48 s / R2 0.2225 (leaky, as run) vs 6565.69 s / R2 0.1197 (leak-free), an 11 % MAE swing.

**Düzeltme:** Change `feature_mode="with_categorical"` to `feature_mode="with_categorical_native"` in notebook 04 cd13 and notebook 05 cd08 (both languages), drop the now-redundant `.astype("category")` loops that follow, retrain, and either delete the unused `with_categorical` branch of prepare_features_for_model or add a test that pins its (leaky) behaviour so the two modes cannot be confused again.

---

## 16. [MAJOR] notebook-pipeline-consistency — reproducibility

**Dosya:** `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb cells nbcm02, nbcz02, nbpm02 (and every other Turkish save_checkpoint cell); src/tuning.py:326-346 (save_checkpoint)`

**Ne:** Scope is narrower than stated and status is stronger. Only three TR save_checkpoint cells differ from their EN counterparts -- nbcm02 (exp_b_constant_median), nbcz02 (exp_b_constant_zero), nbpm02 (exp_b_profile_median) -- not "every other Turkish save_checkpoint cell"; all remaining ~25 calls, including every ML/DL best_params dict and exp_b_user_median's estimator string, are byte-identical between the two notebooks and carry forward correctly. Conversely, the defect is not a prospective working-tree risk: it already executed and is committed at HEAD in commit d14b8f6, which erased the true provenance (55a571f / data c1897f83 / tuning.py 4cdece86) from all three checkpoints and replaced it with e4c1b7c / 3f3d6d19 / 06da3e7a while the metrics blocks stayed byte-identical. No currently reported metric value is itself wrong -- the erased data_sha256 tracks data/processed/100k_job_with_utilization_full.csv, which commit e4c1b7c states nothing reads today, and the three baselines do not depend on tuning.py's changed logic -- so the damage is to the reproducibility record rather than to a number: three rows of the Experiment B table and the master summary now carry a provenance stamp asserting a computation context that never produced them, and load_checkpoint's stale-result warning is permanently silenced for them while it still fires for the neighbouring exp_b_user_median row.

**Kanıt:** Working-tree diff of results/checkpoints/exp_b_constant_median.json: metrics block unchanged, `"estimator": "single global training median, no grouping"` -> `"gruplamasız tek global eğitim medyanı"`, and provenance `git_commit: 55a571f... -> e4c1b7c...` plus `src_sha256[src/tuning.py]: 4cdece86... -> 06da3e7a...` and `data_sha256: c1897f83... -> 3f3d6d19...`. Same pattern in exp_b_constant_zero.json and exp_b_profile_median.json. The Turkish cell nbcm01 reads `if ckpt: constant_median_metrics = ckpt['metrics']` -- the metrics were not recomputed, only the label changed, yet the record now claims they were produced at HEAD.

**Sonucu:** Any checkpoint touched by the Turkish notebook loses the record of when its numbers were actually computed, so load_checkpoint's mismatch warning is silenced for it and a genuinely stale result becomes indistinguishable from a fresh one -- the same failure mode that let the pre-fix ML metrics survive undetected.

**Düzeltme:** Keep the estimator description out of the provenance-sensitive comparison: either use a single language-independent identifier (e.g. `{'estimator': 'constant_median'}`) in both notebooks and render the human-readable label at display time, or restrict save_checkpoint's `same_result` test to `metrics` alone plus a language-independent params hash.

---

## 17. [MAJOR] regression-this-session — regression

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (also notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, notebooks/tr/05_gorev_zamanlayici_degerlendirme_32_gpu.ipynb, notebooks/tr/05_gorev_zamanlayici_degerlendirme_256_gpu.ipynb)`

**Ne:** Correct statement: notebook 05 (both cluster sizes, EN and TR) is left in a partially executed state in which none of its 7 figure cells carry stored outputs, so scripts/export_thesis_results.py extracts 0 figures for NB05_32GPU and 1 for NB05_256GPU against EXPECTED_FIGURE_COUNT=7 and _sync_thesis_figures correctly refuses to refresh thesis/latex/figures. The result is that the ten NB05 figures Chapter 6 includes (nb05-fig01..fig05 for 32 and 256 GPU) are still the Aug 31 15:41 renders, i.e. they predate this session's own NB05 fixes (the added matplotlib style block that repaired 1.5-2.9 pt printed text, per commit 90c81fd) and predate the Sep-5 additions of the SJF-ProfileMedian, SJF-AlibabaEstimate and SJG/SJGG baselines and the non-negative prediction clipping -- so the printed scheduler figures no longer match the code that is supposed to have produced them. Two parts of the original claim are overstated and should be dropped: (a) the results are NOT "unreproducible" -- every cell source is committed and `python scripts/export_thesis_results.py --force-execute` regenerates outputs, figures and thesis copies in one step; (b) re-running the exporter does not "wipe what is on disk and write nothing back" -- it deletes and then rewrites whatever the stored outputs still contain, and the measured net loss is exactly one file, results/figures/thesis_export/html/NB05_32GPU_Table02.html, while thesis/latex/figures is left untouched by design. One addition the claim missed and that matters for the fix: `--execute` will not repair this, because its auto-execute branch is gated on `pngs == 0 and htmls == 0` and both NB05 notebooks still yield a non-zero table count; only `--force-execute` re-executes them.

**Kanıt:** Executed `git show <rev>:notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` and counted outputs per revision: 4e4f5b5 cells_with_outputs=15 png=1 html=5 b4b7fa8 cells_with_outputs=19 png=0 html=7 37c8701 cells_with_outputs=11 png=0 html=0 HEAD cells_with_outputs=11 png=0 html=0 Executed scripts/export_thesis_results.py's own extractor against the working-tree notebooks (PNG_DIR/HTML_DIR redirected to a temp dir): NB05_32GPU pngs=0 htmls=0 expected_figs=7 thesis positions expected=[1..7] produced=[] NB05_256GPU pngs=2 htmls=3 expected_figs=7 thesis positions expected=[1..7] produced=[1, 2] NB04 pngs=5 htmls=14 expected_figs=5 produced=[1,2,3,4,5] run_pipeline() calls `_clean_stale_exports(prefix)` unconditionally before `extract_from_nb_dict(...)`, and `git log --diff-filter=D` shows the deletions already happened this session: 37c8701 -> results/figures/thesis_export/html/NB05_32GPU_Table01.html (…Table02-05 likewise) 6b04232 -> results/figures/thesis_export/png/NB05_32GPU-Figure02.png (…Figure03-05 likewise) `ls results/figures/thesis_export/{png,html} | grep -c NB05_32GPU` returns 0.

**Sonucu:** Every Chapter-5 scheduler table and figure the thesis reports is now unreproducible from the committed notebooks. Re-running the export pipeline destroys the NB05 artifacts that remain and emits only a warning; _sync_thesis_figures() then refuses to update thesis figures for both NB05 prefixes (produced 0 and 2 against EXPECTED_FIGURE_COUNT 7), so the thesis silently keeps pre-session copies of those charts.

**Düzeltme:** Re-execute all four notebook 05 files top to bottom (e.g. `python scripts/export_thesis_results.py --execute` for each language) and commit the notebooks WITH their outputs, then re-run the exporter so NB05_32GPU/NB05_256GPU figures and tables are regenerated. Separately, move `_clean_stale_exports(prefix)` to after a successful non-empty extraction so an empty notebook cannot delete good exports.

---

## 18. [MAJOR] regression-this-session — regression

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells cd19, cd31) and notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb (same cell ids)`

**Ne:** The new Per-Group Median baseline was wired into both results tables without the None-guard and without the checkpoint fallback that every other entry in those cells has. grpmed01 sets `group_median_metrics = None` when the Alibaba estimate export is missing, and cd19/cd31 then subscript None.

**Kanıt:** grpmed01 (both languages): `if not _alibaba_available:\n print("[Exp B / GroupMedian] SKIPPED: the group field arrives with the estimate file.")\n group_median_metrics = None` cd19 row (unguarded), beside three Alibaba rows that ARE guarded: `{"Model": "Per-Group Median (baseline)", "Test MAE (s)": group_median_metrics["mae"], ...},` `{"Model": "Alibaba Estimate (baseline)", ... } if alibaba_estimate_metrics is not None else None,` cd31 row (unguarded), beside the Alibaba loop that skips on falsy (`_ali_m = globals().get(_ali_key); if not _ali_m: continue`): `{"Experiment": "B — ML Categorical", "Track": "Categorical", "Model": "Per-Group Median", "MAE (s)": group_median_metrics["mae"], ...},` Programmatic check of both cells: cd19 has 12 `... in dir() else _m('exp_b_...')` fallback lines and cd31 has 24, and `"exp_b_group_median" in src` is False for both — group_median_metrics is the only metric in either cell with no checkpoint fallback. Executed the exact list-comprehension shape with group_median_metrics=None: `TypeError: 'NoneType' object is not subscriptable`.

**Sonucu:** With configs/paths.yaml's baseline_estimate_file absent (a configuration nbae01/nbae02 explicitly support and print a SKIPPED message for), notebook 04 now dies at cell cd19 with a TypeError. Everything downstream — the Experiment B table, the master summary cd31, and the comparison figure cd32 — is lost, in both language notebooks. Before this session's change the notebook ran to completion in that configuration. Independently, cd19/cd31 can no longer be re-run standalone after a kernel restart, which is the whole point of their `_m('exp_b_...')` fallback block.

**Düzeltme:** In both language notebooks add `group_median_metrics = group_median_metrics if 'group_median_metrics' in dir() else _m('exp_b_group_median')` to the fallback block of cd19 and cd31, and append `if group_median_metrics else None` (cd19) / a `if not group_median_metrics: skip` guard (cd31) to the Per-Group Median row, matching the Alibaba rows.

---

## 19. [MAJOR] reproducibility — reproducibility

**Dosya:** `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell r4cell (identical in 05_scheduler_evaluation_32_gpu.ipynb and both notebooks/tr/05_*.ipynb)`

**Ne:** notebooks/en/05_scheduler_evaluation_256_gpu.ipynb cell r4cell (identical in 05_scheduler_evaluation_32_gpu.ipynb and notebooks/tr/05_gorev_zamanlayici_degerlendirme_{32,256}_gpu.ipynb) gates the robustness-4 multi-seed DL simulation on model files named by seed INDEX -- `lstm_categorical_pt_seed{s}.pth` / `lstm_categorical_seq_seed{s}.pth` for s in (0, 1, 2) -- while notebook 04 writes them by seed VALUE via save_all_seeds_to + src/tuning.py:1979 `.format(seed=seed)` with DL_SEEDS = [42, 1337, 2024]. On disk only lstm_categorical_{pt,seq}_seed{42,1337,2024}.pth exist, so the gate evaluates False and the cell has always taken its else branch: eight committed versions carrying output for it (90c81fd, d4ddc96, 7069423, 3a72b53, ee1fa70, 83e51f8, 6b04232, b4b7fa8) store only the SKIPPED line, and at HEAD the cell's outputs are stripped so it shows nothing. Consequences, stated precisely: no thesis-reported number, figure or table is wrong, because seed_sim_df is consumed only inside the if-branch and nothing downstream depends on it -- the failure is silent. What is defective is reproducibility: the seed-robustness check that src/tuning.py's multi-seed machinery (seeds / save_all_seeds_to) was added to support has never run, so whether the reported 0.19pp (256-GPU) / 0.67pp (32-GPU) LSTM-Categorical-vs-XGBoost-Categorical JCT gap survives initialisation noise is untested despite notebook 04 recording large inter-seed MAE std (exp_d_lstm 509s, exp_f_lstm 651s); the SKIPPED message misattributes the cause to notebook 04 not having saved the files, when notebook 04 is correct and notebook 05's filename pattern is the bug, so a reader who follows the message and re-runs notebook 04 will see SKIPPED forever; and tests/test_tuning.py:198 test_every_seed_model_is_saved_to_its_own_path passes seeds=[0, 1, 2], so the robustness-4 regression test exercises the index-shaped names and passes without ever touching the value-shaped names the pipeline actually produces. Fix is one line per notebook: change `for s in (0, 1, 2)` to the actual DL_SEEDS values (42, 1337, 2024), after which the gate passes against the checkpoints already on disk.

**Kanıt:** r4cell: `_seed_paths_static = {s: MODEL_DIR / f"lstm_categorical_pt_seed{s}.pth" for s in (0, 1, 2)}` and `_seed_paths_seq = {... f"lstm_categorical_seq_seed{s}.pth" ...}`. Notebook 04 cell cd03 sets `DL_SEEDS = [42, 1337, 2024]` and cells cd28/8c2b4283 pass `save_all_seeds_to=str(MODEL_DIR / "lstm_categorical_pt_seed{seed}.pth")`, which src/tuning.py:1979 fills with `.format(seed=seed)`. On disk: lstm_categorical_pt_seed42.pth / _seed1337.pth / _seed2024.pth (and the _seq equivalents) exist; seed0/1/2 do not. Executed check: `all(p.exists() ...)` -> False. Every committed version of the notebook that has outputs for this cell (b4b7fa8, 6b04232, 83e51f8, ee1fa70, 3a72b53, 7069423, d4ddc96) stores exactly: '[robustness-4] SKIPPED: per-seed LSTM-Categorical model files not found on disk (re-run notebook 04's Experiment D/F LSTM cells, which now save all 3 seeds).'

**Sonucu:** The seed-robustness check that the whole multi-seed machinery (finalize_dl_model's `seeds`/`save_all_seeds_to`) exists to support has never executed once. The scheduler results still rest on a single seed-42 LSTM, and the notebook's own message misattributes the failure to notebook 04 not having saved the files -- so a reader re-running notebook 04 will see the same SKIPPED message forever. The 0.19-0.67pp LSTM-vs-XGBoost JCT gap the cell was written to test remains untested against initialisation noise.

**Düzeltme:** Derive the paths from the seed values actually used: `for s in DL_SEEDS` (import or hard-code [42,1337,2024]), or better, glob `MODEL_DIR.glob('lstm_categorical_pt_seed*.pth')` and parse the suffix. Also turn the silent `else` into a loud failure (or at least print the paths it looked for) so a name mismatch cannot masquerade as 'files not produced'.

---

## 20. [MAJOR] reproducibility — provenance

**Dosya:** `src/tuning.py:155-166 (_compute_provenance); configs/paths.yaml:21; src/feature_engineering.py:440-470 (prepare_features_for_model)`

**Ne:** The provenance guard hashes the wrong file in both directions. `_compute_provenance` (src/tuning.py:155-166) records `data_sha256` of data/processed/100k_job_with_utilization_full.csv, which no training path reads -- every prepare_features_for_model call in notebooks 04 and 05 uses the default use_processed=False and loads the raw trace data/alibaba_cluster_trace/pai_job_no_estimate_100K.csv, which is never hashed. False positive: notebook 00 rewrote the processed CSV in commit e4c1b7c, so 26 of the 31 checkpoints in results/checkpoints (not 29 of 32, as the claim states) now emit a data_sha256 mismatch for a file with no causal role in their numbers, and that spurious line is printed in the same warning block as the genuine src_sha256[src/tuning.py] mismatch. False negative, and broader than the claim states: all 20 results/models/*.provenance.json sidecars also carry the stale hash, and model_artifact_is_current (src/tuning.py:394-395) compares ONLY src_sha256 -- it ignores data_sha256 entirely -- so a replaced or re-sampled raw trace would produce no warning on any checkpoint and would not invalidate any cached model artifact. No currently reported number changes, but the reproducibility-4 mechanism the thesis relies on to make stale-vs-fresh results visible is watching a file that cannot affect them while ignoring the one that can.

**Kanıt:** src/tuning.py:157 `data_path = _PROJECT_ROOT / data_cfg["processed_data_dir"] / data_cfg["processed_full_file"]` with configs/paths.yaml:21 `processed_full_file: "100k_job_with_utilization_full.csv"`. prepare_features_for_model reads that file only under `if use_processed:`; a scan of all cells in notebooks/en/*.ipynb finds `use_processed=True` in exactly one cell (00_data_preparation.ipynb cd10) -- notebooks 03, 04 and 05 all use the default `use_processed=False`, i.e. `load_sample(which=dataset)` -> data/alibaba_cluster_trace/pai_job_no_estimate_100K.csv. Consequence visible today: notebook 00 rewrote the processed CSV at 14:22 (commit e4c1b7c), changing data_sha256 from c1897f83 to 3f3d6d19, so 29 of 32 checkpoints now emit a data_sha256 mismatch warning for a file that had no effect on any of their numbers.

**Sonucu:** Every checkpoint warning about `data_sha256` is currently a false positive, which trains the reader to ignore the warning block -- and the block also carries the genuine `src_sha256[src/tuning.py]` mismatch (finding models-and-checkpoints-provenance-diverge). Conversely, if the raw trace file were replaced or re-sampled, no checkpoint and no model artifact would report anything.

**Düzeltme:** Hash the files actually loaded: data/alibaba_cluster_trace/pai_job_no_estimate_100K.csv and pai_job_duration_estimate_100K.csv (from PathsConfig), as a dict of {filename: sha256}. Keep the processed file only if a caller passed use_processed=True.

---

## 21. [MAJOR] reproducibility — provenance

**Dosya:** `src/tuning.py:109`

**Ne:** The provenance/refit guard hashes only src/feature_engineering.py and src/tuning.py, and model_artifact_is_current (src/tuning.py:396) compares nothing but that src_sha256 dict. Changes to src/models/dl_runtime_predictor.py (RuntimePredictorLSTM:37, RuntimePredictorCNN:97, RuntimePredictorCNNLSTM:153), src/models/evaluation.py, src/data_loading.py, configs/models.yaml (cv, random_state, n_iter and every rf/xgb/lgbm/cnn/lstm/cnn_lstm search grid) and configs/paths.yaml therefore leave every one of the 20 model artifacts in results/models/ reported as current, so notebook 04's 21 save cells skip refitting and notebook 05 simulates with architectures/grids that no longer match the published code. Two corrections to the original evidence: (1) load_checkpoint is silent only for UNCOMMITTED edits — git_commit is recorded (the repo is a git repo; _git_commit_hash() returns a real SHA) and is compared in _provenance_mismatches, so a committed edit does raise a warning there; but model_artifact_is_current ignores git_commit entirely, so models are still never refit either way, and that is the load-bearing half of the defect. (2) src/models/evaluation.py does not define mae/mdae/rmse/mape/r2 as separate functions; it computes them inside evaluate_regression (which also owns the 100x MAPE scaling convention).

**Kanıt:** `_PROVENANCE_SRC_FILES = ("src/feature_engineering.py", "src/tuning.py")` (line 109) -- confirmed at runtime: `_compute_provenance()['src_sha256'].keys()` returns exactly those two. Not covered: src/models/dl_runtime_predictor.py (defines RuntimePredictorLSTM at line 37, RuntimePredictorCNN at 97, RuntimePredictorCNNLSTM at 153 -- every DL model in the study), src/models/evaluation.py (defines mae/mdae/rmse/mape/r2), src/data_loading.py (reads the raw CSVs), configs/models.yaml (holds `random_state: 42`, `cv: 3`, `n_iter: 10` and every rf/xgb/lgbm/cnn/lstm/cnn_lstm search grid) and configs/paths.yaml.

**Sonucu:** Changing an LSTM's hidden dimension, a dropout default, the CV fold count, the search grids, or the definition of MAE produces entirely different published numbers while model_artifact_is_current keeps returning True (so no model is refit) and load_checkpoint reports no mismatch. The guard advertises coverage it does not have.

**Düzeltme:** Hash the full set of files that determine a result: add src/models/dl_runtime_predictor.py, src/models/evaluation.py, src/data_loading.py, configs/models.yaml and configs/paths.yaml to _PROVENANCE_SRC_FILES (or hash every tracked .py under src/ plus configs/*.yaml programmatically, so a new module is covered automatically).

---

## 22. [MAJOR] simulator — correctness

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (cells a53bdae7, cd15, cd17, cd19); src/simulation/scheduler_simulator.py:132-134`

**Ne:** The shipped checkpoint results/models/cnn_lstm_numeric_seq.pth emits a single constant value (4128.124023; n_unique=1, spearman=nan, MAE=6702.112332730178, R2=-0.0149) for all 16,437 test jobs. Because SJFPredScheduler.select_job (src/simulation/scheduler_simulator.py:132-134) picks queue["predicted_runtime"].idxmin() and the simulator keeps pending_df in arrival order, the policy 'SJF-CNN-LSTM (Numeric Sequence)' is bit-identical to FIFO: I re-ran both and got meanWait=1367857.06, medWait=1226897.60, p95=2866728.62, meanJCT=1373886.81, maxJCT=3263172.70, meanSlowdown=8270.04 for both, with per-job job_id/start_time/waiting_time/turnaround_time frames comparing equal. The committed cd19 table (32-GPU) and the 256-GPU notebook (148,258.95 / 154,288.69 / 891.27, plus the identical cd17 diagnostics row) show the same, and the degenerate row is persisted with empty Spearman/Kendall in results/analysis/rank_correlation_32gpu.csv and _256gpu.csv. Neither run_policy (cd15) nor the simulator screens zero-variance predictions, and no markdown cell discloses that this row is FIFO under another name, so the evaluation table presents it as a distinct DL scheduling policy with its own MAE/RMSE/R2, and it enters the Wilcoxon table and the CDF/box/heatmap figures as an extra policy that lies exactly on FIFO. TWO PARTS OF THE FILED CLAIM ARE INCORRECT: cell 7a843cee already guards the rank-correlation fit with _fit_ok = np.isfinite(...) and prints the excluded policy by name (comment: "a predictor that cannot order jobs, so its SJF policy is FIFO under another name"), so there is no live np.polyfit LinAlgError halting the notebook, and the row does not silently enter the rank-correlation regression. Severity is major rather than blocker: it misrepresents one of 28 policy rows (and the thesis tables at thesis/latex/chapters/6.results_and_discussion.tex:146,168,272,293 credit it with +19.88%/+30.58% JCT and p<0.001, impossible for all-zero paired differences), but it leaves every other policy's number and the headline ranking untouched.

**Kanıt:** Executed check loading the shipped weights (results/models/cnn_lstm_numeric_seq.pth) with the shipped scalers and the notebook's own sliding-window inference: `cnn_lstm_numeric_seq n_unique=1 min=4128.124023 max=4128.124023 std=0.000488 first5=[4128.124 4128.124 4128.124 4128.124 4128.124]`, `spearman(y_test, pred) = SignificanceResult(statistic=nan, pvalue=nan)` (scipy ConstantInputWarning: 'An input array is constant'). The two sibling sequence models are not degenerate (lstm_numeric_seq n_unique=16021, cnn_numeric_seq n_unique=14958). The committed cd19 table confirms the collapse -- SJF-CNN-LSTM (Numeric Sequence) and FIFO share every digit: Mean Wait 1,367,857.06 / Median Wait 1,226,897.60 / P95 Wait 2,866,728.62 / Mean JCT 1,373,886.81 / Max JCT 3,263,172.70 / Mean Slowdown 8,270.04 / JCT Improvement 0.00; the cd17 diagnostics row is identical too (time_weighted_gpu_util 0.694, cpu 0.354, peak_pending 16156). Same in the 256-GPU notebook (both 148,258.95 / 154,288.69 / 891.27). I reproduced FIFO independently and got exactly meanJCT=1373886.81, meanWait=1367857.06, p95=2866728.62, meanSlowdown=8270.04. The resulting NaN row is persisted in results/analysis/rank_correlation_32gpu.csv: `SJF-CNN-LSTM (Numeric Sequence),,,,,6702.112332730178,0.0`.

**Sonucu:** A row the thesis presents as a deep-learning scheduling policy is the FIFO baseline under a different name, credited with 'Model MAE 6,702 s / R2 -0.01' as if it were a working predictor. It is also the direct cause of the LinAlgError that halts the whole notebook (NaN Spearman rho -> np.polyfit), and it silently enters the rank-correlation regression, the Wilcoxon table and the CDF/box/heatmap figures as a 21st/28th independent policy.

**Düzeltme:** Add a zero-variance guard where predictions are attached (cell cd13) or in run_policy: raise or drop the policy when `np.ptp(jobs[pred_col]) == 0`, since a constant estimate cannot express any priority. Separately, retrain/repair cnn_lstm_numeric_seq (a collapsed-to-the-mean network) in notebook 04 before rerunning notebook 05.

---

## 23. [MAJOR] simulator — reproducibility

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (cell cd13, line `sim_jobs = sim_jobs.sort_values("arrival_time").reset_index(drop=True)`), plus the same line in the 256-GPU and both tr variants`

**Ne:** Cell cd13 of all four notebook 05 variants re-sorts sim_jobs with pandas' default non-stable quicksort on arrival_time, a column where 30.8% of the 16,437 test rows are tied. The frame arrives already in the deliberately stable chronological order that prepare_features_for_model establishes (feature_engineering.py:485, kind="mergesort"), so the call only permutes ties (1,695 rows move), and that permuted order is what MultiNodeClusterSimulator's JobEvent.seq tie-breaker then locks in. The published FIFO baseline is therefore an artifact of the sort algorithm: 32-GPU FIFO Mean JCT 1,373,886.81 (the value in the notebook's stored results table) vs 1,343,790.47 under a stable sort (2.24%), and 256-GPU FIFO 154,288.69 vs 157,556.06 (2.12%, opposite direction). Every "JCT Improvement %" in both configurations is computed against that baseline and shifts 0.4-0.9 pp (32-GPU SJF-Oracle 81.63 -> 81.22, SRF 45.43 -> 44.28; 256-GPU SJF-Oracle 78.83 -> 79.27, SRF 49.13 -> 49.98). Correction to the claim as filed: the SJF policies' absolute metrics are unchanged (SJF-Oracle is bit-identical under both orders), so this is predominantly a common-mode denominator shift that rescales all reported improvements by ~2% without reordering the policy table -- the "larger than the 0.41 pp gap the thesis distinguishes" framing overstates it. Also, the permutation is identical under numpy 1.26.3 and 2.3.5 on this machine, so the cross-version-drift concern is theoretical rather than observed. The real, confirmed defect is that FIFO's and SRF's reported absolute Mean JCT / Mean Wait / P95 Wait / Mean Slowdown, and hence every improvement percentage in the 32-GPU and 256-GPU tables, depend on an arbitrary quicksort tie order instead of the trace's chronological order the pipeline deliberately preserves. Fix: pass kind="mergesort" (or drop the redundant re-sort).

**Kanıt:** Measured on the real test slice: `rows 16437, unique arrival_time 13045; timestamps with >1 job: 1669; jobs sharing a timestamp: 5061 (30.8%); max jobs at one timestamp: 76`. `quicksort order == mergesort order? False`; 1,695 rows differ; first divergence at row 30 (job_id 79954 vs 79953 at 1970-01-07 01:51:39). Running the full 16,437-job simulation on the 32-GPU cluster under each order: FIFO quicksort(notebook) meanJCT=1,373,886.81 meanWait=1,367,857.06 p95wait=2,866,728.62 meanSlowdown=8,270.04 vs FIFO mergesort(stable) meanJCT=1,343,790.47 meanWait=1,337,760.72 p95wait=2,795,176.32 meanSlowdown=8,095.46. Recomputing JCT-improvement-over-FIFO under each order: SRF 45.430% vs 44.279% (1.152 pp), SJF-Oracle 81.632% vs 81.221% (0.411 pp), an SJF-Pred policy 74.065% vs 73.485% (0.581 pp).

**Sonucu:** The FIFO denominator of every 'JCT Improvement %' in notebook 05 moves 2.2% and each policy's improvement moves up to 1.15 pp purely from an arbitrary tie order. That is larger than the gaps the thesis distinguishes between policies (reported 32-GPU table: SJF-XGBoost (One-Hot) 61.62% vs SJF-LSTM (One-Hot) 61.21% = 0.41 pp; SJF-LSTM (One-Hot Sequence) 60.82% = 0.39 pp behind). The ordering is also not guaranteed stable across numpy/pandas versions or platforms, so a re-run elsewhere can produce a different published baseline.

**Düzeltme:** Use `sim_jobs.sort_values("arrival_time", kind="mergesort")` (or drop the line entirely, since job_df already arrives mergesort-ordered by arrival_sec) in all four notebook-05 variants, and add a regression test asserting that the row order fed to the simulator is a stable chronological order.

---

## 24. [MAJOR] simulator — dead-code

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (cell r4cell); same cell id in the 256-GPU and both tr variants`

**Ne:** The multi-seed DL robustness check (robustness-4) in cell r4cell of all four 05 notebooks is permanently dead due to a seed-naming mismatch: it tests for checkpoints named by seed INDEX (`lstm_categorical_pt_seed{0,1,2}.pth`, `lstm_categorical_seq_seed{0,1,2}.pth`), while src/tuning.py:1979 writes them by seed VALUE via `save_all_seeds_to.format(seed=seed)` over `DL_SEEDS = [42, 1337, 2024]`, producing `..._seed42/1337/2024.pth`. The existence gate therefore always evaluates False and the entire simulation body is unreachable. Correction to the claim's wording: the cell does not in fact print the SKIPPED message in the shipped notebooks either -- execution_count is None with zero outputs in all four variants, so the cell has never been executed at all and a reader sees no output rather than a misleadingly-presented result. The substantive consequence is unchanged and confirmed: the sole stated mitigation for audit finding robustness-4 -- re-simulating LSTM-Categorical under all three seeds to test whether the 0.67pp (32-GPU) / 0.19pp (256-GPU) LSTM-vs-XGBoost JCT gap survives initialisation noise -- has never run and cannot run as written, so the thesis's RQ4 ranking rests on a single seed (42) with no seed-robustness evidence. Compounding this, tests/test_tuning.py:190 TestFinalizeDlModelSaveAllSeeds, explicitly labelled a robustness-4 regression test, passes only because it uses seeds=[0, 1, 2] and never exercises the consumer against the pipeline's real DL_SEEDS. Fix is one line per notebook: build the paths from DL_SEEDS (42, 1337, 2024) instead of (0, 1, 2); the six real checkpoints load correctly and the three seeds are genuinely distinct models, so the check runs and is meaningful once renamed.

**Kanıt:** notebooks/en/05_..._32_gpu.ipynb cell r4cell: `_seed_paths_static = {s: MODEL_DIR / f"lstm_categorical_pt_seed{s}.pth" for s in (0, 1, 2)}` and `_seed_paths_seq = {s: MODEL_DIR / f"lstm_categorical_seq_seed{s}.pth" for s in (0, 1, 2)}`, gated by `if all(p.exists() for p in _seed_paths_static.values()) and all(...)`. notebooks/en/04_runtime_prediction_models.ipynb: `DL_SEEDS = [42, 1337, 2024]` and `save_all_seeds_to=str(MODEL_DIR / "lstm_categorical_pt_seed{seed}.pth")` / `"lstm_categorical_seq_seed{seed}.pth"`. On disk: results/models/ contains lstm_categorical_pt_seed42.pth, _seed1337.pth, _seed2024.pth and lstm_categorical_seq_seed42/1337/2024.pth. Executed check: every one of lstm_categorical_pt_seed0/1/2.pth and lstm_categorical_seq_seed0/1/2.pth returns `exists() == False`.

**Sonucu:** The stated mitigation for robustness-4 -- re-simulating LSTM-Categorical under all three seeds to show whether the small LSTM-vs-XGBoost JCT gap (0.67 pp at 32-GPU, 0.19 pp at 256-GPU per the cell's own comment) survives initialisation noise -- has never run and cannot run, while the notebook text presents it as an executed check.

**Düzeltme:** Derive the paths from the same seed list notebook 04 uses, e.g. `DL_SEEDS = [42, 1337, 2024]` and `{s: MODEL_DIR / f"lstm_categorical_pt_seed{s}.pth" for s in DL_SEEDS}`, and turn the silent skip into a loud warning that names the paths it looked for.

---

## 25. [MAJOR] statistics — reproducibility

**Dosya:** `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (cell id 'r4cell'; identical in notebooks/en/05_scheduler_evaluation_32_gpu.ipynb and both notebooks/tr/05_*.ipynb)`

**Ne:** Confirmed as stated, with two precisions. In all four notebook 05 variants (notebooks/en/05_scheduler_evaluation_{32,256}_gpu.ipynb and notebooks/tr/05_gorev_zamanlayici_degerlendirme_{32,256}_gpu.ipynb), cell 'r4cell' builds the robustness-4 model paths from seed INDICES `for s in (0, 1, 2)` while notebook 04 (cell cd03 DL_SEEDS = [42, 1337, 2024], cells ckpt_d_lstm / ckpt_f_lstm) and src/tuning.py:1979 (`torch.save(model, save_all_seeds_to.format(seed=seed))`) write the seed VALUE. On disk there are exactly lstm_categorical_{pt,seq}_seed{42,1337,2024}.pth; running the cell's own gate gives False for all six index-named paths and True for the value-named ones, so the else-branch fires and prints a diagnostic ("per-seed model files not found on disk -- re-run notebook 04's Experiment D/F LSTM cells") that is factually false. tests/test_tuning.py:222 uses seeds=[0, 1, 2], so it green-lights exactly the naming the real pipeline never produces. Precision 1: r4cell has execution_count None and zero stored outputs in all four notebooks (as do all trailing cells from 8d1a4e10 on), so the 'SKIPPED' text is not a captured output of a past run -- it is what any fresh execution produces. Precision 2: no reported number is computed incorrectly by this bug; the damage is that the only seed-level (initialisation) uncertainty check for the headline scheduler ranking never executes. That ranking rests on a 0.54 pp gap at 256 GPU (63.88 vs 63.34 in results/analysis/rank_correlation_256gpu.csv) from a single seed, while results/checkpoints/exp_d_lstm.json and exp_f_lstm.json record mae_std of 200.74 s and 256.60 s across the three seeds -- large enough that the untested gap could plausibly flip. The cell's own comment block additionally cites stale spreads (509 s / 651 s) and stale gaps (0.19 pp at 256 GPU) that no longer match those checkpoints or the CSV. Fix is one line per notebook: derive the paths from the seed values (42, 1337, 2024) or from DL_SEEDS, and update the regression test to use non-index seeds.

**Kanıt:** r4cell lines 12-15: `_seed_paths_static = {s: MODEL_DIR / f"lstm_categorical_pt_seed{s}.pth" for s in (0, 1, 2)}` / `_seed_paths_seq = {s: MODEL_DIR / f"lstm_categorical_seq_seed{s}.pth" for s in (0, 1, 2)}` / `if all(p.exists() for p in _seed_paths_static.values()) and all(p.exists() for p in _seed_paths_seq.values()):`. Notebook 04 cell 'cd03' sets `DL_SEEDS = [42, 1337, 2024]` and cells 'ckpt_d_lstm'/'ckpt_f_lstm' pass `save_all_seeds_to=str(MODEL_DIR / 'lstm_categorical_pt_seed{seed}.pth')` / `'lstm_categorical_seq_seed{seed}.pth'`, and src/tuning.py:1979 writes `torch.save(model, save_all_seeds_to.format(seed=seed))` — i.e. the seed VALUE. Executed check: `ls results/models` gives lstm_categorical_pt_seed42.pth, _seed1337.pth, _seed2024.pth, lstm_categorical_seq_seed42.pth, _seed1337.pth, _seed2024.pth; running the cell's own gate prints `static exists: {...seed0.pth: False, ...seed1.pth: False, ...seed2.pth: False}` and `gate passes: False`. The regression test tests/test_tuning.py::TestFinalizeDlModelSaveAllSeeds uses `seeds=[0, 1, 2]`, so it passes on exactly the naming the real pipeline never produces and cannot catch this. The same comment block also cites stale spreads ("exp_d_lstm: 509s, exp_f_lstm: 651s") while the current checkpoints hold mae_std = 200.74 and 256.60.

**Sonucu:** The notebook's only quantification of seed-level (initialisation) uncertainty never executes. The headline ranking claim that SJF-LSTM (One-Hot) beats SJF-XGBoost (One-Hot) rests on a gap of 0.54 pp at 256 GPU (63.88 vs 63.34 in results/analysis/rank_correlation_256gpu.csv) and 0.41 pp at 32 GPU, produced from a single seed, with the check that was written to test that gap silently reporting 'SKIPPED'.

**Düzeltme:** Read the seed list from the same source notebook 04 uses (`DL_SEEDS`, or the `seeds` list recorded in results/checkpoints/exp_d_lstm.json) rather than hard-coding (0, 1, 2), e.g. `_SEEDS = json.load(open(CKPT_DIR/'exp_d_lstm.json'))['metrics']['seeds']` and build the paths with those values; and change tests/test_tuning.py to use non-index seed values (e.g. [42, 1337, 2024]) so the template/value mismatch is detectable.

---

## 26. [MAJOR] statistics — correctness

**Dosya:** `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (cell id 'stats1cell'; identical in the 32-GPU and both Turkish notebooks)`

**Ne:** In cell 'stats1cell' (identical in notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, notebooks/en/05_scheduler_evaluation_32_gpu.ipynb, notebooks/tr/05_gorev_zamanlayici_degerlendirme_256_gpu.ipynb, notebooks/tr/05_gorev_zamanlayici_degerlendirme_32_gpu.ipynb), the 'independent sub-window replay' t-CI estimates a different quantity from the one the notebook's headline tables report, but is presented as an interval for it. Each of the 10 windows re-simulates 1/10 of the workload from an empty cluster with its own time origin, so the deep backlog that dominates the full oversaturated replay never forms. Reproduced at 256 GPUs, LOAD_FACTOR=0.1, FIFO vs SJF-Oracle on the real 16,437-job test split: full replay FIFO mean JCT 154,288.7 s and mean paired diff 121,630.8 s, versus window-mean FIFO JCT 12,361.8 s and block mean diff 4,342.7 s with 95% t-CI [1,574.6, 7,110.8] -- the headline difference is ~28x the block mean and outside the interval. The block table prints this under 'Mean JCT vs FIFO (s)' with 'Block 95% CI low/high' directly after the significance table's 'JCT vs FIFO (s)' (equivalent label and units, not the literally identical string the claim asserts), and the caption tells the reader to use it as the verdict on the full-replay claims and to limit cross-policy ranking claims to pairs whose block CIs do not overlap. Corrections to the original wording: the column names are equivalent rather than identical; the per-window differences do not rise monotonically across windows (978.9, 2261.1, 6696.5, 4521.0, 1604.2, 4560.7, 2062.2, 1351.4, 5540.3, 13851.1), so the scale gap is caused by the empty-cluster restart and the 1/10 span, not by a trend across windows; and the 'real advantage declared not established' outcome is a consequence of the design rather than an observed event -- in the pair I executed the CI still excluded zero. The fix is to relabel the block quantity as a sub-window-regime difference and state explicitly that its CI does not bound the full-replay difference, instead of offering it as the arbiter of the headline tables.

**Kanıt:** Cell code: `_window_row_groups = np.array_split(np.arange(len(sim_jobs)), _N_WINDOWS)`; `_t0_w = _window_jobs["submit_time"].min(); _window_jobs["submit_time"] = _window_jobs["submit_time"] - _t0_w`; then `_mean_b - _tcrit * _se_b` over the 10 window means, printed as `"'CI excludes zero?' = False means the 'beats FIFO' claim from the tables above is descriptive of this one workload's single replay, not established at the replay level."` Executed reproduction of exactly this procedure on the real 16,437-job test split at 256 GPUs (FIFO vs SJF-Oracle, LOAD_FACTOR=0.1): full replay FIFO mean JCT = 154,288.7 s and mean paired diff = 121,630.8 s; the 10 sub-window replays give window-mean FIFO JCT = 12,361.8 s and block mean diff = 4,342.7 s with 95% t-CI [1,574.6, 7,110.8]. The block CI is ~28x below the headline difference and does not contain it; window means rise from 978.9 s (window 1) to 13,851.1 s (window 10) as backlog accumulates within each isolated window. The notebook's own cell 'stblrgm1' confirms the regime gap: offered load is ~530 concurrent GPUs at natural pace, i.e. ~5,300 at LOAD_FACTOR=0.1 against 256 provisioned.

**Sonucu:** A reader (and the thesis text that cites this check) is invited to read 'CI excludes zero?' as a verdict on the headline 'beats FIFO' differences, but the interval estimates a differently-scaled quantity measured in a far less congested regime. A policy whose full-replay advantage is real can be declared 'not established at the replay level' purely because the sub-windows never reach the congestion where that advantage is generated.

**Düzeltme:** Either make the blocks exchangeable with the headline replay (prime each window with the queue state carried over from the previous window, or replay N independent full-length windows at the headline load) or report the block result as what it is — a fixed-effects estimate at a lower congestion level — by renaming the column (e.g. 'Mean JCT vs FIFO within an isolated 1/10 window (s)') and printing the window-mean FIFO JCT next to the full-replay FIFO JCT so the scale difference is visible in the table itself.

---

## 27. [MAJOR] statistics — consistency

**Dosya:** `notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (markdown cell ids 'ee60c718' and '579a828a')`

**Ne:** Confirmed, and slightly broader than stated. In notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (and the identical cell ids in notebooks/tr/05_gorev_zamanlayici_degerlendirme_256_gpu.ipynb): (1) Untested direction. Cell 'e5cd07' runs the FIFO comparison only as stats.wilcoxon(..., alternative="greater") (its own printed header: "H1: FIFO > Policy"). No alternative="less" test against FIFO exists in the notebook; the only two-sided tests (blc7cell, r4cell) use UserMedian/SRF/XGBoost-Cat as references. Cell 'ee60c718' nevertheless concludes policies are "mathematically no better-and often significantly worse-than a 'blind' arrival-order (FIFO) policy" from p = 1.0. Verified by running scipy: for a policy ~3% worse than FIFO the 'greater' test returns p = 0.99999971 while the never-run 'less' direction returns p = 2.9e-07. A p near 1.0 under H1: FIFO > policy is precisely the result that carries no directional evidence. (2) Wrong policies named. 'ee60c718' attributes the p = 1.0 failures to SJF-LSTM (Numeric Sequence) and SJF-CNN (Numeric Sequence). The notebook's own exported significance table (results/_backup_20260831_1703_pre_q2/thesis_export/html/NB05_256GPU_Table04.html) shows the p = 1.000000 / "No" rows were SJF-CNN-LSTM (Numeric Sequence) (-14,252.2 s) and SJF-CNN (Numeric Sequence) (-39,732.3 s), with SJF-LSTM (Numeric Sequence) significant at +22,952.9 s. In the current run (results/analysis/rank_correlation_256gpu.csv, regenerated in the same commit as the notebook) SJF-LSTM (Numeric Sequence) is +56.32% and SJF-CNN (Numeric Sequence) +8.43%. Both named "failures" are wrong in both runs. (3) Non-universal significance stated as universal. Cell '579a828a' claims "FIFO ... performs significantly worse than all SJF-based policies (p < 0.05)". Table04 already contradicted this with two non-significant rows, and the current run still does: SJF-CNN (One-Hot Sequence) is -2.97% (worse than FIFO) and SJF-CNN-LSTM (Numeric Sequence) is 0.00% with NaN Spearman rho - which cell '8d1a4e10' itself documents as a constant-prediction model whose "SJF policy is FIFO under another name", so its paired differences are all zero and the test cannot be significant (scipy returns NaN; the notebook's except-ValueError fallback records 1.0). (4) Impossible precision. Table04 literally printed "0.000000" and 'ee60c718' quotes it as "maintaining p-values strictly at $0.000000$", while 'e5cd07' has since been changed to format sub-1e-300 values as "< 1e-300" with the comment that 0.000000 is "a value no test can return". (Minor nuance in the original claim's wording: scipy does return the float 0.0 by underflow; the point that stands is that the markdown asserts an exact zero the code was deliberately fixed to stop printing.) Also stale in the same cell, outside the statistics scope: '579a828a' names SJF-XGBoost (Categorical) as achieving the lowest mean JCT among learned models, while the current CSV ranks SJF-LSTM (One-Hot) first at 63.88% vs SJF-XGBoost (One-Hot) at 63.34%. Severity stays major: no computed number is wrong, but these are the notebook's own stated conclusions for its significance section - a directional verdict in a direction never tested, and a universal p < 0.05 contradicted by the notebook's own significance table.

**Kanıt:** Cell 'e5cd07' runs only `stats.wilcoxon(paired["fifo_turnaround_time"], paired["turnaround_time"], alternative="greater")` — H1: FIFO > policy — for every policy; no test in the 'less' direction exists anywhere in the notebook. Cell 'ee60c718' nevertheless states: "purely sequential models like SJF-LSTM (Numeric Sequence) and SJF-CNN (Numeric Sequence) completely failed the significance test ($p = 1.0$) ... mathematically no better—and often significantly worse—than a 'blind' arrival-order (FIFO) policy", and "maintaining p-values strictly at $0.000000$ across the entire workload sample" — while cell 'e5cd07' comments "A Wilcoxon p on ~16k paired jobs underflows well below 1e-300, and the default float repr printed those as '0.000000' -- a value no test can return" and formats them as `"< 1e-300"`. Cell '579a828a' states "the FIFO scheduling policy performs significantly worse than all SJF-based policies (p < 0.05)", but results/analysis/rank_correlation_256gpu.csv records SJF-CNN (One-Hot Sequence) at JCT Improvement % = -2.97 (worse than FIFO) and SJF-CNN-LSTM (Numeric Sequence) at 0.0, both of which the one-sided 'greater' test returns p ≈ 1.0 for.

**Sonucu:** The notebook asserts statistical significance in a direction never tested and universality contradicted by its own results table; 'p = 0.000000' also reports a precision no test produced. These are the summaries a reader carries away from the significance section.

**Düzeltme:** Run the complementary one-sided test (or a two-sided test plus the sign of the rank-biserial effect size) before any 'significantly worse' wording, restrict the 'all SJF-based policies' claim to the policies whose Holm-adjusted p is < 0.05 and whose effect size is positive, and quote underflowed p-values with the same `< 1e-300` bound the code already prints.

---

## 28. [MAJOR] tests — test-gap

**Dosya:** `tests/test_feature_engineering.py`

**Ne:** This session's change to prepare_features_for_model (build the job table with include_cpu_only=True, run the sweep-line over all jobs, restrict to gpu_demand>0 afterwards) has no test. The test that names the behaviour, test_cpu_only_jobs_are_kept_for_cluster_load_but_dropped_for_modelling, calls build_job_table_from_sample / add_cluster_utilization_features by hand and never calls prepare_features_for_model, so it passes whether or not the pipeline actually uses them that way.

**Kanıt:** tests/test_feature_engineering.py:81-123 exercises only the helpers: ``` gpu_only = build_job_table_from_sample(raw, time_unit="s") with_cpu = build_job_table_from_sample(raw, time_unit="s", include_cpu_only=True) load_gpu_only = add_cluster_utilization_features(_atf(gpu_only)) ``` Mutation executed: in src/feature_engineering.py:468 I replaced `job_df = build_job_table_from_sample(raw_df, time_unit=time_unit, include_cpu_only=True)` with `job_df = build_job_table_from_sample(raw_df, time_unit=time_unit)` (the exact pre-fix line) and ran `venv/bin/python -m pytest -q` -> `55 passed in 4.36s`. File restored; `git diff src/feature_engineering.py` is empty.

**Sonucu:** The leakage-4 / robustness-11 defect can be reintroduced into the one function notebooks 04 and 05 actually call, and the suite stays green. Commit 4e4f5b5 documents that this exact regression already cost the thesis a wrong headline number (RF Numeric MAE 6796.98 s computed on the pre-fix pipeline).

**Düzeltme:** Add to tests/test_feature_engineering.py: `test_prepare_features_runs_the_sweepline_over_cpu_only_jobs_too` — call `prepare_features_for_model(feature_mode="numeric_only")`, assert `(job_df["gpu_demand"] > 0).all()` (CPU-only jobs are not modelled) AND assert the returned cluster_load_cpu/active_job_count are element-wise equal to a reference computed with include_cpu_only=True and strictly greater than the include_cpu_only=False reference for at least one row (assert the two references differ, so the test cannot pass vacuously). The check is cheap: on the real trace it takes a few seconds.

---

## 29. [MAJOR] tests — test-gap

**Dosya:** `tests/test_tuning.py`

**Ne:** tests/test_tuning.py:67-103 (TestFinalizeMlModelDataBudget) is named and documented as the regression guard for code_bugs-6 ("XGB/LGBM final refits must use 100% of X_train, matching RF's data budget"), but neither test observes the number of training rows the final refit receives; both assert only model.n_estimators == metrics["n_estimators_effective"], and both sides derive from the same best_n_estimators variable, so the assertion is independent of which rows fit() sees. Refinement to the original claim: the assertion is not entirely inert — on the fixture n_estimators_effective is 198 (XGB) / 195 (LGBM) vs best_params n_estimators=200, so it does catch the other failure mode the docstring names (returning the early-stopped internal-split search_model directly). It simply cannot catch the data-budget mode it is named for.

**Kanıt:** tests/test_tuning.py:67-103, class docstring: 'XGB/LGBM final refits must use 100% of X_train (matching RF's data budget)'. The only assertions are: ``` self.assertIn("n_estimators_effective", metrics) self.assertEqual(model.n_estimators, metrics["n_estimators_effective"]) ``` Nothing inspects the training-row count. Mutation executed: in src/tuning.py I changed the two final refits from `final_model.fit(X_train, y_train, ...)` (lines 977 and 1011) to `final_model.fit(X_tr_split, y_tr_split, ...)` — i.e. the 90% budget the fix removed — and ran `venv/bin/python -m pytest -q tests/test_tuning.py` -> `14 passed in 3.16s`. File restored.

**Sonucu:** The unequal-data-budget bug between model families (RF sees 100% of X_train, XGB/LGBM see 90%, and the missing 10% is the chronologically-latest, closest-to-test-period training data) can silently return while the test named after it passes, biasing every XGB/LGBM metric in the comparison tables downward relative to RF.

**Düzeltme:** Assert the row count the final estimator actually saw. For XGB: `self.assertEqual(model.get_booster().num_boosted_rounds(), metrics["n_estimators_effective"])` plus a direct data-budget check — monkeypatch/spy the final `.fit` (e.g. `unittest.mock.patch.object(xgb.XGBRegressor, "fit", autospec=True, side_effect=real_fit)`) and assert the X passed to the LAST fit call has `len(X) == len(X_train)`, not `len(X_train) * 0.9`. Same for lgbm. Name it `test_final_refit_sees_every_training_row`.

---

## 30. [MAJOR] tests — test-gap

**Dosya:** `src/tuning.py`

**Ne:** Confirmed core: model_artifact_is_current (src/tuning.py:366) and record_model_artifact (src/tuning.py:399), added in 4e4f5b5, have zero tests — grep for both names and for "provenance"/"sidecar" across tests/ returns nothing, and 4e4f5b5's stat touches only the two notebook 04 files and src/tuning.py. I reproduced the mutation: inserting `return Path(dest).exists()` as the first statement gives `55 passed`. I also mutated the OTHER half of the same commit's fix (`same_result = True` in save_checkpoint, the pre-fix always-carry semantics) -> also `55 passed`. So both halves are mutation-survivable. Aggravating: the commit message states "Tests cover both: sidecar absent/present/stale, and provenance carried forward on an unchanged re-save but refreshed when the result actually changes" — no such tests exist, so the commit record falsely certifies coverage a reviewer would rely on. 16/36 artifacts lack sidecars, confirmed by execution. Two supporting prongs are REFUTED and must be dropped, as they overstate live exposure: (1) "every file notebook 05 loads via lstm_categorical_pt_seed{s}.pth" is false. Notebook 05 cell 50 builds paths for `s in (0, 1, 2)`, while notebook 04 saves via save_all_seeds_to.format(seed=seed) with DL_SEEDS = 42/1337/2024 (src/tuning.py:1979). No seed0/1/2 files exist, so the `all(p.exists())` guard is False and the robustness-4 block takes the SKIPPED branch — notebook 05 never loads those seed artifacts at all. (2) The four lstm_scaler_*.joblib are not gated by this mechanism: notebook 04 cells 51 and 61 do a bare joblib.dump with no model_artifact_is_current guard and no record_model_artifact call, so they are rewritten unconditionally every run and cannot go stale via the skip path. Their mtimes (15:10) postdate 4e4f5b5 (13:06). The False return is inert because no code calls the function on them. Corrected statement: the sole regression guard for a defect that demonstrably corrupted reported numbers (RF Numeric MAE 6796.98 in Tables 1/2/12 vs 6787.83 on refit) is entirely untested in both of its halves, and the commit claims test coverage that does not exist — so a revert to `dest.exists()` semantics reintroduces stale-model reporting with a green suite. The claimed present-day staleness of notebook 05's DL seed artefacts and scalers does not hold. Out of scope but found while verifying: the seed0/1/2 vs seed42/1337/2024 name mismatch means notebook 05's robustness-4 multi-seed simulation silently prints SKIPPED instead of producing the seed-mean +/- std JCT it documents.

**Kanıt:** src/tuning.py:366 `def model_artifact_is_current(dest)` and src/tuning.py:399 `def record_model_artifact(dest)`. `git show --stat 4e4f5b5` lists only notebooks/en/04_..., notebooks/tr/04_... and src/tuning.py — no tests/ file. `grep -rn "model_artifact_is_current\|record_model_artifact" tests/` returns nothing. Mutation executed: inserted `return Path(dest).exists()` as the first statement of model_artifact_is_current (the exact `elif _dest.exists(): skip` semantics the fix replaced) -> `venv/bin/python -m pytest -q` -> `55 passed in 4.57s`. File restored. The gap is not hypothetical: of 36 artefacts in results/models, 16 have no .provenance.json sidecar, including every file notebook 05 loads via lstm_categorical_pt_seed{s}.pth / lstm_categorical_seq_seed{s}.pth and the four lstm_scaler_*.joblib. Verified by execution: `model_artifact_is_current('results/models/lstm_categorical_pt_seed42.pth')` -> False, while rf_numeric.joblib / xgb_numeric.joblib / lstm_numeric.pth -> True. `grep 'model_artifact_is_current' notebooks/en/05_scheduler_evaluation_32_gpu.ipynb` finds nothing, so notebook 05 never checks currency of anything it loads.

**Sonucu:** The defect commit 4e4f5b5 was written to fix — trained models outliving a feature-engineering fix, so notebook 05 simulates with pre-fix models against a post-fix test set — can return unnoticed, and already remains uncovered for the multi-seed DL artefacts and scalers that notebook 05 actually consumes.

**Düzeltme:** Add tests/test_tuning.py::TestModelArtifactCurrency with: (a) `test_missing_sidecar_is_not_current` — write a dummy .joblib with no sidecar, assert False; (b) `test_recorded_artifact_is_current` — record_model_artifact then assert True; (c) `test_source_change_invalidates_the_artifact` — record, then patch `src.tuning._compute_provenance` to return a different src_sha256 and assert False (this is the assertion the `.exists()` mutation fails); (d) `test_corrupt_sidecar_is_not_current` — write invalid JSON, assert False. Separately, extend finalize_dl_model's save_all_seeds_to path and notebook 04's scaler/baseline dumps to call record_model_artifact, and add `test_every_saved_seed_gets_a_sidecar`.

---

## 31. [MAJOR] tests — test-gap

**Dosya:** `src/tuning.py`

**Ne:** Real, and understated in scope. Neither half of commit 4e4f5b5's reproducibility-4 fix has any test. (a) save_checkpoint's re-save branch (src/tuning.py:347-355): the sole test, tests/test_tuning.py:283-318, calls save_checkpoint once into a fresh TemporaryDirectory, so `if path.exists()` never executes and the branch is dead under test. Reverting the whole fix to the exact pre-fix guard (`and previous.get("provenance") == clean.get("provenance")` inside same_result, timestamp-only carry-forward) leaves 55 passed in 4.00s, as does the narrower two-line `pass` mutation. Reproduced with the real NB04 call shape (cells pass metrics/best_params/feature_mode/train_size/test_size and never provenance; the recover branch writes the loaded result straight back): pre-fix code restamps the stored src hash to the current one (605f75d2...) and the timestamp to now, and the next load prints NO provenance warning; fixed code carries OLD_PRE_FIX_HASH and the original timestamp forward and the warning keeps firing. (b) The other half of the same fix -- model_artifact_is_current / record_model_artifact, which gates whether stale .joblib models are reused -- is equally untested: `grep -rn "provenance|model_artifact_is_current|record_model_artifact" tests/` returns zero hits across all seven test files. That half is the more directly number-affecting one, since it is what let trained models outlive the sweep-line feature fix and produced the RF-Numeric MAE discrepancy (6796.98 reported vs 6787.83 refit) in Tables 1, 2 and 12 while feeding pre-fix models into notebook 05's simulation. Commit 4e4f5b5's message asserts "Tests cover both: sidecar absent/present/stale, and provenance carried forward on an unchanged re-save but refreshed when the result actually changes", but its --stat touches only notebooks/en/04, notebooks/tr/04 and src/tuning.py -- no test file was added.

**Kanıt:** src/tuning.py:353-355: ``` if "timestamp" in previous: clean["timestamp"] = previous["timestamp"] if "provenance" in previous: clean["provenance"] = previous["provenance"] ``` The comment above it states the stakes: 'a re-save after a source change fell through and restamped the checkpoint with the CURRENT source hash -- which silently relabelled a stale result as freshly produced and suppressed load_checkpoint's mismatch warning'. tests/test_tuning.py:283-318 (test_save_checkpoint_enriches_required_metadata) calls save_checkpoint exactly once into a fresh TemporaryDirectory, so `path.exists()` is False and the whole branch is dead code under test. Mutation executed: replaced those two lines with `pass` -> `venv/bin/python -m pytest -q` -> `55 passed in 4.57s`. File restored.

**Sonucu:** A stale checkpoint can again be relabelled as freshly produced on every kernel restart, and load_checkpoint's provenance-mismatch warning — the only mechanism that tells a reader a published number predates a code fix — is silently suppressed. That is the reproducibility-4 defect the code comment says was already lived through once.

**Düzeltme:** Add tests/test_tuning.py::test_resave_of_identical_result_keeps_the_original_provenance — with `patch("src.tuning._CHECKPOINT_DIR", tmp)` and `patch("src.tuning._compute_provenance")` returning P1, save a checkpoint; then switch the patch to return P2 (different src_sha256) and save the IDENTICAL metrics/best_params again; assert the file on disk still records P1 and the original timestamp. Add the counterpart `test_changed_metrics_get_the_current_provenance` — re-save with a different mae and assert the stored provenance is now P2, so the first test cannot be satisfied by simply never updating provenance.

---

## 32. [MAJOR] tests — reproducibility

**Dosya:** `src/tuning.py`

**Ne:** Correct in substance; two count details are off and one consequence needs narrowing. Measured under ./venv/bin/python at HEAD 897524a, the data_sha256 mismatch is 26 of 31 checkpoints (not 28) and src_sha256[src/tuning.py] is 25 (not 27); src_sha256[src/feature_engineering.py] is 0 as stated, and package_versions match under the repo venv. Also, _compute_provenance is diagnostic only — load_checkpoint prints the mismatch and returns the payload unchanged, and model_artifact_is_current compares only src_sha256 — so the wrong file cannot by itself alter any number currently in the thesis; the raw trace has in fact never changed (single commit 3920ee0). The confirmed defect is that data_sha256 fingerprints data/processed/100k_job_with_utilization_full.csv, which none of the 31 checkpointed experiments read, and does not fingerprint the raw trace they all do read via load_sample — leaving the only data-specific field of the reproducibility-4 guard blind to the actual training input, while a regeneration of the unread processed CSV in e4c1b7c fires a false data-change warning on 26 of 31 checkpoints. Nothing in tests/ exercises _compute_provenance or _provenance_mismatches. Fix: hash the raw sample file resolved from raw_data_dir/main_sample_file (and the processed file only when use_processed is actually in play), and add a test asserting data_sha256 matches the file the experiment consumed.

**Kanıt:** src/tuning.py:157: `data_path = _PROJECT_ROOT / data_cfg["processed_data_dir"] / data_cfg["processed_full_file"]` and src/tuning.py:166: `"data_sha256": _file_sha256(data_path) ...`. `grep -rno "use_processed\s*=\s*\w*" notebooks/ src/ scripts/` returns exactly two hits, both in notebook 00 (`use_processed=True`); notebooks 04 and 05 make 11 prepare_features_for_model calls, all with the default False. Executed over results/checkpoints/*.json with _provenance_mismatches against the current environment: 28 of 31 checkpoints mismatch, and the mismatch counter is `Counter({'git_commit': 28, 'data_sha256': 28, 'src_sha256[src/tuning.py]': 27})` — `src_sha256[src/feature_engineering.py]` mismatches 0 times. Every one of those 28 data_sha256 warnings was caused solely by commit e4c1b7c regenerating the processed CSV, a file none of the 28 experiments read.

**Sonucu:** Two failures at once. (1) Swapping or editing the raw trace the models are actually trained on would leave data_sha256 identical, so load_checkpoint reports no data change — the guard is blind to the input it exists to guard. (2) Regenerating the unrelated processed CSV fired a spurious data-change warning on 28 of 31 checkpoints, training the reader to dismiss the warning that matters.

**Düzeltme:** Hash the file the run actually read: fingerprint `raw_data_dir/main_sample_file` (and baseline_estimate_file when used) as `raw_data_sha256`, and hash the processed file only when use_processed=True. Missing test, tests/test_tuning.py::test_provenance_tracks_the_data_actually_loaded — patch load_paths_config to point at two temp CSVs, assert _compute_provenance() changes when the RAW file's bytes change, and assert it does not change when only the processed file changes while use_processed is False.

---

## 33. [MAJOR] training-and-metrics — correctness

**Dosya:** `src/tuning.py:1161-1162, 1225-1234, 1371-1373`

**Ne:** make_narrow_grid()'s _clip_05_10 floors subsample / colsample_bytree / colsample_bylevel / colsample_bynode at 0.5. configs/models.yaml offers colsample_bytree: [0.3, 0.5] for xgboost and [0.3, 0.5, 0.7] for lightgbm, so when random search selects 0.3 every multiplicative variant (0.15, 0.24, 0.3, 0.36, 0.45) is clipped to 0.5 and the narrow grid degenerates to the single value [0.5] — a value the grid never evaluated against 0.3.

**Kanıt:** Executed: `make_narrow_grid('xgb', {... 'colsample_bytree': 0.3, 'subsample': 0.7, ...})` returns `colsample_bytree: [0.5]` (full printed grid: n_estimators [400,500,600], learning_rate [0.025,0.04,0.05,0.06,0.075], max_depth [4,6,8], colsample_bytree [0.5], subsample [0.7], …). The code path is `_narrow_float`: `if name in {"subsample", "colsample_bytree", "colsample_bylevel", "colsample_bynode"}: vals = [_clip_05_10(x) for x in vals]` with `_clip_05_10 = max(0.5, min(1.0, float(v)))`, reapplied at line 1371-1373. Because the resulting list has length 1 the max_grid_size shrink loop (`if param in grid and len(grid[param]) > 1`) never restores best_params[param] either.

**Sonucu:** The grid stage silently overrides the random-search winner with an unvalidated hyperparameter, and finalize_ml_model refits the reported model with colsample_bytree=0.5 instead of the 0.3 that random search actually selected. best_params recorded in the checkpoints and printed as the tuned configuration is then a clipping artefact, not a search result, and the whole lower half of the configured search range is unreachable in the refinement stage.

**Düzeltme:** Clip subsample/colsample_* to the library-legal bound (>0, <=1) rather than 0.5, or at minimum always union the incoming best_params value back into the grid after clipping (`grid[name] = _uniq_sorted(set(vals) | {v})`) so the narrow grid can never fail to contain the point it is supposed to be centred on.

---

## 34. [MAJOR] training-and-metrics — consistency

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (markdown cells fc720756, 0cb81788, ee775324, 27164164, 1f970a8f) and the matching cells in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb`

**Ne:** Real, but under-scoped: all SEVEN result-interpretation markdown cells in notebook 04 are stale, not five. Beyond fc720756, 0cb81788, ee775324, 27164164 and 1f970a8f, add (a) Table 6, cell a69feeed (EN) / f9a80dee (TR), which says CNN won Experiment F with MAE 6704.19 and R2 0.04 and that LSTM was worst at MAE 13169.47 / MdAE 13582.38 / R2 -0.27, whereas cell 95d3ac8e prints LSTM best at 6137.18 / MdAE 2555.25 / R2 0.13 and CNN worst at 6464.84 / R2 0.03 -- so the paragraph's central argument (recurrent layers fail structurally on sparse sequence inputs, median error exceeding mean error) describes a model behaviour that does not occur in the executed run; and (b) Table 7, cell 994212fb (both languages), the notebook's headline conclusion, which declares XGBoost (One-Hot) the best overall model with MAE 3389.26 and R2 0.51, cites MdAE 1128.89, cites an 'LSTM (Experiment D) MAPE of 6.51%' that appears in no output anywhere, and says '19 evaluated configurations' where cell cd31 prints 28 rows. Cell cd31's executed overall table ranks Alibaba Estimate (SJGG) 3151.10, Alibaba Estimate (SJG) 3312.54 and Per-Group Median 3369.14 above every trained model, with the best learned model being LightGBM (Native Categorical) at 4973.16 and XGBoost (One-Hot) only 13th at 6053.96 -- the opposite of what the prose concludes.

**Kanıt:** Cell cd19's stored output (Experiment B table) shows: XGBoost (One-Hot) MAE 6053.96, MdAE 2754.63, RMSE 14056.63, MAPE 1889.24, R² 0.18; best MAE row is Alibaba Estimate - GroupGPU at 3151.10. The markdown cell 0cb81788 immediately below states 'XGBoost with One-Hot encoding achieved the best overall performance, producing the lowest Test MAE (3389.26) and Test RMSE (11375.19) while also obtaining the highest R² score (0.51)' and 'the lowest MAPE at 10.80% and the lowest MdAE at 1128.89 seconds'. Same pattern for cell cd11 (LightGBM 5811.00 is the lowest MAE; RF 6796.98 / MAPE 2412.01 / R² 0.02) vs cell fc720756 ('Random Forest … lowest Test MAE (4316.39) … lowest MAPE at 16.85% … R² 0.27 … MdAE 1508.10 seconds', 'LightGBM … R² = 0.22'); cell 693a11bd vs ee775324 (CNN 'Test MAE (6453.92)' vs checkpoint 7040.17); 27164164 (LSTM 5836.32 / MAPE 22.30% vs exp_d_lstm 6316.79 / 2356.55); 1f970a8f (LSTM 6923.04 / MAPE 27.64% vs exp_e_lstm 6844.75 / 2504.15).

**Sonucu:** A reader of the notebook is told which model won and by how much using numbers that the notebook's own executed cells refute; the quoted MAPE values (10.80 %, 16.85 %, 22.30 %, 27.64 %) are two orders of magnitude below what evaluate_regression actually returns on this heavy-tailed target (100-2900 %), which misrepresents the models' percentage error entirely.

**Düzeltme:** Regenerate every table-interpretation paragraph from the executed tables after the notebook is re-run (finding stale-checkpoint-metrics-vs-refit-models must be fixed first), or emit them programmatically from the DataFrame so they cannot drift; at minimum strip the hard-coded numerals and let the paragraph reference the table.

---

## 35. [MINOR] config-and-dead-code — consistency

**Dosya:** `notebooks/en/00_data_preparation.ipynb cell 5dd6c695, notebooks/en/03_feature_engineering.ipynb cell c9e6e811 (tr: 5dd6c695, 5c322c99)`

**Ne:** After this session's change to `include_cpu_only=True`, the cells that build the canonical job table no longer drop the 17,816 CPU-only rows -- the restriction moved to the following cell. The markdown cells that narrate those steps were not updated and now state the opposite of the output printed directly above them.

**Kanıt:** nb00 at HEAD: cell [9] cd06 runs `build_job_table_from_sample(raw_df, time_unit="s", include_cpu_only=True)` and its stored output is `[Step 2] After filtering : 100,000 jobs (GPU + CPU-only)`; the immediately following cell [10] 5dd6c695 reads `> **Filtering complete: 100,000 → 82,184 valid GPU jobs.** 17,816 records with zero GPU demand or missing runtime have been excluded.` nb03 at HEAD: cell [11] cd08 prints `[Step 2] Feature matrix shape : (100000, 9)`; the following cell [12] c9e6e811 reads `> **Data Filtering and Shape Check:** 17,816 missing or defective hardware rows ... have been purged, leaving a pure matrix of 82,184 validated GPU jobs.` I confirmed the counts by execution: build_job_table_from_sample(include_cpu_only=True) -> 100000 rows, include_cpu_only=False -> 82184.

**Sonucu:** The two notebooks that document the data-preparation change most directly now assert a filtering step that their own code and printed output show did not happen there, which undermines a reader's ability to confirm the CPU-only fix was applied in the right order.

**Düzeltme:** Rewrite both markdown cells (en and tr) to say the canonical table retains all 100,000 valid rows so the sweep-line sees CPU-only background load, and that the restriction to 82,184 GPU jobs happens after the sweep-line in the next cell.

---

## 36. [MINOR] en-tr-parity — consistency

**Dosya:** `notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb (cell id 'cd28'); EN counterpart notebooks/en/04_runtime_prediction_models.ipynb ('cd28')`

**Ne:** In notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb cell 'cd28' (section "20. LSTM (One-Hot)"), the closing print line is a verbatim copy of cell 'cd27''s line — both the variable and the label: `print(f"[Exp D / CNN] Final — MAE: {cnn_metrics_cat['mae']:.2f}s MdAE: {cnn_metrics_cat['mdae']:.2f}s RMSE: {cnn_metrics_cat['rmse']:.2f}s MAPE: {cnn_metrics_cat['mape']:.4f} R²: {cnn_metrics_cat['r2']:.4f}")`. The EN counterpart at the same id correctly prints `[Exp D / LSTM]` with `lstm_metrics_cat`. Since `cnn_metrics_cat` is bound by 'cd27', the cell runs without error and re-emits the CNN's line; the stored TR output for 'cd28' is byte-identical to 'cd27' (`[Exp D / CNN] Final — MAE: 6524.73s MdAE: 3345.99s RMSE: 14837.29s MAPE: 2239.7931 R²: 0.0908`), whereas EN 'cd28' shows the LSTM's actual result (MAE 6316.79s, R² 0.1145, matching results/checkpoints/exp_d_lstm.json). Two corrections to the claim as filed: (1) it is NOT reported as the LSTM's numbers — the label also reads "[Exp D / CNN]", so the cell shows a duplicated CNN line rather than CNN metrics under an LSTM label; the risk is a reader misreading it because it sits inside the LSTM section, not a false attribution. (2) "the LSTM's real numbers are never printed in the Turkish notebook at all" is false: TR cell 'c0607428', the very next reporting cell, prints the Experiment-D table with `LSTM 6316.79 2945.92 14645.14 1837.11 0.11`, and 'cd31' also resolves `lstm_metrics_cat` correctly. So the misleading window is a single cell and no aggregate table, figure, or thesis-reported number is affected. (The 5836.32 MAE in markdown 'dc9cf8a9' matches neither checkpoint and is a separate prose-staleness issue, not support for this one.)

**Kanıt:** TR 'cd28' last line: `print(f"[Exp D / CNN] Final — MAE: {cnn_metrics_cat['mae']:.2f}s MdAE: {cnn_metrics_cat['mdae']:.2f}s RMSE: {cnn_metrics_cat['rmse']:.2f}s MAPE: {cnn_metrics_cat['mape']:.4f} R²: {cnn_metrics_cat['r2']:.4f}")` vs EN 'cd28': `print(f"[Exp D / LSTM] Final — MAE: {lstm_metrics_cat['mae']:.2f}s ... {lstm_metrics_cat['r2']:.4f}")`. The mismatch is already frozen into the stored outputs: EN 'cd28' output = `[Exp D / LSTM] Final — MAE: 6316.79s MdAE: 2945.92s RMSE: 14645.14s MAPE: 1837.1132 R²: 0.1145`; TR 'cd28' output = `[Exp D / CNN] Final — MAE: 6524.73s MdAE: 3345.99s RMSE: 14837.29s MAPE: 2239.7931 R²: 0.0908`, byte-identical to TR 'cd27' (the CNN cell). I scanned every `... Final —` print in both NB04 files against the metrics variable assigned in the same cell; 'cd28' in the TR notebook is the only mismatch in either language.

**Sonucu:** A reader of the Turkish notebook sees the Exp-D LSTM (One-Hot) reported as MAE 6524.73 s / R² 0.0908 when its actual held-out result is MAE 6316.79 s / R² 0.1145 — the LSTM's real numbers are never printed in the Turkish notebook at all. This directly contradicts the Turkish notebook's own prose: markdown cell 'dc9cf8a9' claims the Deney-D LSTM is the best DL model at 5836.32 MAE, and NB05's markdown 'da4b8662' headlines SJF-LSTM (Categorical) as the leading policy. (The downstream summary table 'cd31' correctly uses `lstm_metrics_cat`, so aggregate tables are unaffected — only the per-model line the reader sees at the point of training.)

**Düzeltme:** In notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb cell 'cd28', change the final print to use `lstm_metrics_cat` and the label `[Exp D / LSTM]`, matching the EN cell verbatim, then re-execute the cell so the stored output carries the LSTM's numbers.

---

## 37. [MINOR] en-tr-parity — correctness

**Dosya:** `notebooks/tr/05_gorev_zamanlayici_degerlendirme_32_gpu.ipynb (markdown 'md14') and notebooks/tr/05_gorev_zamanlayici_degerlendirme_256_gpu.ipynb ('md14'); EN counterparts have no such block`

**Ne:** Both TR notebook-05 files carry a TR-only 'Simülasyon Kurulum Parametreleri' block in cell md14 whose 'Küme Mimarisi' line misdescribes the cluster the code builds, while the EN md14 cells contain no equivalent setup-parameter block. Two specific errors: (a) the node-count prefix — TR 32-GPU says '4 Düğüm × 8 GPU', TR 256-GPU says '32 Düğüm × 8 GPU', but cd15 (identical in EN and TR) calls provision_heterogeneous_gpu_cluster(n_high=2, n_mid=8) / (n_high=16, n_mid=64), and src/simulation/multi_node_simulator.py:619-638 gives high nodes 8 GPU and mid nodes 2 GPU, so the cluster is 10 nodes of two profiles (not 4 × 8 GPU) and 80 nodes (not 32 × 8 GPU) — a 2.5x node-count error; (b) the '+ 3 CPU Düğümü' / '+ 24 CPU Düğümü' clause describes CPU-only nodes that are never created — cd15 hardcodes n_cpu=0 and it is the only provisioning call in either notebook. Running provision_heterogeneous_gpu_cluster with the notebook defaults returns nodes=10/total_gpu=32/cpu_only=0 and nodes=80/total_gpu=256/cpu_only=0. Note the block's own parenthetical GPU breakdown (2 high [16 GPU] + 8 mid [16 GPU]; 16 high [128] + 64 mid [128]) IS correct and matches the code, so the block is internally inconsistent — only the '<N> Düğüm × 8 GPU' prefix and the CPU-node clause are wrong. The rest of the block is accurate (LOAD_FACTOR = 0.1 matches cd13). No computed metric changes; the defect is that the TR notebook misdescribes the hardware configuration underlying every scheduling number in notebook 05, and does so in one language only, contradicting both the code's documented n_cpu=0 rationale and the EN text.

**Kanıt:** TR 32-GPU 'md14': "**Küme Mimarisi:** 4 Düğüm × 8 GPU = Toplam 32 GPU (Heterojen: 2 Yüksek Performanslı Düğüm [16 GPU] + 8 Orta Seviye Düğüm [16 GPU] + 3 CPU Düğümü)." TR 256-GPU 'md14': "32 Düğüm × 8 GPU = Toplam 256 GPU (Heterojen: 16 Yüksek Performanslı Düğüm [128 GPU] + 64 Orta Seviye Düğüm [128 GPU] + 24 CPU Düğümü)." The code in cell 'cd15' of both notebooks (identical in EN and TR) builds `machines = provision_heterogeneous_gpu_cluster(n_high=n_high, n_mid=n_mid, n_cpu=0)` under the comment "32 GPUs total: 2*8 + 8*2" / "256 GPUs total 16*8 + 64*2", plus "n_cpu=0: ... Provisioning none keeps the simulated cluster identical to the node profiles reported in the thesis". src/simulation/multi_node_simulator.py:619-638 confirms `n_cpu: int = 0` with the same rationale. So there are 10 nodes (not 4) at 32 GPUs and 80 nodes (not 32) at 256 GPUs, and zero CPU-only nodes in either.

**Sonucu:** A Turkish reader is told the simulated cluster contains 3 (resp. 24) CPU-only nodes that are never created, and a node count that is off by 2.5x. An English reader gets no such claim at all, so the two versions describe different hardware for the same simulation.

**Düzeltme:** Either delete the 'Küme Mimarisi' bullet from both Turkish 'md14' cells, or correct it to match the code (32-GPU: 2 high-performance nodes x 8 GPU + 8 mid-range nodes x 2 GPU = 10 nodes, no CPU-only nodes; 256-GPU: 16 x 8 + 64 x 2 = 80 nodes, no CPU-only nodes) and add the same corrected block to the English 'md14' cells so both languages state the configuration identically.

---

## 38. [MINOR] export-and-artifacts — consistency

**Dosya:** `notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (cell 7a843cee) and notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (cell 8d1a4e10)`

**Ne:** The rank-correlation cells (notebooks/en/05_scheduler_evaluation_32_gpu.ipynb cell 7a843cee lines 149-154; 05_scheduler_evaluation_256_gpu.ipynb cell 8d1a4e10 lines 143-148) write mae_spearman_vs_jct_gain_{N}gpu.png directly into thesis/latex/figures/ and results/figures/thesis_export/png/, bypassing the all-or-nothing _sync_thesis_figures gate that scripts/export_thesis_results.py:96-98 documents as the single mechanism for thesis figures. Two consequences are confirmed. (1) Same-name conflict in thesis/latex/figures/ only: the notebook writes a 300-dpi render (4461x2299) taken BEFORE the "higher is better" fig.text is added, while _sync_thesis_figures would write the 200-dpi inline capture (rcParams savefig.dpi=200, cell cd01) under the identical filename WITH the key. Whichever ran last wins; the two can never agree. The copy on disk today is the notebook's 300-dpi version, and its top strip visibly lacks the key even though the y-axis label uses arrow notation. (2) The gate is defeated: thesis/latex/figures/ contains mae_spearman_vs_jct_gain_{32,256}gpu.png but not nb05-fig06-load-backfill-sensitivity_{32,256}gpu.png (map position 2), proving _sync_thesis_figures never synced NB05 (the 32-GPU notebook stores 0 inline PNGs, the 256-GPU one stores 1, from cell 31 rather than the rank-corr cell) while the bypass still deposited its output from that same untrusted run. _clean_stale_exports's glob f"{prefix}-Figure*.png" can never clean or refresh that filename in the export dir either. Corrections to the original claim: the shipped variant is HIGHER resolution (300 dpi), not lower-fidelity -- it differs by missing the in-figure key, not by degradation; there is no same-name collision in the export png dir (the exporter names its scrape NB05_32GPU-Figure01.png there); the cited md5 b46abf38... is stale (now c4d011411f77241fc456617aa4bcb20d, still byte-identical between the results/ and thesis/ copies); the cited 1265x709 inline PNG does not exist in either notebook; and no .tex file includegraphics this figure, so it does not currently appear in the compiled thesis. Fix: move the savefig block after the fig.text key, or drop the direct savefig entirely and let THESIS_FIGURE_MAP own the file.

**Kanıt:** Cell 7a843cee, lines 149-161: `for _out_dir in [` / `PROJECT_ROOT / "results" / "figures" / "thesis_export" / "png",` / `PROJECT_ROOT / "thesis" / "latex" / "figures",` / ... / line 154 `fig.savefig(_out_dir / f"mae_spearman_vs_jct_gain_{N_GPU}gpu.png", dpi=300, bbox_inches="tight")` — then line 158 `fig.text(0.995, 0.995, "↑ higher is better", ha="right", va="top",` and line 161 `plt.show()`. Same ordering in 8d1a4e10 (savefig line 148, fig.text line 152). The script's own comment at scripts/export_thesis_results.py:96-98 asserts the opposite — "routing it through the map keeps every thesis figure on this one mechanism". Measured: the errored run's inline PNG is 1265x709 (figure.dpi 200, key present) while results/figures/thesis_export/png/mae_spearman_vs_jct_gain_32gpu.png is 4461x2299 (dpi 300, key absent, authored width 14.87in) — md5 b46abf38a41e171ab844de3bc270af43, byte-identical to the copy in thesis/latex/figures. _clean_stale_exports's glob is `f"{prefix}-Figure*.png"`, so the export script can never clean or refresh that filename either.

**Sonucu:** The one thesis figure the export gate is supposed to protect is the one it cannot protect: a partial notebook run (which the gate then refuses to sync) still overwrites the thesis copy, and the artifact that ships is a lower-fidelity variant missing the in-figure key that the exported copy carries. Two files with the same name in results/ and thesis/ can never be made to agree.

**Düzeltme:** Delete the `for _out_dir in [...] fig.savefig(...)` block and let THESIS_FIGURE_MAP own the file (('NB05_32GPU',1) already maps to it); if a 300-dpi copy is wanted, add the key first and save after plt.show()-equivalent finalisation.

---

## 39. [MINOR] feature-engineering — consistency

**Dosya:** `notebooks/en/00_data_preparation.ipynb (cell md05); notebooks/tr/00_veri_hazirlama.ipynb (cell md05)`

**Ne:** Cell md05 in notebooks/en/00_data_preparation.ipynb and notebooks/tr/00_veri_hazirlama.ipynb contains three separate inaccuracies about the temporal step it introduces (the original claim covers the first two): 1. It names three features that the pipeline never produces: `arrival_hour`, `arrival_dayofweek`, `arrival_minute`. `add_temporal_features` (src/feature_engineering.py:161-210) produces exactly `hour_of_day` and `day_of_week`; cd08's own output prints `['job_id', 'arrival_time', 'arrival_sec', 'job_runtime', 'gpu_demand', 'user', 'gpu_type', 'num_inst', 'num_cpu', 'hour_of_day', 'day_of_week', 'cluster_load_cpu', 'cluster_load_gpu', 'active_job_count']`. A grep over the whole repo finds the three `arrival_*` names only inside these two markdown cells. No minute-resolution feature exists anywhere. 2. It asserts the features "convey the daily and weekly cyclical patterns" (tr: "gunluk ve haftalik dongusellugini"), contradicting src/feature_engineering.py:181-194, which documents `day_of_week` as a "Trace-relative day index (0, 1, 2, ...), counted from the first job's arrival, NOT the calendar weekday" that "does not carry a verified weekly-cycle interpretation and should not be read as e.g. 'Monday' in the text." Confirmed by running the function: a job 8 days after trace start receives day_of_week=7, not 0. The weekly reading was formally retracted elsewhere in the repo (thesis/latex/chapters/3.dataset_and_workload.tex:104 and 6.results_and_discussion.tex:83), so md05 is the last surviving statement of the withdrawn interpretation. 3. Not in the original claim but in the same cell: "The `arrival_time` column is dropped *after* these features are derived" is false. `arrival_time` is never dropped -- cd08's printed columns include it, and both data/processed/100k_job_with_utilization.csv and 100k_job_with_utilization_full.csv carry an `arrival_time` column. It is only excluded from `numeric_cols` in prepare_features_for_model (src/feature_engineering.py:493-504). Only the accompanying `ValueError` statement is accurate (raised at src/feature_engineering.py:199-200 when the column is absent). Severity stays minor: the cell is inert prose and no reported number, figure or table changes. The impact is that a reader auditing the pipeline from the notebook is given wrong feature names, a weekly-cycle interpretation the thesis explicitly withdrew for leakage reasons, and a false claim about column dropping.

**Kanıt:** Cell `md05` (en): "Job arrival timestamps (`arrival_time`) are converted into temporal derived features (`arrival_hour`, `arrival_dayofweek`, `arrival_minute`). These features convey the daily and weekly cyclical patterns of the workload to the model." The cell it introduces (`cd08`) calls `add_temporal_features`, whose printed output for this run is `['... 'hour_of_day', 'day_of_week', ...]` -- there is no `arrival_hour`, `arrival_dayofweek` or `arrival_minute` anywhere in src/. src/feature_engineering.py:181-194 states day_of_week is a "Trace-relative day index (0, 1, 2, ...) ... NOT the calendar weekday" and that it "does not carry a verified weekly-cycle interpretation". The tr cell md05 carries the identical claim ("günlük ve haftalık döngüselliğini").

**Sonucu:** A reader auditing the pipeline from the notebook is told the model receives weekday/weekly-cycle features that do not exist, directly contradicting the corrected day_of_week definition the rest of the repository now documents.

**Düzeltme:** Rewrite md05 (en and tr) to name `hour_of_day` and `day_of_week`, and describe day_of_week as a trace-relative day counter with no weekly-cycle interpretation.

---

## 40. [MINOR] feature-engineering — consistency

**Dosya:** `notebooks/en/00_data_preparation.ipynb (cell 5dd6c695); notebooks/tr/00_veri_hazirlama.ipynb (cell 5dd6c695)`

**Ne:** Confirmed as filed, with one addition and one sharpening. (1) Stale placement — confirmed. In notebooks/en/00_data_preparation.ipynb and notebooks/tr/00_veri_hazirlama.ipynb, cell `cd06` now calls build_job_table_from_sample(..., include_cpu_only=True) and its stored output is "[Step 2] After filtering : 100,000 jobs (GPU + CPU-only)". The markdown cell `5dd6c695` directly beneath it still reads "Filtering complete: 100,000 -> 82,184 valid GPU jobs." The 82,184 restriction actually occurs two cells later in `cd08` (job_df[job_df["gpu_demand"] > 0], output "[Step 3] Restricted to GPU jobs : 82,184 of 100,000"). Verified by git: commit e4c1b7c rewrote `cd06` (its output previously read "[Step 2] After filtering : 82,184 valid GPU jobs") but left cell `5dd6c695` byte-identical. (2) "missing runtime" is factually false — confirmed by running. src/feature_engineering.py:113-116 applies `_valid = df["duration"] > 0` and only ANDs `num_gpu > 0` when include_cpu_only=False. Executing load_main_sample() gives 100,000 rows with duration > 0 for all 100,000 (0 NaN), and num_gpu == 0 for exactly 17,816. So zero rows are dropped for runtime; all 17,816 excluded records are CPU-only jobs, which the change deliberately retains for the sweep-line. This clause was already wrong before the change and remains wrong. (3) Addition the claim missed: the sibling markdown cell `9c133c5e` (EN and TR) has the same stale-narrative defect — it states the sweep-line computed active_job_count / cluster_load_cpu / cluster_load_gpu "at the arrival moment of each of the 82,184 jobs", but post-change the sweep-line runs over all 100,000 rows (GPU + CPU-only) and only its retained output is 82,184. Any fix should update both `5dd6c695` and `9c133c5e` in both language notebooks. Severity stays minor: no computed number, figure or table changes, and the end-state count 82,184 is correct. It stands because the annotations misrepresent the provenance of the cluster-load features the thesis reports — a reader following them concludes CPU-only jobs were removed before the sweep-line, i.e. exactly the leakage-4 / robustness-11 behaviour the change removed — and because "missing runtime" is a false statement about the dataset's composition.

**Kanıt:** Cell `cd06` output: `[Step 2] After filtering : 100,000 jobs (GPU + CPU-only)`. The very next markdown cell `5dd6c695` says: "**Filtering complete: 100,000 -> 82,184 valid GPU jobs.** 17,816 records with zero GPU demand or missing `runtime` have been excluded." The 82,184 restriction now happens two cells later, in `cd08`. The claim about "missing `runtime`" is also false: executed check shows `duration > 0` for all 100,000 rows, so zero rows are dropped for runtime; the 17,816 are exactly the `num_gpu == 0` CPU-only jobs, which are valid jobs deliberately retained for the sweep-line. Identical text in the tr cell 5dd6c695.

**Sonucu:** The notebook's own narrative denies the change that the session made -- a reader following the annotations concludes CPU-only jobs are excluded before the sweep-line, which is exactly the leakage-4/robustness-11 behaviour the change was made to remove.

**Düzeltme:** Rewrite 5dd6c695 (en and tr) to say that all 100,000 valid rows (0 dropped for non-positive duration) enter the sweep-line, and that the 17,816 CPU-only jobs are dropped after it in cd08, leaving 82,184 modelled GPU jobs.

---

## 41. [MINOR] feature-engineering — consistency

**Dosya:** `notebooks/en/03_feature_engineering.ipynb (cell c9e6e811); notebooks/tr/03_ozellik_muhendisligi.ipynb (cell 5c322c99)`

**Ne:** The annotation on the shape-check cell claims 17,816 defective rows were purged; the cell it annotates prints a frame that still has all 100,000 rows, and no row in the trace is defective.

**Kanıt:** Cell `cd08` stored output: `[Step 2] Feature matrix shape : (100000, 9)`. Immediately following markdown `c9e6e811`: "17,816 missing or defective hardware rows capable of corrupting machine learning models have been purged, leaving a pure matrix of 82,184 validated GPU jobs." Cell `cd07`'s own output two cells earlier reads `[Step 2] Jobs incl. CPU-only : 100,000 (removed 0 invalid rows)`. The 17,816 rows are `num_gpu == 0` CPU-only jobs (verified: `a["num_gpu"].value_counts()[0.0] == 17816`), removed later in `cd10`, and they are neither missing nor defective -- they are the background-load rows the sweep-line now deliberately consumes. tr cell 5c322c99 carries the same claim ("eksik veya hatalı 17.816 donanım satırı temizlenmiş").

**Sonucu:** Misdescribes the dataset's composition: it presents a deliberate scope restriction (GPU jobs only) as data cleaning of corrupt records, and attributes the restriction to a cell that performs no filtering.

**Düzeltme:** Reword c9e6e811 / 5c322c99 to state that 0 rows are invalid, that all 100,000 rows enter the sweep-line, and that the 17,816 CPU-only jobs are excluded from modelling (not from the load computation) in the next cell.

---

## 42. [MINOR] feature-engineering — consistency

**Dosya:** `notebooks/en/01_data_overview.ipynb (cells 67390a4c, 35d12bbd)`

**Ne:** In notebooks/en/01_data_overview.ipynb, two committed markdown captions misdescribe the temporal axis. Cell 35d12bbd states "the hour values in the heatmap correspond to **UTC time** and do not reflect the actual local time of the cluster", and cell 67390a4c states arrival_time is "a valid **UTC timestamp** ... which preserves hour-level information". Verified: raw submit_time min = 0 and processed arrival_time min = 1970-01-01 00:00:03 (max 1970-01-08 15:51:32), hour_of_day == arrival_time.dt.hour for all 100k rows, and no tz_localize/tz_convert/utc=True anywhere in src/ or notebooks/en/. arrival_time is therefore epoch + trace-relative offset, so hour_of_day is a 24-hour phase anchored to the trace's undisclosed start hour, not UTC wall-clock time. Note the "valid UTC timestamp" wording in 67390a4c is defensible as a bare datatype statement (pd.to_datetime is nominally UTC); the actual misstatement is the "preserves hour-level information" implication plus 35d12bbd's explicit assertion that the heatmap hours are UTC clock time. Both captions contradict the same notebook's code cell cd21 ("The trace starts at the epoch (1970-01-01), so .dt.dayofweek encodes where the trace happens to sit on the calendar, not a real weekday") and src/feature_engineering.py:167-180 ("a 24-hour cyclical phase of unknown absolute alignment -- still a meaningful diurnal-rhythm signal, just not '9am' in any verifiable sense"); cd21's own axis label uses a third, inconsistent phrasing ("local clock time within each trace day"). Cell 35d12bbd additionally still carries an unresolved placeholder, committed at HEAD: "the specific per-day intensities will be re-described here once the pipeline is re-run under the corrected feature below." Consequence is interpretive, not numeric: no reported metric changes, but a reader is licensed to read absolute clock times off Figure 3/Figure 6, and the captions were left un-updated when the day-index correction landed.

**Kanıt:** Cell `67390a4c`: "The original `arrival_time` is a valid **UTC timestamp** obtained via `pd.to_datetime(submit_time, unit='s')`, which preserves hour-level information." Cell `35d12bbd`: "Since no timezone conversion is applied, the hour values in the heatmap correspond to **UTC time** and do not reflect the actual local time of the cluster." The same notebook's code cell `cd21` states the opposite in a comment: "The trace starts at the epoch (1970-01-01), so .dt.dayofweek encodes where the trace happens to sit on the calendar, not a real weekday". src/feature_engineering.py:175-180 documents hour_of_day as "a 24-hour cyclical phase of unknown absolute alignment -- still a meaningful diurnal-rhythm signal, just not '9am' in any verifiable sense." Executed: raw `submit_time` min = 0, and notebook 00's cd06 output shows `arrival_time` min = `1970-01-01 00:00:00`. Cell `35d12bbd` additionally still reads: "the specific per-day intensities will be re-described here once the pipeline is re-run under the corrected feature below."

**Sonucu:** Readers are told the diurnal axis is anchored to real UTC clock time, which would let them read absolute times off Figure 3/Figure 6; it is only a trace-relative 24-hour phase. The leftover placeholder sentence advertises an unfinished revision in a published notebook.

**Düzeltme:** Replace the UTC claims with the module's own framing (a 24-hour cyclical phase of unknown absolute alignment, valid for relative diurnal structure only), and either complete or delete the 'will be re-described here' sentence.

---

## 43. [MINOR] notebook-pipeline-consistency — reproducibility

**Dosya:** `src/tuning.py:155-169 (_compute_provenance)`

**Ne:** _compute_provenance (src/tuning.py:155-169) fingerprints data/processed/100k_job_with_utilization_full.csv, which no modelling code reads: every prepare_features_for_model call site leaves use_processed=False, so features are recomputed from data/alibaba_cluster_trace/pai_job_no_estimate_100K.csv, a file the provenance never hashes. The data_sha256 leg of the guard is therefore both a false-positive source (the 'c1897f83... -> 3f3d6d19...' line printed by load_checkpoint on ~30 cells of notebook 04 EN/TR is entirely the notebook-00 CSV rewrite in commit e4c1b7c, verified by hashing the file at 6b04232 and at e4c1b7c) and blind to the file the models consume. Two corrections to the claim as filed: (1) the warning block is not raised "purely" by the CSV rewrite -- it also carries a genuine src_sha256[src/tuning.py] mismatch line; (2) the false-negative is partly mitigated, since the raw trace is git-tracked and _provenance_mismatches also compares git_commit, so a committed replacement of the raw trace would still be flagged -- only an uncommitted working-tree swap would pass silently. Impact is confined to the diagnostic message: model_artifact_is_current compares src_sha256 only, so data_sha256 gates no load-vs-retrain or artifact-reuse decision, and no reported metric, figure or table value changes.

**Kanıt:** src/tuning.py:156-157 `data_cfg = load_paths_config()["data"]; data_path = _PROJECT_ROOT / data_cfg["processed_data_dir"] / data_cfg["processed_full_file"]`. grep for `use_processed` across src/, scripts/, tests/ and notebooks/ returns only the definition and docstring in src/feature_engineering.py:409/431/446 plus two comment mentions in the notebook-00 files -- no call site ever passes True, and no notebook calls load_processed_full(). The consequence is visible in notebook 04's own saved output for cell cd17: `- data_sha256: 'c1897f83...' -> '3f3d6d19...'`, a warning raised purely because notebook 00 rewrote the processed CSV (commit e4c1b7c), which cannot affect any model.

**Sonucu:** The reproducibility guard fires on a change that provably cannot alter any result, training readers and re-runners to ignore its warnings, and stays silent if the raw trace file the models actually consume is ever replaced. It is also the noise that masked the genuine feature_engineering mismatch in the finding above.

**Düzeltme:** Hash the file(s) actually read: the raw sample resolved from paths.yaml (`raw_data_dir/main_sample_file`, plus `baseline_estimate_file` when the Alibaba-estimate baselines are in play), and hash the processed CSV only under a separate key used by code paths that set use_processed=True.

---

## 44. [MINOR] notebook-pipeline-consistency — correctness

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb cells cd19 and cd31 (mirrored in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb)`

**Ne:** Confirmed and correctly diagnosed, with one clarification of impact. In notebooks/en/04_runtime_prediction_models.ipynb (and its tr/ mirror), `group_median_metrics` is the only `*metrics*` variable subscripted in cd19 and cd31 without a `... if '...' in dir() else _m('exp_b_group_median')` checkpoint rebind (cd19 has 12 such rebinds, cd31 has 24; regex confirms group_median_metrics is the sole omission in both), and its row is the only baseline row lacking the `is not None` guard the three Alibaba rows carry. Executing the real cell sources reproduces both failures: a fresh namespace running cd19 alone raises NameError at cd19 line 32, cd31 alone at cd31 line 51 -- immediately after `[Checkpoint] Loaded 31 experiment results from disk.`, i.e. exp_b_group_median.json is loaded and never consulted; and with the documented `_alibaba_available = False` skip path simulated, both cells raise TypeError while the Alibaba rows skip cleanly. grpmed01 cannot substitute as the resume path because its `if ckpt:` branch sits after the `_grp_lookup` merge over `_raw_main`/`_est_raw`. Clarification the claim omits: shipped outputs show grpmed01 (exec 32), cd19 (41) and cd31 (75) all completed in the sequential run, so every reported number in the Experiment B table and master summary is correct and unaffected. The defect is a reproducibility regression, not a wrong result -- it blocks regeneration of two reported tables on the checkpoint-resume path the cells explicitly advertise and on the graceful-skip path when pai_job_duration_estimate_100K.csv is absent, and it fails loudly rather than silently. Fix is two lines per cell. (Evidence nit: "ten metric variables" is actually twelve in cd19; substance unaffected.)

**Kanıt:** cd19 rebinds ten metric variables in the form `constant_zero_metrics = constant_zero_metrics if 'constant_zero_metrics' in dir() else _m('exp_b_constant_zero')` but has no such line for `group_median_metrics`; the row `{"Model": "Per-Group Median (baseline)", "Test MAE (s)": group_median_metrics["mae"], ...}` carries no `if r is not None` suffix, unlike the three rows immediately after it. cd31 repeats both omissions. grpmed01 contains `if not _alibaba_available: print("[Exp B / GroupMedian] SKIPPED..."); group_median_metrics = None`.

**Sonucu:** On a checkpoint-resume kernel started at the results-table cells, cd19/cd31 raise NameError; if pai_job_duration_estimate_100K.csv is unavailable they raise TypeError instead of skipping the row, so notebook 04 cannot produce Experiment B or the master summary at all -- even though results/checkpoints/exp_b_group_median.json exists and could supply the value.

**Düzeltme:** Add `group_median_metrics = group_median_metrics if 'group_median_metrics' in dir() else _m('exp_b_group_median')` to cd19 and cd31, and append `if group_median_metrics else None` to the Per-Group Median row in both tables, matching the Alibaba rows.

---

## 45. [MINOR] notebook-pipeline-consistency — consistency

**Dosya:** `notebooks/en/00_data_preparation.ipynb markdown cell md05 (and notebooks/tr/00_veri_hazirlama.ipynb md05)`

**Ne:** Notebook 00 md05 (EN and TR) contains three separate factual errors about the temporal step. (a) It names `arrival_hour`, `arrival_dayofweek`, `arrival_minute`; add_temporal_features (src/feature_engineering.py:205-209) creates exactly `hour_of_day` and `day_of_week`, and the three named strings appear nowhere else in the repository outside md05 and its generated HTML. (b) It asserts "The `arrival_time` column is dropped *after* these features are derived" -- no drop occurs: cd08's stored output lists arrival_time among the 14 columns, cd10 exports job_df unmodified, and the header of data/processed/100k_job_with_utilization_full.csv begins `job_id,arrival_time,arrival_sec,...`. The column is in fact load-bearing downstream: prepare_features_for_model(use_processed=True) re-parses it at src/feature_engineering.py:456-457. (c) "daily and weekly cyclical patterns" reasserts the calendar-weekday reading the day_of_week docstring (src/feature_engineering.py:181-194) explicitly rules out -- it is a trace-relative absolute day counter, chosen precisely because a real dt.dayofweek would fold trace-day 0 onto trace-day 7 across the chronological split. Refinement to the original claim: md05's follow-on sentence "Reversing the order causes a `ValueError`" does describe a real guard (line 201-202 raises when arrival_time is absent); only the "is dropped" premise is false. Fix is to md05 prose only -- no code or number changes.

**Kanıt:** md05: "Job arrival timestamps (`arrival_time`) are converted into temporal derived features (`arrival_hour`, `arrival_dayofweek`, `arrival_minute`). These features convey the daily and weekly cyclical patterns... **Important:** The `arrival_time` column is dropped *after* these features are derived." add_temporal_features (src/feature_engineering.py:205-209) creates exactly `hour_of_day` and `day_of_week`; there is no `arrival_minute`. Notebook 00 never drops arrival_time -- the header of data/processed/100k_job_with_utilization_full.csv is `job_id,arrival_time,arrival_sec,...` and the cell cd08 output prints `Columns: ['job_id', 'arrival_time', ...]`. The "weekly cyclical patterns" reading is also the one the day_of_week docstring (feature_engineering.py:181-194) explicitly rules out: it is an absolute trace-day counter, not a calendar weekday.

**Sonucu:** The chapter-0 narrative describes a different feature set and a different schema than the CSV it writes and than the columns notebooks 02/03/04/05 consume, and it reasserts the calendar-weekday interpretation the code was changed to avoid.

**Düzeltme:** Rewrite md05 to name `hour_of_day` and `day_of_week`, describe day_of_week as a trace-day index rather than a weekday, and delete the claim that arrival_time is dropped (or drop it and stop exporting it, matching notebook 03's cd16).

---

## 46. [MINOR] regression-this-session — consistency

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells ckpt_a_rf, ckpt_a_xgb, ckpt_a_lgb, ckpt_b_rf, ckpt_b_xgb, ckpt_b_lgb_oh, ckpt_b_lgb_nat, cc712f54, ckpt_c_*, ckpt_d_*, ckpt_e_*, ckpt_f_* — 20 cells; identical in notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb)`

**Ne:** Real but latent and partly misstated. Nothing currently reported is affected: all 20 sidecars match the current src_sha256, and the only realized skew (checkpoints record tuning.py@657bd879, artifacts @4e4f5b5) spans a diff that touches only save_checkpoint/model_artifact_is_current/record_model_artifact — not the training path — so the artifacts on disk are numerically the ones the checkpoint metrics describe. It is also not fully silent: load_checkpoint prints a provenance WARNING naming the changed file and the cell prints "[RECOVER] Re-finalizing ...". And the direction of the fix is right, not wrong — refreshing the artifact is what keeps rob7cell/l5r9cell01/NB05 from evaluating a pre-change model on post-change data (the 4e4f5b5 bug); the stale summary numbers come from the pre-existing, deliberately diagnostic-only checkpoint reuse in load_checkpoint. The accurate statement is narrower: the RECOVER branch computes fresh metrics for the artifact it writes and throws them away, so after any substantive edit to src/feature_engineering.py or the training code in src/tuning.py the model file and the metrics reported beside it come from different pipelines, with no reconciliation. Note that simply writing the refit metrics back is not the fix — save_checkpoint would then see a changed result and restamp the checkpoint with the current provenance, suppressing the staleness warning while the hyperparameters still came from a search on the old pipeline; the correct remedy is to invalidate the checkpoint and retrain (or at minimum mark the checkpoint stale) when model_artifact_is_current is False.

**Kanıt:** Training cell cd07: `if ckpt:\n rf_final = None\n rf_final_metrics = ckpt['metrics']` — no retraining, metrics come from disk. Save cell ckpt_a_rf, in order: `save_checkpoint('exp_a_rf', {'metrics': rf_final_metrics, 'best_params': rf_gs_best, ...})` `if locals().get('rf_final') is not None: ...` `elif model_artifact_is_current(_dest): print(f'[SKIP] rf_numeric.joblib already on disk and current.')` `else: ... print('[RECOVER] Re-finalizing Exp A / RF from checkpoint best_params...')` ` rf_final, _ = finalize_ml_model('rf', _ckpt['best_params'], X_train, y_train, X_test, y_test, random_state=42)` ` joblib.dump(rf_final, _dest); record_model_artifact(_dest)` The discarded second return value is the freshly computed metrics. All 20 save cells follow this shape (verified: 20 cells, each with 2 model_artifact_is_current calls, 2 record_model_artifact calls, 2 dumps; the only remaining `_dest.exists()` occurrences are inside the explanatory comment). notebooks/en/05_*.ipynb cell cd04 loads these same files (`rf_num = joblib.load(MODEL_DIR / "rf_numeric.joblib")`, …).

**Sonucu:** On the first run after any edit to feature_engineering.py or tuning.py, every one of the 20 artifacts is silently refit on the new pipeline and overwritten, while results/checkpoints/*.json and every table and figure in notebook 04 keep reporting the pre-change numbers. Notebook 05 then simulates with the new models. This is the same 'model and metrics from different runs' pairing commit 4e4f5b5 identified, with the two halves swapped, and it now happens automatically rather than by accident.

**Düzeltme:** In the [RECOVER] branch, keep the refit's metrics and write them back — e.g. `rf_final, rf_final_metrics = finalize_ml_model(...)` followed by a `save_checkpoint('exp_a_rf', {'metrics': rf_final_metrics, 'best_params': _ckpt['best_params'], ...})` after the dump — or make the branch refuse to write an artifact whose metrics it is not also refreshing.

---

## 47. [MINOR] reproducibility — reproducibility

**Dosya:** `src/tuning.py:347-355 (save_checkpoint); notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb cells nbcm02/nbcz02/nbpm02; results/checkpoints/exp_b_constant_median.json`

**Ne:** save_checkpoint's provenance carry-forward (src/tuning.py:347-350) is gated on best_params equality, but best_params for the non-learned baselines is a single free-text human-language `estimator` string. The Turkish notebook (notebooks/tr/04_calisma_zamani_tahmin_modelleri.ipynb, cells nbcm02/nbcz02/nbpm02) writes the same three checkpoint names with translated labels, so a re-save of metrics that were LOADED from disk (cell nbcm01: `if ckpt: constant_median_metrics = ckpt['metrics']`, no recomputation) fails the equality test and the checkpoint is restamped with the current git commit, source hashes and data hash plus a fresh timestamp -- the exact failure mode the code comment claims to have removed. This did occur: at HEAD d14b8f6, results/checkpoints/exp_b_constant_median.json, exp_b_constant_zero.json and exp_b_profile_median.json changed with metrics and best_params byte-identical while provenance moved 55a571f->e4c1b7c, src/tuning.py 4cdece86->06da3e7a and data_sha256 c1897f83->3f3d6d19. Effect is limited to staleness metadata for those three rows: load_checkpoint still prints the git_commit mismatch, so the result is not fully silenced -- only the data_sha256 and src/tuning.py mismatch lines that all 25 sibling checkpoints from the same 09:23 run still emit are suppressed. No reported number, table or figure changes: the metrics are unchanged and match the exported Tables 2, 7 and 12 (MAE 5818.25, MdAE 534.00, RMSE 16494.97), and the hashed data file is not the modelling input (use_processed defaults to False and no notebook overrides it). Fix by comparing on something canonical (metrics plus the structured fields) or by making the estimator label language-invariant across the EN/TR notebooks.

**Kanıt:** src/tuning.py:347 `same_result = (previous.get("metrics") == clean.get("metrics") and previous.get("best_params") == clean.get("best_params"))`. `git diff results/checkpoints/exp_b_constant_median.json` shows metrics BYTE-IDENTICAL while `"estimator": "single global training median, no grouping"` -> `"gruplamasız tek global eğitim medyanı"` and, in the same diff, `git_commit 55a571f -> e4c1b7c`, `src/tuning.py 4cdece86 -> 06da3e7a`, `data_sha256 c1897f83 -> 3f3d6d19`. I reproduced it in isolation (patched _CHECKPOINT_DIR): re-saving identical metrics with the EN label kept the stale provenance ('OLD_TUNING_HASH'/'OLD_DATA_HASH'), re-saving them with the TR label wrote 06da3e7a/3f3d6d19 and a new timestamp; `load_checkpoint` then printed NO mismatch. Running `./venv/bin/python -c 'load_checkpoint("exp_b_constant_median")'` today prints no warning at all, while `load_checkpoint("exp_d_lstm")` prints three.

**Sonucu:** Three published baseline results (exp_b_constant_median, exp_b_constant_zero, exp_b_profile_median -- the ConstantMedian / ConstantZero / ProfileMedian rows of the Experiment B and master summary tables) now assert they were produced under the current commit, current tuning.py and current data file. They were not: their numbers are unchanged from the 09:23 run under git 55a571f / tuning.py 4cdece86 / data c1897f83. The one mechanism that would flag them as stale has been silenced for exactly those rows.

**Düzeltme:** Base the 'nothing was recomputed' test on the numeric result only (compare `metrics` alone, or a normalised subset of best_params that excludes free-text description fields), and never restamp provenance on a write whose metrics are unchanged. Better still, make record-of-computation explicit: have the caller pass `recomputed=True/False` rather than inferring it from payload equality.

---

## 48. [MINOR] reproducibility — test-gap

**Dosya:** `tests/test_tuning.py`

**Ne:** The provenance machinery in src/tuning.py (_compute_provenance, the save_checkpoint carry-forward branch at lines ~327-357, model_artifact_is_current at line 366, record_model_artifact at line 399) has no test anywhere in tests/, despite commit 4e4f5b5's message explicitly claiming "Tests cover both: sidecar absent/present/stale, and provenance carried forward on an unchanged re-save" — that commit's diffstat touches no test file, and no commit after it touches tests/. This also breaks the convention stated in tests/test_regression_guards.py's own docstring, which five earlier result-corrupting defects each got a guard for. The machinery does behave correctly as of HEAD (verified by execution: carry-forward preserves provenance and timestamp; currency returns False for missing file, missing sidecar and stale src_sha256, True for a fresh sidecar), so no reported number is wrong today — the defect is that the sole guard against the Table 2 / Table 12 / notebook-05 stale-model pairing is unverified and its commit record wrongly says otherwise, so a future edit can silently remove it. The claim's assertion that a round-trip test "would have caught" the src_sha256-only currency scope is incorrect: that scope is stated deliberately in the docstring and is not something a round-trip test detects.

**Kanıt:** grep over tests/*.py for 'provenance|model_artifact_is_current|record_model_artifact' returns nothing. The only save_checkpoint test is test_save_checkpoint_enriches_required_metadata (tests/test_tuning.py:283), which asserts experiment/model/feature_mode/train_size/test_size/status/timestamp and never touches the 'provenance' key or the re-save path. tests/test_regression_guards.py covers seeding-before-construction but not artifact currency.

**Sonucu:** Both defeats found above (the best_params-label restamp and the src_sha256-only currency check) would have been caught by a two-line round-trip test, and there is nothing to stop the next edit from silently removing the guard again.

**Düzeltme:** Add tests that (a) round-trip a checkpoint whose metrics are unchanged but whose best_params description differs, asserting the stored provenance is unchanged; (b) write a sidecar, mutate a provenance field other than src_sha256, and assert model_artifact_is_current reflects the intended policy; (c) assert _PROVENANCE_SRC_FILES covers every module the training path imports.

---

## 49. [MINOR] simulator — correctness

**Dosya:** `src/simulation/multi_node_simulator.py:566-598; notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (cell cd15, _time_weighted_avg)`

**Ne:** The run loop never records a utilization snapshot at the makespan (snapshot precedes the heappop; the `while events or not pending_df.empty` guard then exits), and `_time_weighted_avg`'s left Riemann sum drops both the final drain interval and the last snapshot's value, biasing time-weighted GPU/CPU utilization upward. Contrary to the claim, this does NOT affect cell stats1cell (which computes no utilization diagnostic at all) and does NOT change the headline cd17 table at its printed precision (0.6938 -> 0.6936 both round to 0.694). The one materially affected output is the BACKFILL DIAGNOSTICS table in cell e6b8837b (32-GPU) and a667fa61 (256-GPU), which prints Backfill=True rows only -- exactly the runs with a long drain tail (0.18%-1.05% of makespan) -- where Time-Weighted GPU Util is overstated by up to 0.009 absolute (0.933 vs 0.924 at load factor 0.05) and CPU util by up to 0.005. Fix: append a final snapshot at the makespan after the loop, or terminate the Riemann sum at max(completion_time).

**Kanıt:** In run(), the snapshot block `self.utilization_history.append({"time": self.time, ...})` is followed by `if not events: break` and only then `event = heapq.heappop(events); self.time = event.time`, so the loop exits after the last event without a further snapshot. Executed, 16,437-job FIFO run on the 32-GPU cluster: `time range 0.0 3276092.8` but `makespan (max completion) 3276882.8` (790 s uncovered, 0.02%); my reproduced time-weighted gpu_util 0.6938 matches the notebook's reported 0.694 exactly. On a 4,000-job subset the uncovered tail is larger: last_snap=692061.8 vs makespan=708612.8 (16,551 s, 2.3% of the makespan) for backfill=False and 592203.3 vs 607919.7 (2.6%) for backfill=True.

**Sonucu:** The 'Time-Weighted GPU Util' / 'Time-Weighted CPU Util' diagnostics are systematically slightly optimistic, by 0.02% of the makespan on the headline full-trace runs but by 2-3% on shorter replays such as the 10 sub-window replays in cell stats1cell.

**Düzeltme:** Append one final snapshot after the event loop exits (`self.utilization_history.append({"time": self.time, "cpu_util": ..., "gpu_util": ..., "pending_jobs": 0})`), so the drain tail carries its true weight.

---

## 50. [MINOR] tests — test-gap

**Dosya:** `tests/test_tuning.py`

**Ne:** TestFinalizeDlModelSaveAllSeeds (tests/test_tuning.py:190-229) does not test the regression named in its own docstring. It asserts only that three files exist plus metrics["n_seeds"]==3, never that the three checkpoints differ, so mutating src/tuning.py:1979 to write seed 0's network to all three paths leaves the suite green (verified: 14 passed). The gap is latent rather than live: the downstream consumer it would protect -- the robustness-4 multi-seed cell in notebooks/{en,tr}/05_* -- has never executed (execution_count=None, no outputs in all four files) and cannot find its inputs as written, because notebook 04 saves by seed VALUE (lstm_categorical_pt_seed42/1337/2024.pth, DL_SEEDS=[42,1337,2024]) while notebook 05 reads by seed INDEX (seed0/1/2). No thesis number is currently wrong. The gap is also slightly broader than reported: the test uses seeds=[0,1,2], where seed value coincidentally equals index, and that coincidence is exactly what prevents it from pinning the real filename contract and exposing the 04-to-05 mismatch. Fix: assert pairwise-distinct state_dicts across the saved files and use non-index seeds (e.g. [42,1337]).

**Kanıt:** tests/test_tuning.py:190-229; the only per-seed assertion is: ``` for seed in (0, 1, 2): self.assertTrue( Path(path_template.format(seed=seed)).exists(), f"seed {seed}'s model was not saved to disk", ) ``` Mutation executed: src/tuning.py:1979 changed from `torch.save(model, save_all_seeds_to.format(seed=seed))` to `torch.save(first_model, save_all_seeds_to.format(seed=seed))` (every path gets seed 0's network) and ran `venv/bin/python -m pytest -q tests/test_tuning.py` -> `14 passed in 3.76s`. File restored.

**Sonucu:** Notebook 05 loads lstm_categorical_pt_seed{s}.pth and lstm_categorical_seq_seed{s}.pth to report seed variability of the scheduling results. If those three files ever hold the same network, the reported seed spread collapses to zero and the suite still passes — a fabricated robustness claim with a green test named after it.

**Düzeltme:** Assert the artefacts differ: load each saved model back with `torch.load` and assert that at least one parameter tensor differs between seeds, e.g. `w = [next(torch.load(p).parameters()).detach() for p in paths]; self.assertFalse(torch.equal(w[0], w[1]))`, and assert `torch.equal(w[0], next(model.parameters()).detach())` so the returned model is provably seed 0's. Name it `test_each_seed_path_holds_a_different_network`.

---

## 51. [MINOR] training-and-metrics — consistency

**Dosya:** `notebooks/en/04_runtime_prediction_models.ipynb (cells rob7cell, l5r9cell01); src/tuning.py:1021`

**Ne:** Notebook 04 applies the project's own non-negative clipping policy (src/tuning.py:1021, code_bugs-5) inconsistently across its two diagnostic cells, so one model's MAE is printed with two different values. rob7cell clips the LightGBM-Native row and prints MAE 4857.24; l5r9cell01 clips nothing and prints 4857.41 for the same model on the same test set. Verified by re-running the on-disk artifacts: lgbm_categorical_native emits 10 negative predictions (min -624.3), 4857.41 unclipped vs 4857.24 clipped; xgb_categorical_native emits 381 negatives (min -4815.5), 5930.37 unclipped (matching the shipped output) vs 5920.48 clipped. Three corrections to the claim as filed: 1. The RF / XGB / LGBM-numeric rows in rob7cell being unclipped has NO numeric effect — all three models emit zero negative predictions on this test set (min preds 2798.2, 601.0, 554.6), so clipping them is a strict no-op and those rows are identical either way. The only rows where clipping matters are the two native-categorical models. 2. The claim's XGB numbers are slightly wrong: actual values are 5930.37 unclipped / 5920.48 clipped, not 5929.68 / 5919.79. 3. The claimed consequence that "the unseen-user / burst-deduplication analysis is computed on a prediction vector with physically impossible negative runtimes" overstates the impact: the unseen-user subset contains zero negative predictions for both models, so the unseen-user MAEs (LGBM 2762.60, XGB 6495.66) — the actual research finding of that cell — are bit-identical clipped or unclipped. Only the "all" and "seen users" columns move, by 0.17 s (LGBM) and 9.89 s (XGB). Burst-dedup moves 5814.37 -> 5802.90 for XGB and 4656.97 -> 4656.77 for LGBM, preserving direction and conclusion. Also worth separating from this finding: the headline tables (cd19 / cd31) report LGBM-native = 4973.16 and XGB-native = 6600.07, which are 116 s and 670 s away from these diagnostic cells. That much larger spread comes from checkpoint metrics vs. re-predicting the on-disk artifact, not from clipping — clipping explains only 0.17 s of the 116 s LGBM gap. Anyone fixing the clipping inconsistency should not expect it to reconcile the headline table.

**Kanıt:** rob7cell: `"LightGBM (Native Categorical)"` is added as `(y_test_nat, np.maximum(lgb_nat_final.predict(X_test_nat), 0))` while the three numeric rows are `(y_test, rf_final.predict(X_test))` etc. l5r9cell01: `_subset_models = {"LightGBM (Native Categorical)": (y_test_nat, lgb_nat_final.predict(X_test_nat)), "XGBoost (Native Categorical)": (y_test_nat, xgb_nat_final.predict(X_test_nat)), …}`. The two shipped outputs report the same model on the same test set as 'MAE, all (s)' = 4857.24 (rob7cell) and 4857.41 (l5r9cell01). Executed on the on-disk artifacts: lgbm_categorical_native.joblib emits 10 negative predictions (MAE 4857.41 unclipped vs 4857.24 clipped); xgb_categorical_native.joblib emits 381 negative predictions (MAE 5929.68 unclipped vs 5919.79 clipped).

**Sonucu:** The same model's MAE appears with two different values in two tables of the same notebook, and the unseen-user / burst-deduplication analysis is computed on a prediction vector (with physically impossible negative runtimes) that neither the headline metrics nor the scheduler ever use.

**Düzeltme:** Clip once, centrally: build the prediction arrays with a single helper (`_pred = lambda m, X: np.maximum(m.predict(X), 0)`) and use it in rob7cell, l5r9cell01 and anywhere else a saved model is re-scored, so all tables and the simulator share one definition of a model's output.

---
