# Şekil/Tablo Denetimi — Doğrulanmış Bulgular (2026-09-06)

217 ajanlı iş akışı: **193 ham bulgu → 110'u karşı-doğrulamada çürütüldü → 83'ü doğrulandı.**
Her bulgu, onu bulan ajandan bağımsız bir ajan tarafından *çürütülmeye çalışılarak* sınandı;
yalnız çürütülemeyenler burada.

Ağırlık dağılımı: blocker 24, major 45, minor 13, nit 1

> Not: Denetim, bu oturumdaki tablo düzeltmelerinden ÖNCEKİ export üzerinde koştu.
> Model adları, birimler, denetim ID'leri ve sızan indeks sütunu ile ilgili birkaç bulgu
> zaten kapandı; export yenilenince listeden düşecekler.

## BLOCKER (24)

### `NB01-Figure05.png` — data-integrity

**Ne:** The caption is stale relative to the re-binned (log-spaced) figure, but the "off by roughly 2x" framing needs adjusting. Ground truth from data/processed/100k_job_with_utilization.csv (82,184 jobs; 21.6% same-second pairs excluded, 64,438 positive gaps): the 1-3 s INTERVAL really does contain 24,572 jobs, so the caption's ~25,000 is a correct interval total that has simply been mislabelled as "First bin" - the figure's first bin is 1.00-1.15 s (gaps of exactly 1 s) and holds 12,110, and the y-axis tops out at 12000, so nothing in the plot can read 25,000. The bullet that is genuinely numerically wrong is "3-10 s: ~8,000 jobs": the true count is 25,697 for 3-10 s inclusive (18,105 for 4-9 s), i.e. the caption UNDERSTATES by 2.2-3.2x rather than overstating. "10-100 s: approximately 1,000-4,000" is also wrong at the right end (bars fall to 82-539 above ~58 s; the interval total is 21,609). Fix: describe the actual log-binned figure - tallest bar = 1 s gaps at ~12,100; 1-3 s aggregate ~24,600; 3-10 s ~25,700; 10-100 s ~21,600 with bars decaying from ~3,200 to under 100; >100 s = 162 jobs - and drop or re-word the two "1-3 s" peak assertions that cell 23's own comment identifies as a binning artifact.

**Kanıt:** Caption cell 24: "- First bin (1–3 s): **~25,000 jobs** — highest frequency" and "- 3–10 s: ~8,000 jobs (significant drop)". In the PNG the y gridlines are at 0 (row 845) and 12000 (row 110); the tallest bar's top edge is at row 105, i.e. ~12,100 — visibly ABOVE the 12000 line, not 25,000 (the y axis does not even reach 25,000; it stops at 12000). That bar occupies x from the 10^0 gridline (col 266) to col 307, i.e. 1.00–1.15 s, not 1–3 s. Between 3 s and 10 s the six bars measure ~4,200 / ~3,610 / ~2,970 / ~2,630 / ~2,370 / ~2,190 — none is ~8,000. The caption's "10–100 s: approximately 1,000–4,000" also fails at the right end, where the last bars are ~150–500. The code comment in cell 23 admits the old linear bins made "'peak occurs at 1-3s' an artifact of bin width rather than the data", yet the caption still asserts "1–3 s" twice, including "Burst periods where many jobs arrive 1–3 seconds apart".

**Düzeltme:** Rewrite the cell-24 caption against the current log-binned figure: first bin 1.00–1.15 s with ~12.1k pairs (exactly the 1-second gaps), a monotone decay to ~2k by 10 s, and a tail out past 100 s. Better, compute the numbers instead of hard-coding them, e.g. `_c, _e = np.histogram(inter_arrivals, bins=_log_bins)` and print `_e[0], _e[1], _c[0]` / `_c.max()` so the caption text is generated from the same array that is plotted.

---

### `NB02-Figure03.png` — data-integrity

**Ne:** The defect is real and the quoted evidence is exact, but its scope is understated: it is not confined to the notebook markdown. The identical figure (md5 659dab0dcd5d9e8c31f5d8de7993db7f) is what the thesis LaTeX includes, and /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/thesis/latex/chapters/3.dataset_and_workload.tex carries the stale N twice — body prose line 72 ("using 5,000 randomly selected samples, with both axes on a log scale") and the figure caption line 77 ("Scatter plot of GPU demand versus job runtime (5,000-job sample, log scale)"). So the contradiction appears on the printed page itself, with "82,184 jobs" inside the figure and "5,000-job sample" in the caption below it. Two dependent errors ride along: (a) the caption's claim that "6 and 8 GPU requests appear merely as rare singular outliers" (TR: "sadece 1-2 nadir olay"), and tex line 72's "Higher GPU requests (6-8 GPUs) are rare but appear close to the 10^4 s mark", describe the discarded 5,000-job sample — the code comment records that only 7 of the 82 eight-GPU jobs survived that sample, so the full-trace figure shows materially more; (b) the exported HTML reports are stale, with reports/html/en/02_workload_analysis.html line 8089 and reports/html/tr/02_is_yuku_analizi.html line 8088 still rendering the pre-fix source line ax.set_title("GPU Demand vs Job Runtime (5,000-job sample)"). Fix: change the four "5,000" references (EN cell 15, TR cell 15, tex:72, tex:77) to the full 82,184-job trace, revise the 6-8 GPU frequency wording to match the full data, and re-export the HTML reports.

**Kanıt:** In the PNG, top-left legend box: "82,184 jobs" (single blue swatch). Notebook /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/notebooks/en/02_workload_analysis.ipynb markdown cell 15: "**Figure 3 — GPU Demand vs. Runtime Relationship (5,000-Job Sample Baseline):**" and "Evaluating the logarithmic scatter dispersion of an unbiased 5,000-job sample". Same error in the TR twin, notebooks/tr/02_is_yuku_analizi.ipynb cell 15: "(GPU Demand vs. Runtime Scatter - 5.000 İşlik Örneklem)" and "Rastgele seçilmiş 5.000 işlik örneklemin". The code cell 14 that draws it sets `sample = job_df` and its own comment says "All jobs, not a 5,000-job sample"; cell 6 output confirms "[Step 1] Jobs : 82,184". The caption was never updated when the sample was dropped.

**Düzeltme:** In notebooks/en/02_workload_analysis.ipynb markdown cell 15, replace "(5,000-Job Sample Baseline)" with "(All 82,184 Jobs)" and "an unbiased 5,000-job sample" with "the complete 82,184-job trace". Mirror in notebooks/tr/02_is_yuku_analizi.ipynb cell 15: "5.000 İşlik Örneklem" -> "82.184 İşin Tamamı", "Rastgele seçilmiş 5.000 işlik örneklemin" -> "82.184 işin tamamının". Re-export the figure bundle.

---

### `NB04-Figure01.png` — resolution

**Ne:** NB04-Figure01 (thesis/latex/figures/nb04-fig01-model-comparison.png, 3586x1768 px @ 200 dpi = 17.93 x 8.84 in from figsize=(18, 8) in cell 93) is included at width=\textwidth in chapters/6.results_and_discussion.tex:52, into a 6.10-in column (thesis.cls: a4paper, left 3.5 cm, right 2 cm), a scale factor of 0.340. Three corrections to the original claim: (1) the tick labels are 11 pt, not "matplotlib's default 10 pt" -- the notebook explicitly sets xtick.labelsize/ytick.labelsize to 11 in rcParams, and the measured 22-23 px ink at 200 dpi corresponds to 11 pt; the printed size is therefore ~3.7 pt, not 3.4 pt (the claim's "roughly 3.5-4 pt" headline range is right). (2) The figure now has 27 rows per panel, not 24 -- it was regenerated during this audit (12:23) with three added "Alibaba Estimate (SJU/SJG/SJGG)" baselines -- so the duplicated model-name column is 54 y-tick strings, not 48. (3) The "~1.6 pt at 0.48\textwidth" figure is hypothetical and does not apply: this figure is placed at full \textwidth (0.48\textwidth is used only for the paired NB05 figures). The defect itself stands: at the actual placement every string prints below the ~6 pt legibility floor -- model names ~3.7 pt, axis labels ~4.1 pt, panel titles ~4.8 pt, and even the 16 pt suptitle "Comparative Performance Analysis: All Models & Features" only 5.4 pt -- and the same 27 model-name strings are typeset twice because both panels carry a full y-tick label column.

**Kanıt:** The PNG is 3586 x 1768 px at 200 dpi = 17.93 x 8.84 in (figsize=(18, 8) in the source cell). Measured ink height of the y-tick label "Profile Median" is 23 px and of the x-tick "1000" is 22 px, i.e. matplotlib's default 10 pt. Placed at 1.0\textwidth in a 6.1-in column the scale factor is 6.1/17.93 = 0.34, so 23 px of cap height becomes 0.039 in = 2.8 pt cap height (~3.4 pt type); at 0.48\textwidth it is ~1.6 pt. The largest text on the page, the 16 pt suptitle "Comparative Performance Analysis: All Models & Features", shrinks to 5.4 pt. The waste is structural: both panels print the full 24-row model-name column, so the same 24 strings are typeset twice.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 93, change figsize=(18, 8) to a print-native size such as figsize=(7.0, 8.5) with the two panels stacked (plt.subplots(2, 1)) so one shared y-tick column serves both, or keep 1x2 but drop the right panel's duplicate tick labels via axes[1].sharey(axes[0]). Then set explicit sizes at that scale: plt.rcParams.update({'font.size': 8, 'xtick.labelsize': 7, 'ytick.labelsize': 7}), title fontsize 9, xlabel fontsize 8, suptitle fontsize 10. Verify the exported PNG has a tick-label cap height of at least 6 pt at final placement width.

---

### `NB04-Figure03.png` — resolution

**Ne:** The defect is real but its lower bound is understated. Text in NB04-Figure03.png prints between about 2.7 pt and 4.8 pt, not 3.3-4.8 pt. The 17.84 in x 5.90 in canvas (3568x1180 px at 200 dpi) is placed with \includegraphics[width=\textwidth] at 6.results_and_discussion.tex:98 into a 15.5 cm = 6.1024 in text column (thesis.cls:97 branch, selected by \documentclass[msc] in main.tex:5), a 0.3421x reduction. Nominal -> printed: axes.titlesize/suptitle 14 -> 4.79 pt, axes.labelsize 12 -> 4.10 pt, x/y tick labels 11 -> 3.76 pt, and the legend "Perfect prediction" -- set by an explicit ax.legend(fontsize=8) in notebook cell 97 that overrides the rcParams legend.fontsize of 11 -- 8 -> 2.74 pt (measured glyph band rows 173-192 = 20 px = 2.46 pt). Measured bands otherwise match the claim (suptitle 20-49 = 30 px = 3.69 pt; x tick 10^1 1081-1107 = 27 px = 3.32 pt; x-axis title 1127-1159 = 33 px = 4.06 pt), except the panel title band is 106-140 = 35 px = 4.31 pt rather than 34 px = 4.19 pt.

**Kanıt:** File is 3568x1180 px at 200 dpi = 17.84 in x 5.90 in (PIL: size (3568,1180), dpi 199.9996). It is placed with \includegraphics[width=\textwidth] (thesis/latex/chapters/6.results_and_discussion.tex:98) into thesis.cls geometry a4paper, left=3.5cm, right=2cm -> textwidth 15.5 cm = 6.10 in, so scale = 6.10/17.84 = 0.342. Measured glyph bands in the PNG (1 px = 0.1231 printed pt): suptitle 'Predicted vs Actual Runtime — Tree-Based Models' rows 20-49 = 30 px -> 3.69 pt; panel title 'Random Forest (Numeric)' rows 106-139 = 34 px -> 4.19 pt; x tick label '10^1' rows 1081-1107 = 27 px -> 3.32 pt; x-axis title 'Actual Runtime (s, log scale)' rows 1127-1159 = 33 px -> 4.06 pt. Nominal sizes come from cell 5 rcParams (xtick.labelsize 11, ytick.labelsize 11, axes.labelsize 12, axes.titlesize 14) whose own comment claims they 'stay legible after the figure is scaled down into a 6.1-inch text column' — 11 x 0.342 = 3.8 pt, 14 x 0.342 = 4.8 pt.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell index 97, change `fig, axes = plt.subplots(1, 3, figsize=(18, 6))` to `figsize=(9.2, 3.2)` (1.5x the 6.10-in column). With the existing rcParams this prints ticks at 7.3 pt, axis titles at 8.0 pt and panel titles at 9.3 pt. Marker size s=6 should be raised to s=10 to compensate for the smaller axes. Re-export and re-copy to thesis/latex/figures/nb04-fig03-pred-vs-actual.png.

---

### `NB04-Figure05.png` — data-integrity

**Ne:** The contradiction is real and blocking, but the figure is the CORRECT side and the LaTeX is the stale side, and the staleness is wider than the 12 DL cells. The notebook's own exported outputs match the figure to the rounding: results/figures/thesis_export/html/NB04_Table08.html (Exp C) = LSTM 6688.74, CNN 7040.17, CNN-LSTM 7230.19; NB04_Table09.html (Exp D) = LSTM 6316.79, CNN 6524.73, CNN-LSTM 6929.09; NB04_Table10.html (Exp E) = CNN 6703.64, CNN-LSTM 6716.67, LSTM 6844.75; NB04_Table11.html (Exp F) = LSTM 6137.18, CNN-LSTM 6387.03, CNN 6464.84. The consolidated NB04_Table12.html repeats these and additionally refutes the Experiment A and B rows of Table 6.1: it gives LightGBM (Numeric) 5811.00, Random Forest (Numeric) 6796.98, XGBoost (Numeric) 6968.35 against the table's 4,316 / 4,808 / 5,212, and XGBoost (One-Hot) 6053.96 with R^2 = 0.18 against the thesis headline claim of "MAE of 3{,}389\,s ... R^2 of 0.51" (line 65 and the Traditional ML subsection). So all 18 rows of Table 6.1, the three DL appendix tables (tab:expc-full lines 94-96, tab:expd-full line 114, tab:expe-full lines 134-136, tab:expf-full lines 154-156), and the surrounding prose are from an earlier run and must be regenerated from the current notebook outputs - the figure should not be changed to match them. Note also that the notebook's own markdown commentary cell immediately below the figure in notebooks/en/04_runtime_prediction_models.ipynb is stale in the same way (it still says "Test MAE of 5836.32" and "CNN (Experiment F - MAE 6704.19)"), which is the likely source of the stale LaTeX. One further nuance a fix must handle: the figure plots the multi-seed mean MAE with std error bars, while NB04_Table13.html also carries a "saved seed" column (e.g. C-CNN mean 7040.17, std 268.47, saved seed 6733.31) - the network actually used by notebook 05's scheduling simulation is the seed0 one, so the regenerated table should state which of the two it reports.

**Kanıt:** Figure bar labels, left to right: 7040s, 6689s, 7230s (blue C) | 6525s, 6317s, 6929s (orange D) | 6704s, 6845s, 6717s (green E) | 6465s, 6137s, 6387s (red F). Thesis/latex/chapters/6.results_and_discussion.tex lines 29-42 give, for the same 12 cells: C CNN 6{,}454; C CNN-LSTM Hybrid 7{,}020; C LSTM 8{,}184; D LSTM 5{,}836; D CNN-LSTM Hybrid 6{,}618; D CNN 6{,}866; E LSTM 6{,}923; E CNN 8{,}512; E CNN-LSTM 8{,}907; F CNN 6{,}704; F CNN-LSTM 10{,}212; F LSTM 13{,}169. Not one matches. The prose at line 68 says "In its best configuration (Experiment D, One-Hot), the LSTM achieved R\textsuperscript{2} = 0.06 and an MAE of 5{,}836\,s" while the figure labels that exact bar "6317s"; line 66 says the addition of sequencing "resulted in a significant increase in the MAE, especially for the LSTM architecture", but in the figure the F-LSTM bar (6137s) is the shortest of all twelve. Same contradiction in appendices.tex line 114 (LSTM 5{,}836). The image is byte-identical to the shipped thesis asset (md5 7e2a9c03cf3850593b693778351a871e for both results/figures/thesis_export/png/NB04-Figure05.png and thesis/latex/figures/nb04-fig05-dl-comparison.png).

**Düzeltme:** The figure and the table come from different pipeline runs. Regenerate both from one run of notebooks/en/04_runtime_prediction_models.ipynb (the cell at line 9445, "# ── 36. Deep Learning Model Comparison (Experiments C-F)"), then rewrite the tab:predresults C-F rows, the sentence at 6.results_and_discussion.tex:68 ("an MAE of 5{,}836\,s"), the LSTM claim at line 66, and appendices.tex lines 114/144 against the regenerated numbers. Do not ship until the twelve bar labels and the twelve table cells match digit for digit.

---

### `NB04_Table01.html NB04_Table02.html NB04_Table03.html NB04_Table04.html` — data-integrity

**Ne:** Real defect, but the description understates its scope and slightly overstates the ranking inversion. SCOPE (larger than claimed): the stale figures are not confined to a notebook caption — they are the numbers actually printed in the thesis. /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/thesis/latex/chapters/6.results_and_discussion.tex line 22 reads "A & Random Forest (Numeric) & 4{,}316 & 1{,}508 & 13{,}831 & 16.85 & 0.27" inside Table tab:predresults, with lines 23-24 giving XGBoost 4,808 / R2 0.26 and LightGBM 5,212 / R2 0.22, and line 59 repeating "Random Forest, R^2 = 0.27". So the printed thesis reports Experiment A metrics that contradict the project's own current results artefacts by roughly 1.6x on MAE (4,316 vs 6,796.98), 13x on R2 (0.27 vs 0.02) and 143x on MAPE (16.85 vs 2412.01). NB04_Table02.html independently corroborates the current run (RF MAE-all 6892.51, LightGBM 5810.97, XGBoost 6976.65). The notebook caption is the symptom; the LaTeX results table is the blocker. RANKING (needs nuance): "inverts the ranking" is not uniformly true. In the table Random Forest does genuinely hold the best RMSE (15374.96) and the best R2 (0.02), so the caption's directional claims of "lowest Test RMSE" and "highest R2 value" for RF survive — only their numbers are wrong. What is actually inverted is MAE, MdAE and MAPE: LightGBM is best on all three (5811.00 / 536.92 / 332.53), so "lowest Test MAE" and "lowest MAPE" for RF are false, and "LightGBM exhibited the lowest performance, producing the highest error values" is contradicted by the table's own ascending-MAE sort, which places LightGBM in row 1. MAGNITUDE (minor imprecision): 16.85 vs the table's smallest MAPE of 332.53 is about 1.3 orders of magnitude, not "roughly two"; it is over two orders against Random Forest's own MAPE of 2412.01. SEPARATE OBSERVATION worth flagging to the author: the current Experiment A checkpoints look like a degraded run rather than a full one — exp_a_lgbm.json has n_estimators_effective 1 and train_time 0.29 s, exp_a_xgb.json n_estimators_effective 7 and train_time 0.22 s, exp_a_rf.json train_time 1.90 s. Which of the two number sets is scientifically correct is therefore undetermined; the reader-facing defect — that the caption, the LaTeX table and the exported table disagree — holds either way and must be resolved by a rerun, not by editing prose.

**Kanıt:** NB04_Table01.html body rows, in file order: "LightGBM (Numeric) | 5811.00 | 536.92 | 16486.03 | 332.53 | -0.12"; "Random Forest (Numeric) | 6796.98 | 3387.89 | 15374.96 | 2412.01 | 0.02"; "XGBoost (Numeric) | 6968.35 | 4341.02 | 15458.66 | 2804.80 | 0.01". The caption in /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/notebooks/en/04_runtime_prediction_models.ipynb cell 17 reads "> **Table 1 — Experiment A – Model Performance Evaluation**" ... "lowest **Test MAE (4316.39)** and **Test RMSE (13830.74)**, while also obtaining the highest **R² value (0.27)**" ... "**lowest MAPE at 16.85%**" ... "**1508.10 seconds** for Random Forest" ... "**XGBoost** ... slightly lower **R² score (0.26)**. **LightGBM** ... (**R² = 0.22**)". No number in the caption (4316.39, 13830.74, 0.27, 16.85, 1508.10, 0.26, 0.22) occurs anywhere in the table, and the caption's MAPE of 16.85% is off by roughly two orders of magnitude from the table's smallest MAPE, 332.53. The table is also sorted ascending by "Test MAE (s)", which ranks LightGBM first — the model the caption calls worst.

**Düzeltme:** Regenerate the cell-17 markdown caption from tuning_progress_numeric instead of leaving hand-typed numbers: emit the caption from code (f-strings reading rf_final_metrics/xgb_final_metrics/lgb_final_metrics) so it cannot drift from the displayed DataFrame, and re-write the narrative ranking to match the actual sort (LightGBM lowest MAE, Random Forest highest R²).

---

### `NB04_Table01.html NB04_Table02.html NB04_Table03.html NB04_Table04.html` — data-integrity

**Ne:** The inconsistency is real, but prediction clipping is NOT the cause of it (except for one negligible 0.05 s pair). src/tuning.py line 963 (not 962) does clip, `y_pred = np.maximum(final_model.predict(X_test), 0)`, and cells 40/43 do use raw `predict`, but on this test set Random Forest, XGBoost (Numeric), and XGBoost (Native Categorical) emit ZERO negative predictions, so clipping is a provable no-op for them: I measured raw MAE == clipped MAE to four decimals for all three. Clipping explains only the LightGBM-Native Table02 (4962.32, clipped, 5 negatives) vs Table04 (4962.37, raw) gap of 0.05 s. The actual cause of every material discrepancy is that the four tables are computed from three mutually inconsistent artefact states within one export: (1) Tables 01 and 07 print metrics read back from stale JSON checkpoints in results/checkpoints/ (cell 16 does `_m('exp_a_rf')`; exp_a_rf.json = 6796.98, exp_b_lgbm_nat.json = 4973.16, exp_b_xgb_nat.json = 6600.07); (2) Tables 02 and 04 recompute from the .joblib models in results/models/, which no longer reproduce those checkpoint metrics (rf_numeric.joblib -> 6892.51, lgbm_categorical_native.joblib -> 4962.37, xgb_categorical_native.joblib -> 6572.33); (3) Table 03's "Full feature set (reference)" row is a fresh retrain performed at notebook-run time, which today yields 4857.24 with 95 effective trees, whereas the saved model carries 63 trees and the checkpoint records 63. So the checkpoint metrics, the persisted model weights, and a fresh refit of the same configuration all disagree, and all three are printed as the same quantity. Fixing this requires invalidating results/checkpoints/*.json and re-running Experiments A and B so checkpoints, saved models, and the ablation reference come from one run -- not changing the clipping.

**Kanıt:** NB04_Table01.html "Test MAE (s)": "Random Forest (Numeric) | 6796.98", "XGBoost (Numeric) | 6968.35", "LightGBM (Numeric) | 5811.00". NB04_Table02.html "MAE, all (s)" for the same three rows: "Random Forest (Numeric) | ... | 6892.51", "XGBoost (Numeric) | ... | 6976.65", "LightGBM (Numeric) | ... | 5810.97" — deltas of 95.53 s, 8.30 s and -0.03 s. NB04_Table02.html "LightGBM (Native Categorical) | ... | 4962.32" vs NB04_Table04.html "LightGBM (Native Categorical) | 4962.37" vs NB04_Table03.html "Full feature set (reference) | (none) | 4857.24" (cell 41 states these rows "reuse the SAME tuned LightGBM-Native hyperparameters found for Experiment B (no re-search) and the same train/test split already loaded", so the reference row must reproduce Experiment B) vs the sibling export NB04_Table07.html "LightGBM (Native Categorical) | 4973.16". NB04_Table04.html "XGBoost (Native Categorical) | 6572.33" vs NB04_Table07.html "XGBoost (Native Categorical) | 6600.07". The baseline rows do agree exactly across tables ("Per-User Median" = 5191.50 in Tables 02, 04 and 07; "Constant Median" = 5818.25; "Profile Median" = 4389.67), which isolates the disagreement to the model rows. Cause is visible in source: /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/src/tuning.py line 962 computes the headline metrics from clipped predictions, `y_pred = np.maximum(final_model.predict(X_test), 0)`, whereas notebook cell 40 uses raw `rf_final.predict(X_test)` / `xgb_final.predict(X_test)` / `lgb_final.predict(X_test)` and cell 43 uses raw `lgb_nat_final.predict(X_test_nat)` / `xgb_nat_final.predict(X_test_nat)`.

**Düzeltme:** Clip once, centrally: in notebook cells 40 and 43 wrap every entry of `_tail_models` and `_subset_models` in `np.maximum(..., 0)` exactly as the LightGBM-Native entry in cell 40 already does, then re-run and confirm Table02/Table04 "MAE, all (s)" reproduces Table01/Table07 "Test MAE (s)" to the printed 2 decimals. Separately, re-run cell 41 (or reuse lgb_nat_final_metrics directly) so the "Full feature set (reference)" row equals the Experiment B LightGBM (Native Categorical) row rather than 4857.24 vs 4973.16; if the residual gap survives clipping, the checkpointed metrics and the on-disk .joblib are from different runs and the checkpoints must be invalidated and regenerated.

---

### `NB04_Table05.html NB04_Table06.html NB04_Table07.html NB04_Table08.html` — data-integrity

**Ne:** The defect is real but understated in both scope and cause. Scope: the thesis prints THREE distinct values for the same quantity (LightGBM Native-Categorical test MAE) across seven exported tables, not a disagreeing pair in three places. NB04_Table02 ("MAE, all (s)") = 4962.32, the zero-clipped variant from cell 40; NB04_Table04 ("MAE, all (s)") and NB04_Table05 ("MAE per job (s)") = 4962.37, the unclipped recomputation from cell 43; NB04_Table07 and NB04_Table12 ("Test MAE (s)"), plus NB05_32GPU_Table01 and NB05_256GPU_Table01 ("Model MAE (s)"), = 4973.16. XGBoost Native-Categorical shows the same split: 6572.33 in Table04/Table05 versus 6600.07 in Table07, Table12 and both NB05 Table01s. Cause: the claim attributes the gap to "recomputed from disk vs. Experiment-B fit results," which is the right mechanism but not the root cause. Two effects are conflated. (1) A 0.05 s zero-clipping difference — src/tuning.py:963 clips predictions with np.maximum(..., 0) while cell 43 does not — which explains only 4962.32 vs 4962.37. (2) The remaining ~10.8 s (LightGBM) and ~27.7 s (XGBoost) gap is stale model artefacts: results/models/lgbm_categorical_native.joblib (mtime Sep 6 00:28) and xgb_categorical_native.joblib (Sep 6 00:29) are older than the checkpoints that supply Table07's numbers, results/checkpoints/exp_b_lgbm_nat.json and exp_b_xgb_nat.json (mtime Sep 6 12:24, internal timestamp 2026-09-06T09:23:30Z). A resumed kernel left lgb_nat_final/xgb_nat_final as None, so cell 43's joblib.load guard scored the superseded on-disk models while cell 47 reported the newer refit's checkpointed metrics. The persisted models therefore no longer correspond to the metrics the thesis reports for them — which also means notebook 05's scheduler simulation runs on models whose true MAE is not the MAE printed in its own results tables.

**Kanıt:** NB04_Table05.html, column header "MAE per job (s)": row "LightGBM (Native Categorical)" = 4962.37, row "XGBoost (Native Categorical)" = 6572.33, row "Per-User Median" = 5191.50. NB04_Table07.html, column header "Test MAE (s)": row "LightGBM (Native Categorical)" = 4973.16, row "XGBoost (Native Categorical)" = 6600.07, row "Per-User Median (baseline)" = 5191.50. Both columns are the mean absolute error over the same chronological test set. The deterministic baseline agrees to the cent (5191.50 = 5191.50), proving the two tables are describing the same test set and the same metric; only the two learned models disagree (+10.79 s / +0.22% and +27.74 s / +0.42%). Cause is visible in the notebook: cell 43 recomputes errors from models reloaded off disk (`lgb_nat_final = joblib.load(MODEL_DIR / "lgbm_categorical_native.joblib")`, then `"MAE per job (s)": float(_err.mean())` on `lgb_nat_final.predict(X_test_nat)`), while cell 47 reads the Experiment-B fit results (`"Test MAE (s)": lgb_nat_final_metrics["mae"]`). NB04_Table04.html, same cell 43, repeats the Table05 numbers under yet another header ("MAE, all (s)" = 4962.37 / 6572.33), so the thesis carries the disagreeing pair in three places.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 43, stop re-predicting from the reloaded joblib artefacts. Cache the Experiment-B test predictions at fit time (e.g. `lgb_nat_test_pred`, `xgb_nat_test_pred` alongside `lgb_nat_final_metrics`) and build `_subset_models` from those cached arrays so Tables 04/05 and Table07 share one prediction source. Add a guard immediately after building `_burst_rows`: `assert np.isclose(_err.mean(), lgb_nat_final_metrics['mae'], atol=0.01)` for each model, so the export fails loudly rather than shipping two headline MAEs. Re-export all four tables afterwards.

---

### `NB04_Table09.html NB04_Table10.html NB04_Table11.html` — data-integrity

**Ne:** The defect is real but broader than described: it is not limited to line 68 and the notebook caption, and the reversal is not merely a caption artefact. The thesis reports the entire Experiment D block from a superseded pre-retrain run (values reproduced exactly in results/_backup_pre_retrain/checkpoints_cdef/), whereas NB04_Table09.html, NB04_Table12.html and the live results/checkpoints/exp_d_*.json all carry the current run. Stale locations: (a) 6.results_and_discussion.tex lines 34-36, the main results table, prints D LSTM 5,836 / 1,576 / 15,057 / 6.51 / 0.06, D CNN-LSTM 6,618 / 3,607 / 14,702 / 22.30 / 0.11, D CNN 6,866 / 3,717 / 15,002 / 23.78 / 0.07 against current 6,316.79 / 2,945.92 / 14,645.14 / 1,837.11 / 0.11, 6,929.09 / 3,772.26 / 14,859.82 / 2,356.55 / 0.09 and 6,524.73 / 3,345.99 / 14,837.29 / 2,239.79 / 0.09; (b) line 68 as quoted; (c) appendices.tex lines 114-116, the tab:expd-full table, repeats the same three stale rows; (d) appendices.tex line 105 asserts the LSTM R2 rose "from a value of 0.00 to 0.06" and that the "maximum R2 = .11 CNN-LSTM". Critically, the R2 inversion propagates into the thesis's reasoning, not just a caption: line 66 concludes "the static LSTM and the CNN-LSTM hybrid separate only marginally, with the hybrid attaining the higher R2", which the current data contradicts outright, since the LSTM now has the higher R2 (0.11 vs 0.09) as well as the lower MAE, making it the unambiguous best model on both metrics. The MAPE gap is the clearest symptom of the different run: the thesis prints 6.51% for the D LSTM where the current export gives 1,837.11%. Note also that the whole results table is from the old run, not only the D rows - e.g. Experiment A Random Forest is printed as MAE 4,316 / R2 0.27 while Table12 gives 6,796.98 / 0.02 - so a fix scoped only to Experiment D would leave the chapter internally inconsistent.

**Kanıt:** NB04_Table09.html body rows read LSTM | 6316.79 | 2945.92 | 14645.14 | 1837.11 | 0.11, then CNN | 6524.73 | 3345.99 | 14837.29 | 2239.79 | 0.09, then CNN-LSTM | 6929.09 | 3772.26 | 14859.82 | 2356.55 | 0.09. The caption markdown cell 69 of notebooks/en/04_runtime_prediction_models.ipynb says 'the LSTM model demonstrated the best overall error minimization, achieving the lowest **Test MAE (5836.32)**', 'The **CNN-LSTM** hybrid model followed with a **Test MAE of 6618.09**' (MdAE 3607.06, RMSE 14702.46, MAPE 22.30%), and 'The **CNN** model yielded the highest error profile in this block with a **Test MAE of 6865.60** and an **R² score of 0.07**'. thesis/latex/chapters/6.results_and_discussion.tex line 68 prints the same stale run: 'In its best configuration (Experiment D, One-Hot), the LSTM achieved R\textsuperscript{2} = 0.06 and an MAE of 5{,}836\,s.' Not one of 5836.32, 6618.09, 6865.60, 3607.06, 14702.46, 0.07 or 0.06 appears anywhere in the exported table, and the table ranks CNN (6524.73) second and CNN-LSTM (6929.09) last — the opposite of the caption. Table09's values are confirmed correct against NB04_Table12.html rows 'D — DL Categorical | Categorical | LSTM (One-Hot) | 6316.79 ... 0.11' and against results/checkpoints/exp_d_lstm.json (r2=0.11454553569314656), so the table is current and the prose is stale.

**Düzeltme:** Rewrite markdown cell 69 and thesis/latex/chapters/6.results_and_discussion.tex line 68 from the current checkpoint values: LSTM MAE 6316.79 s / R² 0.11 (best), CNN MAE 6524.73 s / R² 0.09, CNN-LSTM MAE 6929.09 s / R² 0.09 (worst). Delete the 'CNN-LSTM followed' and 'CNN yielded the highest error profile' claims and the MAPE figures 22.30%/23.78%, which are also on a 100x-wrong scale relative to the table's 2239.79 and 2356.55 (src/models/evaluation.py returns MAPE on a 0-100 scale).

---

### `NB04_Table09.html NB04_Table10.html NB04_Table11.html` — data-integrity

**Ne:** The defect is real and slightly wider than described. (1) Chapter 6 asserts negative R2 for Experiment F in two places, not one: line 68 ("the application of these sequential models to sliding-window input sequences in Experiments E and F resulted in negative values of R\textsuperscript{2}") and line 11 ("Temporal or sequence-based models from Experiments E and F yielded negative R\textsuperscript{2} values, which indicate their predictions were worse than a simple mean baseline"). Only Experiment E (NB04_Table10: -0.02, -0.01, -0.00) supports this; every Experiment F R2 in NB04_Table11 is positive (0.13, 0.04, 0.03). (2) The misordered caption is the notebook markdown cell 89 in notebooks/en/04_runtime_prediction_models.ipynb, not a caption in the printed thesis; the printed-thesis counterpart of the same stale run is Table 6.1 (\label{tab:predresults}, line 13ff), whose F rows read CNN 6,704 / 0.04, CNN-LSTM 10,212 / -0.07, LSTM 13,169 / -0.27 — the reverse ranking from the current data. (3) The staleness is not confined to F: the same printed table's D block (LSTM 5,836 / R2 0.06) disagrees with NB04_Table09.html and exp_d_lstm.json (6316.79 / 0.11), and its MAPE column is on a different scale entirely from the exported tables (e.g. 25.23 vs 2190.86). The narrative, Table 6.1 and cell 89 all reflect a superseded run and must be regenerated from the current checkpoints.

**Kanıt:** NB04_Table11.html body rows read LSTM | 6137.18 | 2555.25 | 14528.68 | 1604.51 | 0.13, then CNN-LSTM | 6387.03 | 3057.63 | 15236.33 | 1957.24 | 0.04, then CNN | 6464.84 | 3621.12 | 15315.53 | 2190.86 | 0.03 — all three 'Test R²' cells positive. thesis/latex/chapters/6.results_and_discussion.tex line 68 says 'the application of these sequential models to sliding-window input sequences in Experiments E and F resulted in negative values of R\textsuperscript{2}. A flat prediction of the mean has therefore outperformed these models.' Caption markdown cell 89 says 'the **CNN** model demonstrated the best overall performance in this sequential setup, achieving the lowest **Test MAE (6704.19)** and the only positive **R² score (0.04)** in this block', 'the **CNN-LSTM** hybrid model yielded a higher error profile with a **Test MAE of 10212.43**', and 'the **LSTM** architecture posted the highest error metrics (**MAE: 13169.47**) and a significant negative explanatory variance (**R² = -0.27**)'. The table has LSTM best at 6137.18 with R² 0.13. results/checkpoints/exp_f_lstm.json confirms r2=0.12860522025327992, and NB04_Table12.html carries the identical row 'F — DL Categorical (Sequence) | Categorical | LSTM (One-Hot Sequence) | 6137.18 ... 0.13'.

**Düzeltme:** Rewrite markdown cell 89 and correct thesis/latex/chapters/6.results_and_discussion.tex line 68: Experiment F R² values are +0.13 (LSTM), +0.04 (CNN-LSTM) and +0.03 (CNN), so the 'negative values of R²' claim and the conclusion that 'a flat prediction of the mean has therefore outperformed these models' must be narrowed to Experiment E only. Replace 6704.19 / 10212.43 / 13169.47 / -0.27 / 13582.38 with the table's 6464.84 / 6387.03 / 6137.18 / 0.13 / 2555.25 and invert the architecture ranking.

---

### `NB04_Table09.html NB04_Table10.html NB04_Table11.html` — data-integrity

**Ne:** The finding is real but understates its scope: it is not confined to Experiment E / Table10, and the caption's MAPE figures are wrong too. Table10 (Exp E, caption cell 79): caption names LSTM best at "Test MAE of 6923.04" with CNN 8511.93 and CNN-LSTM 8907.21; the table (sorted ascending on Test MAE) reads CNN 6703.64, CNN-LSTM 6716.67, LSTM 6844.75 - so LSTM is in fact the worst, and none of the three quoted MAE values appear in any cell. Caption MdAE 4378.99/6624.42/7269.48 vs table 3762.03/3470.14/3817.59: no match. Caption R2 range "-0.01 to -0.02" excludes the table's -0.00. Additionally (not noted in the original claim), the caption's MAPE values of 27.64%, 45.20% and 49.07% sit against table cells of 2504.15, 2371.54 and 2415.03 - roughly two orders of magnitude apart. The same caption/table divergence affects the sibling artefacts named alongside it: - Table09 (Exp D, caption cell 69): caption claims LSTM best at MAE 5836.32, CNN-LSTM 6618.09, CNN worst at 6865.60 with CNN-LSTM holding the top R2 of 0.11; the table reads LSTM 6316.79 (R2 0.11), CNN 6524.73 (0.09), CNN-LSTM 6929.09 (0.09) - the 2nd/3rd ranking is inverted and no quoted value matches a cell. - Table11 (Exp F, caption cell 89): caption claims CNN best at MAE 6704.19 with "the only positive R2 score (0.04)" and LSTM worst at MAE 13169.47 / R2 -0.27 / MdAE 13582.38; the table reads LSTM BEST at 6137.18 (R2 0.13), CNN-LSTM 6387.03 (0.04), CNN worst at 6464.84 (0.03), with all three R2 positive. The ranking is fully inverted and the claim about a single positive R2 is false against the table. - Table08 (Exp C, caption cell 58, not in the original artefact list) is wrong the same way: caption names CNN best (6453.92) and LSTM weakest (8184.20), while the table reads LSTM best at 6688.74, CNN 7040.17, CNN-LSTM 7230.19. In short, all four deep-learning experiment captions (C, D, E, F) appear to have been written against a different, superseded run and were never regenerated after the tables were re-exported.

**Kanıt:** NB04_Table10.html body rows read CNN | 6703.64 | 3470.14 | 15718.00 | 2371.54 | -0.02, then CNN-LSTM | 6716.67 | 3817.59 | 15674.71 | 2415.03 | -0.01, then LSTM | 6844.75 | 3762.03 | 15573.61 | 2504.15 | -0.00. Caption markdown cell 79 says 'the **LSTM** model demonstrated the best relative performance in this sequential setup with a **Test MAE of 6923.04**' and 'The **CNN** model and the **CNN-LSTM** hybrid models showed higher error profiles, posting **Test MAE values of 8511.93 and 8907.21** respectively'. The table is sorted ascending on 'Test MAE (s)' and puts LSTM last at 6844.75, i.e. worst; 6923.04, 8511.93 and 8907.21 appear in no cell of the table. The caption's MdAE figures (4378.99, 6624.42, 7269.48) likewise match no cell against the table's 3762.03, 3470.14 and 3817.59. The caption also states 'All models in this numeric-only sequence block yielded slightly negative **R² values (-0.01 to -0.02)**' while the table's third R² cell reads -0.00, outside that stated range.

**Düzeltme:** Rewrite markdown cell 79 from the table: CNN best at MAE 6703.64 s, CNN-LSTM 6716.67 s, LSTM worst at 6844.75 s; state the R² range as -0.02 to -0.00 (or -0.02 to 0.00). Remove the 'LSTM demonstrated the best relative performance' claim entirely.

---

### `NB04_Table12.html NB04_Table13.html NB04_Table14.html` — data-integrity

**Ne:** The caption for Table 12 (notebook "Table 7 — Overall Comparison of All Experiments") declares "XGBoost with One-Hot encoding (Experiment B)" the best overall model "achieving the lowest MAE (3389.26) and the highest R² score (0.51)" and "MdAE: 1128.89 seconds" — none of those three numbers appear anywhere in the table, and that model is not the winner in it.

**Kanıt:** NB04_Table12.html, the XGBoost (One-Hot) row reads <td>B — ML Categorical</td><td>Categorical</td><td>XGBoost (One-Hot)</td><td>6053.96</td><td>2754.63</td><td>14056.63</td><td>1889.24</td><td>0.18</td>. Parsing all 27 data rows: the minimum "MAE (s)" in the table is 3151.10 and the maximum "R²" is 0.35, both belonging to <td>Alibaba Estimate (SJGG)</td>. The values 3389.26 and 0.51 occur in no cell of the file. The caption text in notebooks/en/04_runtime_prediction_models.ipynb cell 92 reads: "Among all 19 evaluated configurations, **XGBoost with One-Hot encoding (Experiment B)** emerged as the **best overall model**, achieving the lowest **MAE (3389.26)** and the highest **R² score (0.51)**" and later "(MdAE: 1128.89 seconds)". The table-producing cell 91 even contains the comment "SJGG reaches MAE 3151 s / R² 0.35, ahead of every model trained here", contradicting the caption's "traditional machine learning models showed significantly better performance".

**Düzeltme:** Regenerate the caption programmatically from df_all instead of hand-writing it: emit the champion row with df_all.loc[df_all['MAE (s)'].idxmin()] and df_all.loc[df_all['R²'].idxmax()], and f-string the MAE / MdAE / R² straight out of that row so the prose can never drift from the exported table.

---

### `NB04_Table12.html NB04_Table13.html NB04_Table14.html` — data-integrity

**Ne:** The caption that accompanies the deep-learning comparison (notebook "Figure 5", the same dl_summary object exported as Table 13) quotes MAE, MAPE and MdAE values that contradict every corresponding cell of Table 13, and it calls the Experiment-F LSTM the worst model when the table shows it as the best.

**Kanıt:** NB04_Table13.html row <td>D — Categorical</td><td>LSTM</td> reads MAE (s) 6316.79, MdAE (s) 2945.92, MAPE (%) 1837.11; the caption (cell 102) claims for that same model "a **Test MAE of 5836.32**" and "achieving a **6.51% MAPE** and a **1576.04 Test MdAE**". Row <td>F — Categorical (Sequence)</td><td>LSTM</td> reads MAE (s) 6137.18, MdAE (s) 2555.25, MAPE (%) 1604.51 — the lowest MAE of all 12 rows in the file — while the caption says "the LSTM architecture in Experiment F which reached the highest error profile" with "an **85.66% MAPE** and a **13582.38 MdAE**—which even exceeded its mean error". Row <td>F — Categorical (Sequence)</td><td>CNN</td> reads MAE (s) 6464.84 against the caption's "**CNN (Experiment F - MAE 6704.19)**".

**Düzeltme:** Rebuild the caption from dl_summary: pull the best/worst rows with dl_summary.loc[dl_summary['mae'].idxmin()] / .idxmax() and interpolate mae, mdae, mape from those rows, rather than restating numbers from a superseded run.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — naming

**Ne:** The exported tables label the native-categorical LightGBM policy "SJF-LGBM (Categorical)" while the notebook code now names it "SJF-LGBM (Native Cat)" — inside the same table "(Categorical)" means one-hot (RF/XGBoost/CNN/LSTM all predict on the 591-column X_test_cat_oh) and a distinct "(Native Cat)" suffix already exists for XGBoost, so the LGBM row carries the wrong one of the two suffixes and points at results/models/lgbm_categorical.joblib, a one-hot model that exists but was never simulated. Two corrections to the claim's evidence: (1) the string "SJF-LGBM (Categorical)" DOES appear 11 times in 05_scheduler_evaluation_256_gpu.ipynb (lines 1449, 1616, 2796, 3123, 3375, 3587, 3608, 3915, 4090, 4682, 4786), but exclusively inside stale stored cell outputs, never in executable code — so the exports are stale relative to the current code, not contradicted by the notebook file as a whole. (2) The scope is wider than three files: NB05_256GPU_Table01/02/03/04/05 and all five NB05_32GPU_Table01-05 carry the stale label, and no exported table anywhere uses "SJF-LGBM (Native Cat)". Fix requires re-running the notebook's table-export cells, not editing three files.

**Kanıt:** Table01 row 20 cell: `<td>SJF-LGBM (Categorical)</td>`; Table02 row19 `SJF-LGBM (Categorical)`; Table03 rows `SJF-LGBM (Categorical)`. Notebook cell 24: `_PRED_COL_MAP = { ... "SJF-LGBM (Native Cat)": "pred_lgb_cat", "SJF-XGBoost (Categorical)": "pred_xgb_cat", "SJF-XGBoost (Native Cat)": "pred_xgb_nat", ...}` and `POLICIES` list entry `"SJF-LGBM (Native Cat)"` — the string "SJF-LGBM (Categorical)" does not exist anywhere in the notebook. Cell 5 shows the two encodings are genuinely different: `preds_xgb_cat = xgb_cat.predict(X_test_cat_oh)` (one-hot) vs `preds_lgb_cat = lgb_cat_nat.predict(X_test_cat_base)` (native). Table01 therefore carries both `SJF-XGBoost (Categorical)` and `SJF-XGBoost (Native Cat)` rows but only one LGBM row, labelled with the wrong one of the two suffixes.

**Düzeltme:** Re-export the three tables from the current notebook so the row reads "SJF-LGBM (Native Cat)", or rename in place. Every exported NB05 table (256GPU and 32GPU) carries the stale label, so the whole export is out of date with the notebook — re-run the export rather than patching one file. Also define "Native Cat" (native categorical splits) vs "Categorical" (one-hot) in the table caption; "Native Cat" appears only as a bare abbreviation in data cells.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** The contradiction is real but sits in three different captions of notebooks/en/05_scheduler_evaluation_256_gpu.ipynb (57 cells total), not in the "Table 2 caption" alone: 1. Cell 22 — the **Table 1** caption (not Table 2) — contains "SJF-XGBoost (Categorical) demonstrates the best performance, achieving the lowest mean JCT among the learned models ... followed by SJF-LightGBM (Categorical) and SJF-Random Forest (Categorical)". Table01's ascending Mean JCT sort makes SJF-XGBoost (Categorical) only 3rd among learned policies (55704.31), behind SJF-LSTM (Categorical) 55056.31 and SJF-XGBoost (Native Cat) 55650.34, and puts SJF-LGBM (Categorical) at 100599.05, row 20 of 28. 2. Cell 33 — the actual **Table 2** caption — repeats the wrong winner ("tree-based ... consistently outperform deep learning-based policies", contradicted by LSTM Cat 61.31% vs LGBM Cat 29.31%) and additionally cites numbers that appear nowhere in the table: SJF-XGBoost (Categorical) "55.06% JCT improvement" and "0.14 R²" (table: 60.86 and 0.18) and SJF-Oracle "82.38% improvement" (table: 77.05). 3. The median-wait sentence naming SJF-Random Forest (Categorical) and SJF-XGBoost (Categorical) as having "the absolute lowest median waiting times among practical policies" is in cell 55, the **Figure 5** caption (there is no cell 2011). Table01/Table05 give SJF-LSTM (Categorical) 21599.4 and SJF-LGBM (No Cluster-Load Features) 27707.5 below XGB Cat 31404.3 and RF Cat 40670.6. Cell 55 is also self-contradictory: a later bullet in the same caption credits SJF-LSTM (Categorical) with "the highest overall JCT reduction in the 256-GPU environment". Related numbering issue found while verifying: the export files are scraped in display order, so NB05_256GPU_Table01.html is the comprehensive evaluation table described by the cell 33 "Table 2" caption, Table02.html is the Wilcoxon table described by the cell 52 "Table 3" caption, and Table03.html (paired significance vs non-FIFO references, from cell 48) has no caption at all. The cell 22 "Table 1" caption describes a sim_jobs preview that cell 20 deliberately prints rather than displays, so no exported table corresponds to it. The same stale ranking prose appears in the 32-GPU notebook.

**Kanıt:** Notebook cell 22: "**SJF-XGBoost (Categorical)** demonstrates the best performance, achieving the lowest mean JCT among the learned models ... This is followed by **SJF-LightGBM (Categorical)** and **SJF-Random Forest (Categorical)**." Table01 Mean JCT (s), sorted ascending: SJF-LSTM (Categorical) 55056.31, SJF-XGBoost (Native Cat) 55650.34, then SJF-XGBoost (Categorical) 55704.31; SJF-LGBM (Categorical) is 100599.05 (20th row of 28). Cell 33 likewise claims tree-based categorical models "consistently outperform deep learning-based policies" while Table01 gives SJF-LSTM (Categorical) JCT Improvement % = 61.31 against SJF-LGBM (Categorical) 29.31. Cell 2011 claims SJF-Random Forest (Categorical) and SJF-XGBoost (Categorical) "achieve the absolute lowest median waiting times among practical policies", but Table01 Median Wait (s) gives SJF-LSTM (Categorical) 21599.4 and SJF-LGBM (No Cluster-Load Features) 27707.5 below SJF-XGBoost (Categorical) 31404.3 and SJF-RF (Categorical) 40670.6.

**Düzeltme:** Rewrite the captions in notebook cells 22, 33 and the cell at line 2011 to describe the sort order the exported table actually has: best learned policy is SJF-LSTM (Categorical), then SJF-XGBoost (Native Cat), then SJF-XGBoost (Categorical); drop the "tree-based consistently outperform deep learning" claim and the median-wait claim, or restate them with the table's real values.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** Real defect, with two corrections to the description. (1) The parenthetical explanation of the 55.06 figure is wrong: 55056.31 is SJF-LSTM (Categorical)'s Mean JCT (s) (line 55), not SJF-XGBoost (Categorical)'s, whose Mean JCT is 55704.31 (line 85) - so the "transposed from that row's Mean JCT" account does not hold. (2) The "worsen JCT by 45.4%" sentence is in the Figure 4 (improvement heatmap) caption, cell 45, not in Table01's caption; a matching "-45.4% degradation" also appears in the Figure 1 caption, cell 36. Both still contradict the same JCT Improvement % column of Table01, whose worst value is -22.62. Two further contradictions belong in the finding: the caption's "Leading Predictor" is SJF-XGBoost (Categorical), but Table01's best practical policy is SJF-LSTM (Categorical) at 61.31 vs XGBoost's 60.86, directly contradicting the same notebook's Figure 1 caption (cell 36), which names LSTM as the leader; and the policy the captions blame for the catastrophic degradation, SJF-LSTM (Numeric Sequence), actually shows +51.95 in Table01 (line 153) - a top-half improvement - so the caption inverts the sign of its own conclusion. The stale paragraph is duplicated verbatim in notebooks/en/05_scheduler_evaluation_32_gpu.ipynb cell 33 and needs the same fix there.

**Kanıt:** Cell 33: "**SJF-Oracle** provides the best possible performance (82.38% improvement)" — Table01 JCT Improvement % for SJF-Oracle is 77.05. Same cell: SJF-XGBoost (Categorical) "delivering a **55.06% JCT improvement** over FIFO" — Table01 gives 60.86 (55056.31 is that row's Mean JCT (s), which looks like the source of the transposed figure). Same bullet: "prediction accuracy (0.14 R²)" — Table01 Model R² for that row is 0.18. Cell at line 1505: numeric sequence models "worsen JCT by **45.4%**" — the most negative value in Table01's JCT Improvement % column is -22.62 (SJF-CNN (Numeric Sequence)).

**Düzeltme:** Recompute every figure quoted in the captions from the exported table rather than from a previous run: 77.05, 60.86, 0.18, -22.62. These look like leftovers from the 32-GPU configuration or an earlier simulation.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** The defect is real and broader than described: the entire cell-33 caption for NB05_256GPU_Table01.html is stale relative to the table it captions. (1) It claims SJF-LSTM (Numeric Sequence) shows negative improvement / worse than FIFO, while the table shows +51.95% (Mean JCT 68381.37 vs FIFO 142315.25), 8th of 28. (2) It states SJF-Oracle gives "82.38% improvement"; the table shows 77.05%. (3) It names SJF-XGBoost (Categorical) the "Leading Predictor" at "55.06% JCT improvement"; the table shows 60.86% for that policy and the actual leader among practical policies is SJF-LSTM (Categorical) at 61.31%. (4) It asserts tree-based categorical models "consistently outperform deep learning-based policies", which the table contradicts (LSTM Categorical ranks above all tree models). Separately, and relevant to the printed thesis: thesis/latex/chapters/6.results_and_discussion.tex reports an entirely different 256-GPU result set (FIFO mean JCT 50,386; SJF-LSTM (Numeric Sequence) 73,266 at -45.41%; SJF-Oracle 71.21%), so the exported Table01 and the thesis body text describe two different simulation runs. Only Table01 is affected among the three named artefacts; Table02 and Table03 carry no such caption conflict.

**Kanıt:** Cell 33: "The results for **SJF-LSTM (Numeric Sequence)** highlight the critical importance of model accuracy; poor runtime predictions can actually lead to worse performance than the simple FIFO baseline (negative improvement)." Table01 row `SJF-LSTM (Numeric Sequence)`: Mean JCT (s) 68381.37, JCT Improvement % 51.95 — 8th best of 28 rows. The only rows with negative improvement are SJF-AlibabaEstimate-Group (baseline) at -0.26 and SJF-CNN (Numeric Sequence) at -22.62.

**Düzeltme:** Replace SJF-LSTM (Numeric Sequence) with SJF-CNN (Numeric Sequence) (-22.62) in that caption bullet, which is the policy that actually lands below FIFO.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — precision

**Ne:** All 216 numeric cells in NB05_256GPU_Table02.html (27 rows x 8 numeric columns, no exceptions) render with exactly six decimal places, because notebook cell 47 ends with display(sig_df.style.map(...), subset=["Significant?"]) — a Styler used only to colour the "Significant?" column, carrying no .format(), so pandas' default styler.format.precision=6 governs every float. Consequences, in order of severity: (1) the p-value and p (Holm) columns are rendered as "0.000000" for all 26 significant policies and "1.000000" for the last, asserting a probability of exactly zero and erasing all discrimination between policies — the sibling Table03, built from the same statistics by plain to_html() in cell 48, correctly shows 2.382329e-68, 1.213794e-88, 6.975640e-24; (2) the same quantity is printed inconsistently across adjacent thesis tables, SJF-Oracle Mean JCT being 32657.89 in Table01 and 32657.900000 in Table02; (3) false precision on values the notebook had already rounded — five fabricated trailing zeros on the 1-decimal columns (Mean JCT, JCT vs FIFO, both CI bounds, Wilcoxon W) and three on the 3-decimal Effect size (r) column, so Wilcoxon W prints as 118716811.500000, 15 significant figures. The claim's blanket phrase "five fabricated trailing zeros" is inaccurate for the Effect size (r) column, which gains three, and understates the defect by framing it as zero-padding when the p-value column is materially misreported. Fix is a .format() on the Styler (e.g. per-column {:.1f}, {:.3f}, and {:.3g} or scientific notation for the two p columns).

**Kanıt:** Table02 cells: Mean JCT (s) `32657.900000`, JCT vs FIFO (s) `109657.400000`, 95% CI low `108013.100000`, Effect size (r) `0.871000`, Wilcoxon W `118716811.500000`. Notebook cell 47 builds these as `round(df_p["turnaround_time"].mean(), 1)`, `round(diff.mean(), 1)`, `round(lo, 1)`, `round(_rank_biserial(diff), 3)`, `round(stat, 1)` and then renders with `display(sig_df.style.map(...))` — a Styler with no `.format()`, so pandas' default six-decimal float repr pads the already-rounded numbers.

**Düzeltme:** Add an explicit format to the Styler before export, e.g. `.format({'Mean JCT (s)':'{:,.1f}', 'JCT vs FIFO (s)':'{:,.1f}', '95% CI low':'{:,.1f}', '95% CI high':'{:,.1f}', 'Effect size (r)':'{:.3f}', 'Wilcoxon W':'{:,.0f}', 'p-value':'{:.3g}', 'p (Holm)':'{:.3g}'})`.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** Both NB05_256GPU significance tables print unattainable p-values of exactly zero: 26 of 27 rows in Table02 read `0.000000` in both the `p-value` and `p (Holm)` columns (only row26, SJF-CNN (Numeric Sequence), differs at 1.000000), and 16 of 43 rows in Table03 read `0.000000e+00` in both columns. The same defect is present in the 32-GPU twins (NB05_32GPU_Table02/Table03, identical 26/27 and 16/43 counts). One precision correction to the claim's framing: the underflow diagnosis is directly demonstrable only for Table03, whose scientific format shows real neighbours down to 1.105640e-298 alongside the zeros. Table02 applies a uniform 6-decimal fixed format to the whole frame (Mean JCT likewise prints as 32657.900000), so its `0.000000` strictly means p < 5e-7 and the table itself cannot distinguish true underflow from a merely tiny p — the underlying cause is the same asymptotic-Wilcoxon tail, but in Table02 the fixed format is an additional, independent reason the value is unreadable. Fix both by reporting a floor (e.g. "p < 1e-300", or "< 2.2e-308 (float64 underflow)") instead of zero, and by correcting the Table 3 narrative in cell 52 of notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, which currently singles out three policies as having "the absolute strongest statistical evidence, maintaining p-values strictly at $0.000000$" when all 26 significant rows show that same printed value.

**Kanıt:** Table02 `p-value` and `p (Holm)` cells read `0.000000` for every row except row26; Table03 `p-value` / `p (Holm)` cells read `0.000000e+00` for 16 rows (e.g. `SJF-UserMedian (baseline)` vs `SJF-Oracle`, `SRF (Heuristic)` vs `SJF-Oracle`). Table03 shows the same column can carry real magnitudes: `2.382329e-68`, `1.167435e-280`, so the zeros are underflow of the normal-approximation tail, not a formatting choice.

**Düzeltme:** Print a bound rather than a value: format p-values below the double-precision floor as "< 1e-308" (or "< 0.001" if the thesis uses three-decimal convention), and state the convention in the caption. Never let a p-value print as 0.

---

### `NB05_256GPU_Table04.html NB05_256GPU_Table05.html` — data-integrity

**Ne:** Real defect; two small counting corrections. (1) In NB05_256GPU_Table05 'Median (s)', SJF-RF (Categorical) 40670.6 ranks 8th of 28 overall, or 7th among non-Oracle policies - behind SJF-LSTM (Categorical) 21599.4, SJF-LGBM (No Cluster-Load Features) 27707.5, SJF-XGBoost (Native Cat) 30048.7, SJF-LSTM (Categorical Sequence) 30947.6, SJF-XGBoost (Categorical) 31404.3 and SJF-CNN-LSTM (Categorical) 34189.1. (2) In 'P95 (s)', SJF-LSTM (Categorical) 207965.3 ranks 14th of 28; 13 policies beat it, of which 11 (not 12) are SJF-prediction-model policies - the other two are SJF-Oracle 142927.0 and SRF (Heuristic) 191597.7. The claim also understates the problem: the same caption's fourth bullet says "SJF-LSTM Numeric Sequence post disastrous median wait times - performing nearly 3 times worse than the 'blind' FIFO baseline", but Table05 gives SJF-LSTM (Numeric Sequence) median 50506.9 against FIFO 116385.6, i.e. roughly 2.3 times better, not 3 times worse. The identical inversion appears in the 32-GPU notebook's Figure 5 caption (Table05_32GPU: SJF-LSTM (Categorical) lowest non-Oracle median 297868.6; 8th on P95 at 1754058.7), so both notebooks need the caption rewritten to match their tables.

**Kanıt:** Table05 'Median (s)': SJF-LSTM (Categorical) = 21599.4, SJF-LGBM (No Cluster-Load Features) = 27707.5, SJF-XGBoost (Native Cat) = 30048.7, SJF-XGBoost (Categorical) = 31404.3, SJF-RF (Categorical) = 40670.6 — RF (Categorical) is 7th, not lowest. Table05 'P95 (s)': SJF-LSTM (Categorical) = 207965.3 while SJF-XGBoost (Native Cat) = 167963.3, SJF-RF (Categorical) = 174546.4, SJF-CNN (Numeric) = 180521.2, SJF-XGBoost (Categorical) = 182981.8 (12 prediction policies beat it). The same caption's "SJF-Oracle maintains the lowest values across all percentiles" also fails: Oracle 'P99 (s)' = 235249.1 vs SJF-CNN-LSTM (Numeric Sequence) 210186.6 and SJF-XGBoost (Native Cat) 217716.3; Oracle 'Max (s)' = 384163.7 vs SRF (Heuristic) 346397.1.

**Düzeltme:** Rewrite the three bullets in notebook cell 55 from the exported numbers: median leader among practical policies is SJF-LSTM (Categorical) (21599.4 s); P95 leader is SJF-XGBoost (Native Cat) (167963.3 s); restrict the Oracle claim to Median/P75/P90/P95, where it does hold.

---

### `NB05_256GPU_Table04.html NB05_256GPU_Table05.html` — data-integrity

**Ne:** The caption is stale narrative from a superseded simulation run, not a mistakenly swapped comparison, and the contradiction is wider than one caption. Where it lives: the sentence is not inside NB05_256GPU_Table05.html (that file has no caption at all). It is in notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, markdown cell 55 ("Figure 5 — Wait Time Percentile Analysis Across Scheduling Policies (256 GPUs)"), the shared commentary for the wait-percentile table and dot plot produced by code cell 54, and it is reproduced in reports/html/en/05_scheduler_evaluation_256_gpu.html directly beneath the table. Against the current run the caption is false: Table05 gives SJF-LSTM (Numeric Sequence) median wait 50,506.9 s vs FIFO 116,385.6 s, i.e. 2.30x BETTER, and no policy in the table reaches even 1.5x FIFO's median. NB05_256GPU_Table04.html independently confirms the reversal: that policy's "JCT vs FIFO (s)" is +73,933.9 with iid 95% CI [72,238.4, 75,474.8] and user-cluster 95% CI [51,572.5, 91,919.7] — the whole interval on the better-than-FIFO side. Why it reads as "inverted": three runs coexist. The oldest run (reports/html/en/05_scheduler_evaluation_256_gpu.html: FIFO median 27,901.7, SJF-LSTM (Numeric Sequence) 74,734 = 2.68x) made "nearly 3 times worse" correct, and that older run is still exactly what the printed thesis prints in thesis/latex/chapters/6.results_and_discussion.tex tab:waitpercentile (256-GPU block: FIFO 27,901 / 69,707 / 102,425 / 123,407 / 124,024 / 124,462). The Aug-31 backup run (results/_backup_20260831_1703_pre_q2/) already broke the claim (FIFO 130,412.2 vs 95,806.1). The caption was never refreshed across either re-run. The reader-facing blocker: the thesis's headline "Sequence Trap" finding — thesis/latex/chapters/7.conclusions_and_future_work.tex line 22, "SJF-LSTM (Numeric Sequence) exhibited a degradation in scheduling performance relative to a blind FIFO baseline of -45.41%", plus the matching rows in chapter 6 — is contradicted by both currently exported artefacts, where that policy beats FIFO on JCT and on median wait. Only SJF-CNN (Numeric Sequence) is still worse than FIFO (Table04: -32,189.9). Fix by re-exporting the thesis tables from the current run and rewriting the notebook commentary and the Sequence-Trap narrative together, or by explicitly documenting which run the thesis reports.

**Kanıt:** Table05 row 'SJF-LSTM (Numeric Sequence)' 'Median (s)' = 50506.9; row 'FIFO' 'Median (s)' = 116385.6 (50506.9 / 116385.6 = 0.43). Even the worst sequence model in the table, 'SJF-CNN (Numeric Sequence)' = 173704.4, is only 1.49x FIFO's median, never 3x.

**Düzeltme:** Delete or rewrite the "Deep Learning Instability without Categorical Context" bullet in notebook cell 55; if a genuinely bad sequence policy is wanted, cite SJF-CNN (Numeric Sequence) at 173704.4 s (1.5x FIFO).

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** The defect stands as described; only one supporting row index in the claim is off by one. Accurately: in NB05_32GPU_Table01.html (28 rows, ascending Mean JCT) the caption's claimed 1st/2nd/3rd learned models are rows 3 (SJF-XGBoost (Categorical), 538743.61), 18 (SJF-LGBM (Categorical), 879366.94) and 6 (SJF-RF (Categorical), 618918.52) — not rows 2, 18 and 6. Row 2 is SJF-LSTM (Categorical) at 507394.68, the actual lowest-mean-JCT learned model. SJF-LGBM (Categorical) at 18th ranks below the SRF heuristic (row 13, 748782.63) and the SJF-UserMedian baseline (row 16, 846496.17).

**Kanıt:** Notebook cell 22 caption: "**SJF-XGBoost (Categorical)** demonstrates the best performance, achieving the lowest mean JCT among the learned models ... This is followed by **SJF-LightGBM (Categorical)** and **SJF-Random Forest (Categorical)**." Table01 Mean JCT (s): SJF-LSTM (Categorical) 507394.68; SJF-XGBoost (Categorical) 538743.61; SJF-RF (Categorical) 618918.52; SJF-UserMedian (baseline) 846496.17; SJF-LGBM (Categorical) 879366.94. Table01 is sorted ascending by Mean JCT, so the caption's claimed 1st/2nd/3rd are rows 2, 18 and 6.

**Düzeltme:** Rewrite the cell-22 caption from the sorted table: lowest mean JCT among learned policies is SJF-LSTM (Categorical) at 507,394.68 s, followed by SJF-XGBoost (Categorical) 538,743.61 s and SJF-XGBoost (Native Cat) 541,452.43 s; state explicitly that SJF-LGBM (Categorical) at 879,366.94 s is worse than the SJF-UserMedian baseline.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** The claim is correct but understates the scope. Accurate statement: cell 47's missing Styler .format() collapses BOTH p columns of NB05_32GPU_Table02.html to two strings ("0.000000" x26, "1.000000" x1 for row26 SJF-CNN (Numeric Sequence)), destroying real distinctions -- reconstruction from the printed Wilcoxon W and effect size r shows true p-values of ~2.0e-80 (row24), ~1.3e-31 (row25) and genuine underflow-to-zero (row20) all printing as "0.000000". The same missing .format() additionally over-prints every other numeric column past its intended precision (the source round()s to 1 or 3 decimals, yet the table shows "252353.800000", "127787576.500000", "0.905000"). The identical defect is present in the sibling NB05_256GPU_Table02.html (also 27 rows, same 26x"0.000000" + 1x"1.000000"). One caveat to the claimed contrast: Table03 is not fully clean either -- it prints "0.000000e+00" for p-values that underflow -- so it is better only in that it preserves magnitude where magnitude exists.

**Kanıt:** Every Table02 data row prints p-value and p (Holm) as exactly "0.000000" except row26 (SJF-CNN (Numeric Sequence)) which prints "1.000000" in both. Compare Table03's p-value column for the same kind of Wilcoxon test: "1.042926e-24", "2.598422e-40", "9.109344e-01", "4.759153e-01". Cause: notebook cell 47 renders `display(sig_df.style.map(...))` with no `.format()`, so pandas Styler's default 6-decimal float format is applied to the p columns.

**Düzeltme:** In cell 47 add a Styler format, e.g. `.format({"p-value": "{:.3g}", "p (Holm)": "{:.3g}"})`, and replace scipy's underflowed 0.0 with a floor string ("< 1e-300") rather than printing 0.000000.

---

### `NB05_32GPU_Table04.html NB05_32GPU_Table05.html` — data-integrity

**Ne:** Table04's "iid 95% CI low"/"iid 95% CI high" disagree with NB05_32GPU_Table02.html's "95% CI low"/"95% CI high" for the identical quantity in all 27 of 27 rows — e.g. SJF-Oracle is [1110203.5, 1139829.1] in Table04 but [1108812.6, 1138862.5] in Table02, for the same point estimate 1124356.5.

**Kanıt:** NB05_32GPU_Table04.html row "SJF-Oracle": "JCT vs FIFO (s)" = 1124356.5, "iid 95% CI low" = 1110203.5, "iid 95% CI high" = 1139829.1. NB05_32GPU_Table02.html row "SJF-Oracle": "JCT vs FIFO (s)" = 1124356.5 (identical), "95% CI low" = 1108812.6, "95% CI high" = 1138862.5. The point estimates match in 27/27 rows but the CI bounds mismatch in 27/27 rows, by up to 2802.7 s on the low bound and 2981.9 s on the high bound (e.g. row "SJF-AlibabaEstimate-Group (baseline)": iid low 195256.1 vs 196729.4; row "SJF-AlibabaEstimate-GroupGPU (baseline)": iid high 204510.5 vs 206317.4). Cause in notebooks/en/05_scheduler_evaluation_32_gpu.ipynb: both tables call the same `_boot_ci(diff)`, which draws from a module-level `_rng = np.random.default_rng(42)` created once outside the function; the generator state is not reset between the two cells, so the two tables bootstrap on different random draws.

**Düzeltme:** Make `_boot_ci` deterministic per call — instantiate `rng = np.random.default_rng(42)` inside the function (or pass a fresh seeded generator) — or better, compute the iid bootstrap CI once and reuse the same values in both tables. Re-run the notebook and re-export both Table02 and Table04 so the printed CI for a given policy is one number, not two.

---

## MAJOR (45)

### `NB01-Figure01.png` — resolution

**Ne:** The claim is accurate as written; only one secondary number is slightly off. "Runtime (seconds)" measures 25 px for the letters (30 px including the parenthesis ascender/descender), not 26 px. This does not change the conclusion. Two amplifying facts worth adding: the same defect is dpi-independent (22 px is 0.795% of the 2766 px figure width, so at 155 mm printed width the glyph is 1.23 mm regardless of the dpi metadata), and the figure prints only 55 mm tall (984/2766 x 15.5 cm) while occupying the full text width.

**Kanıt:** PNG is 2766 x 984 px; rcParams in notebook cell 3 set "savefig.dpi": 200, so the canvas is 2766/200 = 13.83 in wide (cell 15 sets figsize=(14, 5)). thesis/latex/chapters/3.dataset_and_workload.tex:44 is \includegraphics[width=\textwidth]{figures/nb01-fig01-runtime-dist.png}, and thesis.cls:97-101 gives a4paper with left=3.5cm, right=2cm, so \textwidth = 21 - 3.5 - 2 = 15.5 cm = 6.10 in. Scale factor = 6.10/13.83 = 0.44. Applied to rcParams "xtick.labelsize": 11 -> 4.85 pt, "ytick.labelsize": 11 -> 4.85 pt, "axes.labelsize": 12 -> 5.29 pt, "axes.titlesize": 14 -> 6.18 pt, suptitle fontsize=14 -> 6.18 pt. Measured glyph heights in the PNG: "70000" = 22 px, "100000" = 22 px, "Runtime (seconds)" = 26 px, "Raw Runtime Distribution" = 30 px; at the 0.44 print scale 22 px = 1.23 mm cap height. The inline comment in cell 3 claims "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column", but the 14-inch figsize defeats it.

**Düzeltme:** In notebooks/en/01_data_overview.ipynb cell 15 (and the identical notebooks/tr/01_veri_ozeti.ipynb cell 15) change fig, axes = plt.subplots(1, 2, figsize=(14, 5)) to figsize=(7.0, 2.8), so the print scale becomes 6.10/7.0 = 0.87 and the 11 pt ticks land at ~9.6 pt. To keep the exported raster above the 300 ppi floor at that smaller canvas, also raise "figure.dpi"/"savefig.dpi" from 200 to 300 in cell 3 (7.0 in x 300 = 2100 px over 6.10 in = 344 ppi).

---

### `NB01-Figure03.png` — resolution

**Ne:** The figure is exported 13.84 in wide but placed at width=\textwidth (6.10 in), a 0.441x reduction, so the printed text lands at ~4.9 pt tick labels ("0".."8", "0".."1400"), ~5.3 pt axis titles ("Elapsed Time (days)", "Jobs per Hour") and ~6.2 pt plot title ("Hourly Job Arrival Rate Over the Trace Duration") — all below the ~8 pt legibility floor for print.

**Kanıt:** PNG is 2767x968 px with embedded dpi 199.9996, i.e. 13.835 x 4.84 in; the source cell (notebooks/en/01_data_overview.ipynb, cell 19) sets figsize=(14, 5). Measured cap height of the y-tick digits "1400", "1200", "1000", "800", "600", "400", "200", "0" in the left margin column is 22 px at 200 dpi = 0.110 in = 7.9 pt cap height, confirming the 11 pt "xtick.labelsize"/"ytick.labelsize" set in cell 3. thesis/latex/chapters/3.dataset_and_workload.tex line 88 reads \includegraphics[width=\textwidth]{figures/nb01-fig03-arrival-rate.png}, and thesis.cls lines 97-101 give a4paper with left=3.5cm, right=2cm, so \textwidth = 21 - 3.5 - 2 = 15.5 cm = 6.10 in. Scale factor 6.10/13.835 = 0.441, giving 11x0.441 = 4.85 pt ticks, 12x0.441 = 5.29 pt labels, 14x0.441 = 6.17 pt title. The rcParams comment in cell 3 claims these "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column", which does not hold at a 14-inch figure width.

**Düzeltme:** In notebooks/en/01_data_overview.ipynb cell 19, shrink the canvas instead of the type: change fig, ax = plt.subplots(figsize=(14, 5)) to figsize=(7, 2.5) (same 2.8:1 aspect), so the scale into the 6.10-in column becomes 0.87x and the 11/12/14 pt rcParams sizes print at ~9.6/10.4/12.2 pt. Because 7 in x 200 dpi = 1400 px over 6.10 in is only 230 ppi, pair this with "figure.dpi": 300 and "savefig.dpi": 300 in cell 3 (7 in x 300 dpi = 2100 px = 344 ppi, above the 300 ppi floor). If the 14-in canvas must be kept, instead override per-figure: ax.set_title(..., fontsize=30), ax.set_xlabel(...)/ax.set_ylabel(..., fontsize=26), ax.tick_params(labelsize=24), which land at ~13/11.5/10.6 pt after the 0.441x reduction. Re-export via scripts/export_thesis_results.py and refresh thesis/latex/figures/nb01-fig03-arrival-rate.png.

---

### `NB01-Figure04.png` — resolution

**Ne:** Cell 21 of notebooks/en/01_data_overview.ipynb hard-codes `fontsize=8` on `ax.set_xticklabels(...)`, overriding the notebook-wide `"xtick.labelsize": 11` from cell 3, so NB01-Figure04 carries 8 pt x tick labels beside 11 pt y tick labels in the same axes (verified by pixel-exact control render: rotated "8" = 16x15 px = 8 pt; y-tick "0" = 15x22 px = 11 pt). The claim's scaling estimates are wrong: the figure is already placed in the thesis at thesis/latex/chapters/3.dataset_and_workload.tex:66 as `\includegraphics[width=0.75\textwidth]{figures/nb01-fig04-gpu-demand.png}` (file is md5-identical to the export). With textwidth = 15.5 cm = 6.10 in, that is 4.58 in from a 9.84-in figure, a 0.465 scale, so the x ticks print at about 3.7 pt (not 4.9 pt) against y ticks at about 5.1 pt; the 0.48\textwidth / 2.4 pt scenario does not apply to this figure. Minor measurement corrections: the y-tick "0" box is 15x22 px not 16x22, the rotated "1" is 17x14 not 15x16, and the ink gaps are 9 px ("0.25"-"0.3") and 15 px ("0.5"-"0.6") rather than 8 and 14.

**Kanıt:** In the exported PNG the y tick digits ("35000" bbox 92x22 px, "30000" 92x22 px, "0" 16x22 px) are 22 px tall, but a rotated x tick digit occupies only a 15x16 px box ("8" at x=1835-1850, "1" at x=1353-1369). Rendering the same glyph with the notebook's own rcParams at 200 dpi gives 15x16 px for fontsize=8 and 22x22 px for fontsize=11, so the x ticks are 8 pt and the y ticks 11 pt in the same axes. Source: cell 21 of notebooks/en/01_data_overview.ipynb, `ax.set_xticklabels([f"{v:g}" for v in demand_counts.index], rotation=45, fontsize=8)`, which overrides the cell-3 setting `"xtick.labelsize": 11` whose own comment says "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column (thesis.cls: a4paper, 3.5cm/2cm margins)." The sibling figure from the same notebook, NB01-Figure02.png, keeps 11 pt x ticks. The 8 pt setting is already crowding the axis: the ink gap between "0.25" (columns 1020-1058) and "0.3" (1067-1095) is 8 px, and between "0.5" (1185-1213) and "0.6" (1228-1256) is 14 px.

**Düzeltme:** Delete `fontsize=8` so the ticks inherit `xtick.labelsize=11`, and thin the labelled set at the same time (at 11 pt each label is 1.375x wider, so "0.25"/"0.3" would overlap by about 5 px). Keep all 19 gridline positions but label only the values carrying visible mass: _show = {"0.01", "0.05", "0.1", "0.25", "0.5", "1", "2", "5", "8"} ax.set_xticks(list(demand_counts.index)) ax.set_xticklabels([f"{v:g}" if f"{v:g}" in _show else "" for v in demand_counts.index], rotation=45)

---

### `NB01-Figure05.png` — data-integrity

**Ne:** The figure omits 17,745 of 82,183 consecutive job pairs (21.6%) — every zero-second (same-second) inter-arrival — with no disclosure on any reader-facing path: not in the PNG (verified bare at full resolution), not in notebook caption cell 24, and not in the printed thesis, whose LaTeX caption and body text in chapters/3.dataset_and_workload.tex likewise never mention it (the embedded figure is byte-identical to the export per md5). The exclusion is technically unavoidable on a log axis and reflects the trace's integer-second timestamp resolution; the defect is purely the missing disclosure, which the author's own code comment shows was intended ("reported separately rather than silently dropped from the figure") but which survives only in notebook stdout. The claim's reasoning that this "weakens the caption's claim that the workload is bursty" is directionally inverted: dropping the burstiest pairs makes the figure understate burstiness, so the bursty/non-Poisson conclusion holds a fortiori. The actual reader-facing harm is twofold — a figure presented as the complete inter-arrival distribution is missing a fifth of its mass with no n= or note, and the LaTeX caption's claim that "The peak occurs at the short time interval of 1-3 s" is made while the true modal inter-arrival value, 0 s with 17,745 pairs, is 47% larger than the tallest bar shown (1 s, 12,110) and entirely invisible. Fix: add one clause to the caption, e.g. "17,745 pairs (21.6%) with zero inter-arrival are not shown, as a log axis cannot render zero."

**Kanıt:** Cell 23: `inter_arrivals = inter_arrivals_all[inter_arrivals_all > 0]`. The cell's stdout, which does NOT reach the exported PNG, reads: "[Inter-arrival] 21.6% of consecutive job pairs arrived in the same second (excluded above; not visualizable on a log time axis)." In the PNG there is no annotation, footnote, or n= text anywhere — the plot area is bare apart from bars and gridlines, the title is only "Inter-Arrival Time Distribution", and the x label is only "Inter-Arrival Time (seconds, log scale)". Caption cell 24 likewise never mentions the exclusion. This directly weakens the caption's own claim that the workload is bursty, since the burstiest 21.6% is the part removed.

**Düzeltme:** Surface the exclusion inside the figure. Either put it in the axis label — `ax.set_xlabel("Inter-Arrival Time (seconds, log scale; 21.6% same-second pairs excluded)")` — or add `ax.text(0.98, 0.95, f"{_zero_frac:.1%} of pairs arrived in the same second (0 s, off log axis)", transform=ax.transAxes, ha="right", va="top", fontsize=9, color="0.35")`, using the computed `_zero_frac` so the number cannot go stale. Add the same sentence to the cell-24 caption.

---

### `NB01-Figure06.png` — overlap

**Ne:** The blank masked block (trace day 7, hours 16-23) is crossed by 8 vertical and 1 horizontal stray grey gridlines from the seaborn `whitegrid` background, and they sit at cell CENTRES, misaligned with the heatmap's own white cell separators, so the empty corner reads as a broken cross-hatch of content.

**Kanıt:** Measured on the PNG: grey RGB (204,204,204) lines. Vertical lines centred at x = 1565.4, 1653.0, 1740.5, 1828.1, 1915.6, 2003.1, 2090.7, 2178.2 px -- exactly the centres of the hour-16..23 columns (the x-tick positions). The heatmap's own cell boundaries are at x = 1521.7, 1609.2, 1696.8, 1784.3, 1871.8, 1959.4, 2046.9, 2134.5, 2222.0 px, so no grey line coincides with any cell edge. One horizontal grey line runs at y = 800 px from the masked-region edge to the right edge of the axes (x=2221); row 7 spans y = 752-848, so y=800 is its exact centre (the y-tick position), i.e. the line bisects the row rather than bounding it. Visible in the bottom-right of the figure under the '16'-'23' tick labels.

**Düzeltme:** In notebooks/en/01_data_overview.ipynb cell idx 25, add `ax.grid(False)` immediately after the `sns.heatmap(...)` call. The gridlines are hidden behind the opaque cells everywhere else, so this only affects the masked block.

---

### `NB01-Figure06.png` — units

**Ne:** The finding is real and slightly understated. The x axis of NB01-Figure06 is labelled "Hour of Day" (ticks 0-23) and the title says "Hour-of-Day", while the y axis of the same figure carries the corrected trace-relative label "Trace Day Index". The plotted variable is hour_of_day = arrival_time.dt.hour where arrival_time = pd.to_datetime(submit_time, unit='s') and the raw trace's submit_time is a trace-relative offset beginning at exactly 0 (max 1,043,390 s), so arrival_time spans 1970-01-01 00:00:03 to 1970-01-08 15:51:32 and the axis is really (seconds since trace start) mod 24 h with unknown phase -- bin 0 is the trace's first hour, not midnight. Beyond the label inconsistency, the figure's caption in notebooks/en/01_data_overview.ipynb makes an affirmative factual error, stating the hour values "correspond to UTC time"; the Figure 3 note likewise calls arrival_time "a valid UTC timestamp". Both are false given submit_time starts at 0, and they instruct the reader to read the bins as real clock hours. The fix therefore spans three places, not one: relabel the x axis and title to a trace-relative form (e.g. "Trace Hour Index (24 h phase, unknown wall-clock alignment)"), delete or rewrite the "Timezone note"/UTC claims in the Figure 6 and Figure 3 captions, and update the stale cross-reference at 01_data_overview.ipynb:802 which still calls this "Figure 6 (Day-of-Week x Hour-of-Day Heatmap)". Supporting evidence that the bins are phase-shifted: the hourly marginal peaks at bins 11 and 0 and troughs at 22-23, 2-3 and 8, which under a wall-clock reading would absurdly make midnight the second-busiest hour and 8am one of the quietest.

**Kanıt:** Ran the notebook's own pipeline on the real data: `job_df['arrival_time'].min()` = 1970-01-01 00:00:03, i.e. `submit_time` is a normalised offset from the trace start, not a real timestamp, so `.dt.hour` is (seconds since trace start) mod 24 h with unknown phase. The project's own docstring at src/feature_engineering.py:176-180 says the same: hour_of_day is "a 24-hour cyclical phase of unknown absolute alignment -- still a meaningful diurnal-rhythm signal, just not '9am' in any verifiable sense." The rendered figure nevertheless reads 'Hour of Day' on the x axis and 'Job Arrival Heatmap - Trace Day x Hour-of-Day' in the title, alongside the corrected y label 'Trace Day Index'.

**Düzeltme:** In cell idx 25 change `ax.set_xlabel("Hour of Day")` to `ax.set_xlabel("Hour index within trace day (24 h cyclical phase; absolute clock alignment unknown)")` and `ax.set_title("Job Arrival Heatmap - Trace Day x Hour Index")`. Delete the 'correspond to UTC time' sentence from the markdown cell idx 26 below the figure, and align the caption at thesis/latex/chapters/3.dataset_and_workload.tex:109 to the same wording.

---

### `NB01-Figure06.png` — resolution

**Ne:** At the width the thesis actually includes this figure (0.75\textwidth), every piece of in-figure text prints between 3.9 pt and 5.0 pt, well under the 6-8 pt floor; tick labels are 3.9 pt.

**Kanıt:** PNG is 2560x968 px at 200 dpi = 12.80 in wide. thesis/latex/chapters/3.dataset_and_workload.tex:108 reads `\includegraphics[width=0.75\textwidth]{figures/nb01-fig06-arrival-heatmap.png}`; with \textwidth = 6.10 in the printed width is 4.58 in, a scale of 0.358. Font sizes set in cell idx 3 are xtick/ytick 11 pt, axes.labelsize 12 pt, axes.titlesize 14 pt, so on the page they become 3.9 pt, 4.3 pt and 5.0 pt. Measured directly: the x-tick digit '0' has a 22 px cap height = 0.11 in at native size = 0.039 in (2.8 pt cap height) on the printed page. Even at full 1.0\textwidth (6.10 in) the tick labels are only 5.2 pt.

**Düzeltme:** Shrink the canvas toward printed size rather than scaling down: in cell idx 25 change `plt.subplots(figsize=(14, 5))` to `figsize=(6.5, 2.8)`, keep savefig.dpi at 400, and change the include at 3.dataset_and_workload.tex:108 to `width=\textwidth`. Re-run and re-export.

---

### `NB02-Figure01.png` — units

**Ne:** Real but MINOR and export-only, not major. NB02-Figure01.png is not a printed-thesis figure: thesis/latex/figures/ contains no nb02-fig01 (the only NB02 figure carried into the thesis is nb02-fig03-gpu-vs-runtime.png), so no reader of the printed thesis encounters this unitless axis standing alone. The same unitless label "log₁₀(Runtime + 1)" does reach print, but only as the right panel of nb01-fig01-runtime-dist.png (byte-identical, md5 b8f3eed9c83b31ad1be956f754062905, to the NB01-Figure01.png export), where the unit is recoverable twice over: the companion left panel is labelled "Runtime (seconds)" and the caption at thesis/latex/chapters/3.dataset_and_workload.tex:45 reads "Distribution of job runtime (in seconds)". The defect is therefore a labelling-consistency issue in the export set — worth fixing because the figure is uninterpretable if ever promoted or shown standalone, and because chapter 3 names the same variable four different ways ("Runtime (seconds)", "log₁₀(Runtime + 1)", "Runtime (seconds, log scale)", "job_runtime"). Fix: change notebooks/en/02_workload_analysis.ipynb cell 10 to ax.set_xlabel("log₁₀(Runtime [s] + 1)") and apply the same to the nb01 log panel.

**Kanıt:** Bottom-centre x-axis label reads exactly "log₁₀(Runtime + 1)". The only other text in the frame is the title "Job Runtime Distribution (Log₁₀ Scale)" and the y label "Frequency" — neither names a unit. The peak bar sits at x ≈ 2.4, which could be read as 250 s, 250 min or 250 h; the notebook markdown under the cell interprets it as "~250 seconds", a fact the figure itself never states. (In the sibling NB01-Figure01 the unit is only recoverable because its left panel is labelled "Runtime (seconds)"; this figure stands alone.)

**Düzeltme:** In notebooks/en/02_workload_analysis.ipynb, cell 10 (the "# ── 4. Runtime histogram" cell), change ax.set_xlabel("log₁₀(Runtime + 1)") to ax.set_xlabel("log₁₀(Runtime [s] + 1)") — matching the "Runtime (seconds)" wording already used in NB01-Figure01.

---

### `NB02-Figure02.png` — resolution

**Ne:** NB02-Figure02.png is exported 13.835 in wide (2767x968 px at 200 dpi) from figsize=(14,5) in 02_workload_analysis.ipynb cell 12, the widest figure in that notebook. The file itself is deliberately not shipped to the thesis (scripts/export_thesis_results.py:79-80 excludes NB02 figures 1-2 as duplicates of NB01 charts), but its geometrically identical twin nb01-fig03-arrival-rate.png — same 2767x968 at 200 dpi, same figsize=(14,5) and rcParams, title differing only by the word "the" — is placed at width=\textwidth in thesis/latex/chapters/3.dataset_and_workload.tex:88. With \textwidth = 15.5 cm = 6.102 in (thesis.cls a4paper, 3.5cm/2cm margins, [msc] option active), the scale is 0.441, printing 11 pt tick labels at 4.85 pt, 12 pt axis titles at 5.29 pt and the 14 pt title at 6.18 pt; the "1400" tick has a cap height of 1.23 mm on paper. This is systemic rather than unique to this chart — nb04-fig01-model-comparison.png (3586 px = 17.93 in at \textwidth, scale 0.34) prints its ticks at roughly 3.7 pt.

**Kanıt:** PNG is 2767x968 px at 200 dpi = 13.835 x 4.84 in. Measured glyph boxes in the PNG: y-tick "1400" is 22 px tall (=7.9 pt cap height at 200 dpi, i.e. the 11 pt rcParam), title "Hourly Job Arrival Rate Over Trace Duration" is 835 px wide x 38 px tall, xlabel "Elapsed Time (days)" is 333x33 px. thesis.cls is a4paper with 3.5cm/2cm margins => \textwidth = 15.5 cm = 6.102 in, so scale = 6.102/13.835 = 0.441: 11 pt -> 4.85 pt, 12 pt -> 5.29 pt, 14 pt -> 6.17 pt (cap height of "1400" would be 1.23 mm on paper). The identical chart is placed at width=\textwidth in thesis/latex/chapters/3.dataset_and_workload.tex:88. Cell 3's own rcParams comment claims "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column", but cell 12 overrides the 12-in default with figsize=(14, 5), which is the widest figure in this notebook (NB02-Figure01 is 11.84 in, NB02-Figure03 is 9.84 in) and therefore shrinks the most.

**Düzeltme:** In notebooks/en/02_workload_analysis.ipynb cell 12 change `fig, ax = plt.subplots(figsize=(14, 5))` to `fig, ax = plt.subplots(figsize=(7.5, 2.8))` so the exported PNG lands near the 6.1-inch column and the existing 11/12/14 pt fonts print at ~8.9/9.8/11.4 pt. (Apply the same change to the twin cell in notebooks/en/01_data_overview.ipynb cell 19, which produces the figure actually placed in the thesis.)

---

### `NB02-Figure03.png` — data-integrity

**Ne:** The caption text is stale relative to the figure, not merely imprecise. Confirmed facts: (1) the underlying table has zero jobs with gpu_demand == 0 (min 0.01, median 0.5, n = 82,184), so no 0-GPU population exists to cluster at; (2) the figure's x axis is logarithmic (10^-2 to 10^1) and therefore cannot render 0 at all; (3) the densest columns measured from the PNG are x=1.00 (732 marker px), x=0.25 (620), x=0.50 (604), x=0.10 (496) — i.e. the second and third heaviest columns are the fractional sharing levels 0.25 (20,615 jobs) and 0.50 (14,001 jobs), which neither caption names. A reader comparing the caption to the plot will map the phrase "0 GPU" onto the fractional columns at 0.05-0.5 and conclude those are zero-GPU jobs, when they are fractional GPU-sharing requests — 52.5% of the workload. The correct statement is "jobs demanding a fractional share of a GPU (0.05-0.5) or exactly 1 GPU". One inaccuracy in the claimed evidence: 216 jobs ARE plotted at exactly x=0.01 (the leftmost blue pixel column is 229, the 10^-2 gridline); nothing is plotted to the LEFT of 0.01. The cell-14 code comment ("a linear x axis squeezed every sub-1-GPU level into one unresolvable smear at the origin") confirms the caption was written against the superseded linear-axis version and not updated. Same defect in both EN cell 15 and TR cell 15 of notebooks/{en/02_workload_analysis.ipynb, tr/02_is_yuku_analizi.ipynb}.

**Kanıt:** Caption (EN cell 15): "The visual dominantly clusters around jobs demanding exactly **0 or 1 GPU**"; TR cell 15: "**0 ve 1 GPU talep eden görevlerde** toplanmıştır". In the image the dense columns sit at x = 0.05, 0.10, 0.15, 0.20, 0.25, 0.50, 1.00, 2.00 (measured pixel column centres 626, 794, 894, 963, 1017, 1186, 1354, 1523 against the 10^-1 gridline at col 794 and 10^0 at col 1353). Column density peaks: x=1.00 (801 marker pixels), x=0.25 (674), x=0.50 (662), x=0.10 (592). Nothing is plotted at or left of x=0.01, and the code comment in cell 14 states "median 0.5, min 0.01", so a 0-GPU population does not exist.

**Düzeltme:** Rewrite the sentence in both captions to "clusters at the fractional sharing levels 0.1, 0.25 and 0.5 GPU and at exactly 1 GPU" (TR: "0,1 / 0,25 / 0,5 GPU kesirli paylaşım seviyeleri ile tam 1 GPU talep eden görevlerde").

---

### `NB02-Figure03.png` — data-integrity

**Ne:** The claim's substance holds but its pixel citation and its "highest point in the figure" phrasing are slightly off. Accurate statement: in NB02-Figure03.png the 1-GPU column spans roughly 6 s to 5.98x10^5 s (~6.9 days), not the "10^1 to 10^5 seconds ... up to 300,000 seconds (~3.5 days)" claimed by EN/TR cell 15. The topmost 1-GPU marker is at image rows 109-113, cols 1356-1361 (~0.78 decades above the 10^5 gridline, = 598,415 s in the data); rows 117-124 / cols 1351-1357 cited in the claim are the second-highest 1-GPU marker (~5.3x10^5 s). The single highest point in the whole figure is not a 1-GPU job but a 2-GPU job at 599,445 s (rows 108-113, col ~1522). The same caption paragraph is stale in two further respects visible in the same figure: it calls the plot a "5,000-Job Sample Baseline" although cell 14 plots all 82,184 jobs, and it places 6- and 8-GPU jobs "around the 10^4 second mark" although the figure shows a 6-GPU point at 426,170 s (rows 134-137, col ~1789) and an 8-GPU point at 543,132 s (rows 116-124, col ~1862).

**Kanıt:** Caption (EN cell 15): "fluctuating freely between 10¹ seconds and a sprawling 10⁵ seconds" and "linger relentlessly for up to 300,000 seconds (spanning ~3.5 days)"; TR cell 15: "300.000 saniye de (yaklaşık 3.5 gün)". In the PNG the topmost marker pixels are at image row 119, columns 1353-1356 (x = 1.00-1.01); with the 10^5 gridline at row 245 and one decade = 171.8 px this maps to ~5.4x10^5 s. The code comment in cell 14 gives the full-data maximum as "599,445 s". The 1-GPU column also runs down to ~6 s, below the 10^1 floor the caption quotes.

**Düzeltme:** Change "up to 300,000 seconds (spanning ~3.5 days)" to "up to 599,445 seconds (~6.9 days)" and "between 10¹ seconds and a sprawling 10⁵ seconds" to "from under 10 seconds to nearly 10⁶ seconds", in both the EN and TR captions.

---

### `NB03-Figure01.png` — resolution

**Ne:** The defect stands as stated; two supporting details are slightly off. (1) Downsampling NB03-Figure01.png to a 1831 px / 6.1 in / 300 dpi raster leaves the y tick digits about 13-14 px tall, not "roughly 9 px" -- the native cap height is 22 px at 200 dpi, and 22 x (1831/2968) = 13.6 px. The font-size arithmetic (4.52 / 4.93 / 5.75 pt) is unaffected and correct. (2) It is not quite the widest export outside the NB04 group: mae_spearman_vs_jct_gain_32gpu.png and _256gpu.png are 4461 px at 300 dpi = 14.87 in, marginally wider -- though neither appears in any \includegraphics in the LaTeX source, so among figures actually placed in the thesis the claim's ranking is right. Also worth stating explicitly rather than as an assumption: the figure really is placed at width=\textwidth (thesis/latex/chapters/3.dataset_and_workload.tex:168), so the 0.411x reduction is what the printed thesis actually does; the parenthetical "at 0.48\textwidth: 2.2 / 2.4 / 2.8 pt" is a hypothetical that does not apply to this figure.

**Kanıt:** The exported PNG measures 2968x1568 px; with "savefig.dpi": 200 from cell 3 that is 14.84 x 7.84 in, matching cell 15's figsize=(15, 8). Placing 14.84 in into a 6.1 in column is a 0.411x reduction, so the rcParams sizes "xtick.labelsize": 11 -> 4.52 pt, "axes.labelsize": 12 -> 4.93 pt, "axes.titlesize": 14 -> 5.75 pt (at 0.48\textwidth: 2.2 / 2.4 / 2.8 pt). Downsampling the PNG to a true 6.1 in / 300 dpi raster (1830 px) confirms the y tick digits collapse to roughly 9 px tall. It is the widest figure in the export set except the NB04 group; NB03-Figure02.png next to it is only 1821 px wide and keeps its ticks at 7.4 pt. The comment in cell 3, "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column", was written for the "figure.figsize": (12, 6) default that cell 15 overrides.

**Düzeltme:** In /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/notebooks/en/03_feature_engineering.ipynb cell 15, change fig, axes = plt.subplots(2, 1, figsize=(15, 8), sharex=True) to figsize=(9, 5). At 0.678x that gives 7.5 pt ticks, 8.1 pt axis titles and 9.5 pt panel titles, and it aligns this figure's on-page text size with NB03-Figure02. While there, bump lw=0.8 to lw=1.0 on both plot() calls so the trace does not fall to a 0.5 pt hairline after the reduction.

---

### `NB03-Figure02.png` — data-integrity

**Ne:** The Figure 2 caption in cell 22 of notebooks/en/03_feature_engineering.ipynb contains TWO numeric mismatches against NB03-Figure02.png, not one. (a) It states the Runtime-vs-gpu_demand Pearson r is 0.05; the figure cell and the cell 21 printout (gpu_demand 0.064362) both give 0.06. (b) It states the engineered cluster-utilization features "tightly correlate with each other (0.83)"; the figure's cluster-load block shows 0.88 for Background CPU load x Background GPU load, plus 0.89 and 0.82 for the Background-job-count pairs - no 0.83 appears anywhere in the matrix. The 0.08 for num_cpu (0.080097) is the only one of the three quoted coefficients that is correct. Both wrong numbers should be corrected in the same caption edit.

**Kanıt:** In the image, row "GPU demand (GPUs)" x column "Runtime (s)" (the top-left populated cell) reads "0.06". The caption in notebook cell 22 reads: "requesting more CPUs (`num_cpu`) or GPUs (`gpu_demand`) linearly dictates a longer execution duration (`job_runtime`), the actual Pearson correlation coefficients brutally betray this assumption, yielding minuscule scores of **0.08 and 0.05** respectively." The printed ground truth in cell 21's stdout is `gpu_demand 0.064362`, which rounds to 0.06, not 0.05. The 0.08 for num_cpu (0.080097) is correct; only the second number is wrong.

**Düzeltme:** In notebook cell 22 (the markdown "Figure 2" caption), change "**0.08 and 0.05**" to "**0.08 and 0.06**".

---

### `NB04-Figure01.png` — colour

**Ne:** Four non-learned reference baselines (Profile Median, Per-User Median, Constant Median, Constant Zero) are plotted in the same darkorange (#DF8920) as the trained categorical models, and the figure's only key — titled "Feature set" — therefore claims all four use the categorical feature set. Strictly, two of the four do key on categorical fields (Per-User Median groups by `user`; Profile Median groups by user/num_inst/num_cpu/gpu_demand/gpu_type), so orange is arguably defensible for those; the outright false cases are Constant Median (single global training median, no grouping) and Constant Zero (predicts 0 for every job, regardless of features), which consume no features at all yet are colour-coded as categorical-feature models. The broader reader-facing problem is that none of the four is a learned model and the figure gives no cue that they are baselines — the "(baseline)" suffix used in the Experiment B table (cell 47) is dropped here — while the accompanying caption reasons from bar colour to claim a "Categorical Boost" effect.

**Kanıt:** In both panels the bars labelled "Constant Zero", "Constant Median", "Profile Median" and "Per-User Median" are the same darkorange (#DF8920) as "XGBoost (One-Hot)" and "LightGBM (Native Categorical)". The only legend, titled "Feature set", maps that orange to "Categorical". Source: cell 91 assigns "Track": "Categorical" to the Per-User Median, Profile Median, Constant Median and Constant Zero rows, and cell 93 colours by Track with palette={"Numeric": "steelblue", "Categorical": "darkorange"}.

**Düzeltme:** In cell 91 give the four baseline rows their own track, e.g. "Track": "Baseline", and in cell 93 extend the palette to palette={"Numeric": "steelblue", "Categorical": "darkorange", "Baseline": "0.6"} with a matching third legend handle, so the grey bars read as reference lines rather than as a third modelling approach.

---

### `NB04-Figure01.png` — data-integrity

**Ne:** The defect is real but slightly overstated in one respect: XGB (One-Hot) IS legitimately the benchmark for variance explanation — it tops the R2 panel at 0.18. Only the "error minimization" half of the cell-94 caption is contradicted by the figure, where XGB (One-Hot) ranks 8th of 24 on MAE (6053.96 s) behind Profile Median (4389.67 s) and even behind the Constant Median and Constant Zero baselines. Separately, the cell-92 Table 7 prose quotes MAE 3389.26 and R2 0.51, values that match neither the figure nor exported Table NB04_Table12.html (6053.96 and 0.18); the identical stale figures also appear in cell 48's Table 2 caption (adding RMSE 11375.19 vs the actual 14056.63), and cell 92 further says "19 evaluated configurations" where the figure and table both show 24 rows.

**Kanıt:** Left panel, top row: "Profile Median" at MAE 4389.67 s; "XGBoost (One-Hot)" is the 8th row at 6053.96 s, behind LightGBM (Native Categorical) 4973.16, Per-User Median 5191.50, LightGBM (One-Hot) 5305.43, LightGBM (Numeric) 5811.00, Constant Median 5818.25 and Constant Zero 6029.75 (values confirmed in NB04_Table12.html). The caption in cell 94 states: "specifically **XGB (One-Hot)**, which establishes the benchmark for both error minimization and variance explanation." Cell 92 states XGBoost (One-Hot) achieved "the lowest **MAE (3389.26)** and the highest **R² score (0.51)**" — but the R² panel maxes out at 0.18 for that model and the x-axis ends at 0.20, so 0.51 cannot be on this chart and 3389.26 s is not the value drawn.

**Düzeltme:** Either correct the prose in cells 92 and 94 to match the plotted numbers (XGBoost (One-Hot) is best on R² = 0.18 only, while the Profile Median baseline has the lowest MAE at 4389.67 s), or regenerate the figure from the run the 3389.26 / 0.51 numbers came from. Do not ship the figure and the caption disagreeing on which model wins.

---

### `NB04-Figure02.png` — resolution

**Ne:** Cell 95 of notebooks/en/04_runtime_prediction_models.ipynb renders this three-panel figure at figsize=(18, 6), exported at 200 dpi as a 3568x1180 px / 17.84 in wide PNG. thesis/latex/chapters/6.results_and_discussion.tex:87 includes it at width=\textwidth, and thesis.cls (a4paper, 3.5 cm left / 2 cm right) gives a 15.5 cm = 6.102 in column, a 0.342x reduction. In print the y-tick feature names (11 pt) become 3.8 pt, the axis unit labels (12 pt) 4.1 pt, and the panel titles and suptitle (14 pt) 4.8 pt - illegible, and the feature names are the figure's entire payload. Correction to the claim's framing: reverting to the notebook's own rcParams default of (12, 6) would NOT restore the stated contract either, since 11 pt at a 0.51x reduction still prints at ~5.6 pt; the figure needs to be authored near the 6.1 in target width (or split/stacked), not merely narrowed to 12 in. The same 18-inch override affects NB04-Figure01 (17.93 in) and NB04-Figure03 (17.84 in), which are also included at \textwidth.

**Kanıt:** The PNG is 3568x1180 px with embedded dpi 200, i.e. 17.8 in wide. The y-tick label "gpu_demand" measures a 30 px ascender-to-descender span (rows 220-249), which at 200 dpi is 11 pt — matching the notebook's rcParams "xtick.labelsize": 11 / "ytick.labelsize": 11. Scaling 17.8 in down to a 6.1 in column is 0.34x, giving 11 x 0.34 = 3.7 pt tick labels, 12 x 0.34 = 4.1 pt axis labels ("Mean decrease in impurity (sums to 1)"), and 14 x 0.34 = 4.7 pt for both the panel titles and the suptitle "Feature Importance — All 9 Numeric Features by Model". The same rcParams block states "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column" and sets "figure.figsize": (12, 6); this cell is the one that widens it to 18, breaking that contract by 1.5x.

**Düzeltme:** In cell 95, change fig, axes = plt.subplots(1, 3, figsize=(18, 6)) to figsize=(12, 4.5) — the notebook's own declared 12-inch design width, which restores tick text to about 5.6 pt at \textwidth — and swap plt.tight_layout() for constrained_layout=True on the subplots call to keep the three panels from crowding at the narrower width.

---

### `NB04-Figure03.png` — resolution

**Ne:** The legend hard-codes fontsize=8, making 'Perfect prediction' the smallest text in the figure at 2.5 pt printed, and overriding the notebook's own legend.fontsize=11 setting.

**Kanıt:** Cell index 97 line `ax.legend(fontsize=8)`, against cell 5 `"legend.fontsize": 11`. Measured legend text band in panel 1 (Random Forest): rows 173-192 = 20 px tall -> 20 x 0.1231 = 2.46 pt printed; nominal 8 pt x 0.342 = 2.74 pt. The same 8 pt legend is repeated in all three panels.

**Düzeltme:** In cell index 97 replace `ax.legend(fontsize=8)` with `ax.legend()` so it inherits legend.fontsize=11 from the notebook rcParams (7.5 pt printed once the canvas fix above is applied).

---

### `NB04-Figure03.png` — data-integrity

**Ne:** The defect is real but the fault lies with the table, not the figure. NB04-Figure03.png is the correct, current artefact: it faithfully plots the retrained Experiment A models, and the LightGBM panel's flat ~533-739 s band is genuine -- results/checkpoints/exp_a_lgbm.json shows the tuned model collapsed to n_estimators_effective = 1, making it a near-constant predictor whose test metrics (MAE 5811.00, MdAE 536.92, R^2 -0.12) are statistically indistinguishable from the ConstantMedian baseline in the same notebook (MAE 5818.25, R^2 -0.1231, constant prediction 568.00 s). What is wrong is Table tab:predresults: the Experiment A rows in thesis/latex/chapters/6.results_and_discussion.tex:22-24 and the matching rows in thesis/latex/chapters/appendices.tex:53-55 still carry pre-retrain values (RF 4,316 / 0.27; XGB 4,808 / 0.26; LGBM 5,212 / 0.22) traceable to results/_backup_pre_retrain/checkpoints/exp_a_rf.json, whereas the current run gives RF 6,797 / 0.02, XGB 6,968 / 0.01, LGBM 5,811 / -0.12. All three Experiment A rows are stale, not just LightGBM, and the narrative at 6.results_and_discussion.tex:93 compounds the error by citing 'the modest R^2 values (0.22-0.27)' as if current, as does the Experiment A discussion at line 76 ('Random Forest, R^2 = 0.27'). The secondary evidentiary point that 'MedAE 536.92 s matches the plotted band exactly' is not a valid inference -- MedAE is a median absolute error, not a median prediction -- but it is not needed: the checkpoint file and the constant-median baseline confirm the conclusion directly. Fix: regenerate the Experiment A rows of both tables from results/checkpoints/exp_a_*.json and revise the surrounding text, which currently claims tree-based numeric models capture real signal when they do not beat a constant.

**Kanıt:** Right-hand panel 'LightGBM (Numeric)': the green cloud occupies image rows 665-687 only, between the 10^3 gridline (row 633) and the 10^2 gridline (row 831) -> predicted values 533-689 s, a 1.29x spread, while the x extent runs from 13.6 s to 9.03e4 s. A predictor pinned at ~600 s when the target mean is several thousand seconds has R^2 <= 0 by construction. thesis/latex/chapters/6.results_and_discussion.tex:24 states 'A & LightGBM (Numeric) & 5{,}212 & 2{,}259 & 14{,}315 & 20.66 & 0.22'; the exported source table results/figures/thesis_export/html/NB04_Table01.html instead reads 'LightGBM | 5811.00 | 536.92 | 16486.03 | 332.53 | -0.12' — MedAE 536.92 s matches the plotted band exactly, and R^2 = -0.12 matches the flat cloud. The same split applies to Random Forest (figure band 2.88e3-2.57e4 s; NB04_Table01 '6796.98 ... 0.02' vs tex:22 '4,316 ... 0.27').

**Düzeltme:** The figure and Table 6.1 come from different runs. In cell index 91 write df_all to results/analysis/prediction_metrics.csv and generate the tab:predresults body with df_all.to_latex() from that same object, then re-export this figure from the identical run so the panel and the table cannot disagree.

---

### `NB04-Figure03.png` — data-integrity

**Ne:** The defect is real but the claim's numbers are off. Accurate statement: line 104 of thesis/latex/chapters/6.results_and_discussion.tex directs the reader to the 'upper right hand portion' of Figure 6.x (NB04-Figure03.png) as evidence that all three models underestimate long jobs. Measured marker-pixel counts in the region actual > 10^4 AND predicted > 10^4 are: LightGBM 0 px (0.00% — its maximum prediction is ~750 s, so the top half of that panel is entirely blank), XGBoost ~284 px (0.83%, essentially one stray marker sitting on the diagonal at actual ~2.8e4 / predicted ~2.9e4 — not 14 px with max 1.42e4 as claimed), Random Forest ~1,736 px (3.06% — a sparse scatter of roughly 40 markers, not 27 px, so 'blank' overstates it for this panel). The claimed 'LightGBM highest point 739 s' is right (I measure ~748 s within pixel resolution). The substantive error is twofold: (1) the cited region holds a negligible fraction of the sample rather than 'a large number of jobs', and (2) the direction is inverted — points above the diagonal at high actual would indicate OVERestimation, whereas the underestimation actually shown lies below the diagonal in the lower right, as the flat prediction bands at ~3,850 s (RF), ~4,590 s (XGB) and ~579 s (LGBM) running out to actual ~10^5 s. The sentence should point to the lower right / below the diagonal.

**Kanıt:** thesis/latex/chapters/6.results_and_discussion.tex:104: 'Figure~\ref{fig:pred-vs-actual} also demonstrates this through the large number of jobs with heavy-tails present in its upper right hand portion.' Pixel count of coloured markers with actual > 10^4 AND predicted > 10^4: LightGBM panel 0 px (its highest point is 739 s), XGBoost panel 14 px (max predicted 1.42e4 s), Random Forest panel 27 px. All three panels are visibly empty above the diagonal on the right-hand side; the dense bands sit far below the red dashed line there.

**Düzeltme:** Rewrite the sentence in 6.results_and_discussion.tex:104 to point at the correct region, e.g. '...through the long-runtime jobs in the lower right of each panel, which fall one to two orders of magnitude below the dashed identity line.'

---

### `NB04-Figure04.png` — resolution

**Ne:** Cell index 99 of notebooks/en/04_runtime_prediction_models.ipynb calls ax.legend(fontsize=8) on a figsize=(16,5) figure, overriding the notebook's own rcParams legend.fontsize of 11. The exported PNG (results/figures/thesis_export/png/NB04-Figure04.png, byte-identical to thesis/latex/figures/nb04-fig04-residuals.png) is 3167 px at 200 dpi = 15.835 in wide, and thesis/latex/chapters/6.results_and_discussion.tex line 108 places it at width=\textwidth. With \documentclass[msc]{thesis} the thesis.cls geometry (a4paper, left 3.5cm, right 2cm) gives \textwidth = 15.5 cm = 6.102 in, a scale factor of 0.385, so the legend renders at about 3.1 pt on the page -- confirmed by measurement (16 px cap height of 3167 px total). The mean residual values (-951 s, -1043 s, -5445 s) appear nowhere in the thesis body text or caption, so the illegible legend is their only statement, and it is also the only key distinguishing the gold mean line from the adjacent red zero line. Two corrections to the claimed secondary numbers: the tick labels use xtick/ytick.labelsize 11, not font.size 12, so they land at 4.24 pt rather than 4.62 pt (4.62 pt is correct for the axis labels at 12); and the panel titles use axes.titlesize 14, so they land at 5.39 pt -- the same as the suptitle -- not 5.55 pt.

**Kanıt:** Cell 99 calls ax.legend(fontsize=8), overriding the notebook's own rcParams legend.fontsize of 11. The PNG is 3167 px at 200 dpi = 15.835 in wide; thesis/latex/chapters/6.results_and_discussion.tex line 108 is \includegraphics[width=\textwidth]{figures/nb04-fig04-residuals.png}, so at a 6.1 in column the scale factor is 0.385. 8 pt x 0.385 = 3.08 pt for "Zero error" and "Mean = -951s"; ticks and axis labels at font.size 12 land at 4.62 pt, panel titles at 5.55 pt, the suptitle at 5.39 pt. Measured glyph heights confirm the ratio: legend text 16 px tall vs tick labels 22 px and the xlabel 30 px.

**Düzeltme:** Drop ax.legend(fontsize=8) so the rcParams value of 11 applies (or set fontsize=11), and reduce figsize from (16, 5) to roughly (9, 3.2) so the scale-down factor at \textwidth is ~0.68 instead of 0.385. That puts the legend near 7.5 pt and ticks near 8 pt on the page.

---

### `NB04-Figure04.png` — axis

**Ne:** The three panels plot the same quantity ("Frequency", same test set) side by side but each autoscales its own y axis (top ticks 6000 / 10000 / 12000), so all three modal bars are drawn at exactly the same physical height while representing counts that differ by 1.8x.

**Kanıt:** Measured from the PNG: the tallest bar in every panel has its top at the identical pixel row y=191. Calibrating each panel against its own gridlines (Random Forest: baseline y=865, 103.4 px per 1000; XGBoost: 127.6 px per 2000; LightGBM: 116.7 px per 2000) gives peaks of ~6,520, ~10,560 and ~11,550 jobs — three visually identical bars standing for very different counts. Cell 99 deliberately harmonises the x range with the comment "One shared x-range across the three panels: with independent ranges the eye reads three differently-scaled spreads as if they were comparable", but never applies the same reasoning to y.

**Düzeltme:** Add sharey=True to plt.subplots(1, 3, figsize=..., sharey=True), or after the loop compute _y_hi = max(ax.get_ylim()[1] for ax, _ in _res_axes) and call _ax.set_ylim(0, _y_hi) in the same loop that sets set_xlim. Then drop the redundant "Frequency" ylabel from panels 2 and 3.

---

### `NB04-Figure04.png` — data-integrity

**Ne:** The claim is accurate; one sub-point needs a small refinement. The caption's mean residual values (RF ~ 12.5ks, XGBoost ~ 1.7ks, LightGBM ~ 0.5ks) contradict the figure's own legend (RF -951s, XGBoost -1043s, LightGBM -5445s) in sign, in magnitude (RF ~13x too large, LightGBM ~11x too small), and in rank order (the caption makes RF the most biased and LightGBM the least; the figure shows the reverse). Refinement on the sign point: since residual = Predicted - Actual, the plotted negative means DO correctly denote systematic underestimation, so the word "underestimation" is the right direction for the figure. The defect is that the caption pairs that word with positive numbers, which would denote overestimation — the caption is internally inconsistent as well as inconsistent with the plot. Correct fix: restate the values as RF = -951s, XGBoost = -1043s, LightGBM = -5445s, and note that LightGBM carries the largest negative bias while RF and XGBoost are the least biased in the mean.

**Kanıt:** Notebook cell 100, the markdown caption directly beneath this figure, states "the mean residual values (Random Forest ≈ 12.5ks, XGBoost ≈ 1.7ks, LightGBM ≈ 0.5ks)". The figure's legends read "Mean = -951s", "Mean = -1043s" and "Mean = -5445s". Every number is wrong: RF is off by 13x, and the ranking is inverted — the caption makes Random Forest the most biased and LightGBM the least, whereas the figure makes LightGBM the most biased (-5445s, the only mean whose gold line is visibly separated from the red zero line, measured 42 px left of the 0k gridline = -5,468 s). The caption also calls positive values evidence of "systematic underestimation", while the plotted means are all negative.

**Düzeltme:** Replace the hard-coded numbers in the cell 100 markdown with the values the figure actually plots (Random Forest -951 s, XGBoost -1043 s, LightGBM -5445 s), correct the ordering claim so LightGBM is named as the most biased, and drop the "12.5ks / 1.7ks / 0.5ks" figures entirely. Better, have cell 99 print the three means so the caption can be copied from output rather than retyped.

---

### `NB04-Figure05.png` — axis

**Ne:** The defect is real but two details in the description need correcting. Source line numbers are wrong. In notebooks/en/04_runtime_prediction_models.ipynb (8,313 lines total, so lines 9492/9502/9515 do not exist) the actual lines are: 7916 `_err = dl_summary["mae_std"] if _has_std else None`, 7926 `ax.bar(x, dl_summary["mae"], yerr=_err, capsize=4,`, and 7939 `_n = int(dl_summary["n_seeds"].max())`. The code content quoted is otherwise accurate, and `_n` is confirmed dead (single occurrence in the file). "Nothing ... says over how many runs" overstates it slightly at thesis level. The seed protocol IS stated once, two chapters earlier, in thesis/latex/chapters/4.prediction_models.tex:171: "Each DL architecture's final refit is instead repeated over three random seeds (42, 1337, 2024); reported metrics are the mean across seeds, with the standard deviation and the specific seed-42 network's own score ... reported alongside it." So the accurate statement is: the figure draws ±1 standard deviation across 3 seeds, but neither the figure nor its caption identifies the whisker, and the accompanying Table 6.1 omits both the std and the seed count (and its DL MAE values are stale relative to the figure — e.g. C/CNN 6,454 in the table vs 7,040 in the figure). A reader must reach back to Chapter 4 and infer that the undefined whiskers are that SD rather than SEM or a CI. The intended in-figure "n = 3" annotation was written and dropped, evidenced by the unused `_n`.

**Kanıt:** Twelve capped error bars are drawn (e.g. the C CNN-LSTM "7230s" bar has a whisker spanning roughly 6580-7880 on the y axis). The only text in the image is the title "Deep Learning Models — Final Test MAE", the y label "MAE (seconds) ↓", the four legend entries, and the key "↓ lower is better". The LaTeX caption is only "Deep learning model performance across Experiments C, D, E, and F (MAE)." In the source, notebooks/en/04_runtime_prediction_models.ipynb line 9492 sets `_err = dl_summary["mae_std"]` (the standard deviation across seeds) and line 9502 passes `yerr=_err, capsize=4`. Line 9515 computes `_n = int(dl_summary["n_seeds"].max())` — which equals 3 — and then never uses `_n` anywhere, so the intended "n = 3 seeds" annotation was written and dropped.

**Düzeltme:** Use the already-computed `_n`: change line 9517 to `ax.set_title(f"Deep Learning Models — Final Test MAE\n(bars: mean over {_n} seeds; whiskers: ±1 SD)", pad=34)`, or add the same sentence to the \caption in thesis/latex/chapters/6.results_and_discussion.tex line 76.

---

### `NB04-Figure05.png` — resolution

**Ne:** NB04-Figure05 is authored 13.95 in wide (2790x1197 px at 200 dpi, confirmed both in results/figures/thesis_export/png/NB04-Figure05.png and in the copy LaTeX includes, thesis/latex/figures/nb04-fig05-dl-comparison.png) and placed at width=\textwidth in a 6.102-inch column (thesis.cls: report 12pt, A4, left 3.5 cm / right 2 cm), a 0.437x reduction. The generating cell is at lines 7924-7947 of notebooks/en/04_runtime_prediction_models.ipynb (NOT lines 9500-9524 as claimed; the file has only 8313 lines): plt.subplots(figsize=(14, 6)) at 7924, xticklabels fontsize=10 at 7928, legend fontsize=9 at 7931, the "down lower is better" key fontsize=9 at 7944, and the per-bar value labels fontsize=9 at 7947. On the page that yields about 3.9 pt for the twelve MAE value labels, the legend and the arrow key; about 4.4 pt for the architecture tick labels; about 4.8 pt for the y-axis numbers; about 5.2 pt for "MAE (seconds)". Measured on the raster, the "7040s" digits are 17 px tall = 0.94 mm on the page, roughly one third the cap height of the 12 pt body text. The cell overrides the notebook's own rcParams (lines 185-203: xtick/ytick/legend 11, axes.labelsize 12, figsize (12, 6)) whose comment states the base sizes were chosen to stay legible after scaling into the 6.1-inch column, and additionally widens the canvas to 14 in, deepening the reduction.

**Kanıt:** The PNG is 2790x1197 px; rcParams set "figure.dpi": 200 / "savefig.dpi": 200, so the canvas is 2790/200 = 13.95 in wide (`plt.subplots(figsize=(14, 6))`, line 9500). thesis/latex/chapters/6.results_and_discussion.tex line 74 is `\includegraphics[width=\textwidth]{figures/nb04-fig05-dl-comparison.png}` → scale 6.1/13.95 = 0.437. Applying that: value labels `fontsize=9` (line 9524) → 3.9 pt; legend `fontsize=9` (line 9508) → 3.9 pt; the "↓ lower is better" key `fontsize=9` (line 9520) → 3.9 pt; x ticks `fontsize=10` (line 9505) → 4.4 pt; y ticks rcParam 11 → 4.8 pt; "MAE (seconds) ↓" rcParam 12 → 5.2 pt. The cell overrides the notebook's own deliberately chosen base sizes downward — the rcParams block carries the comment "Base sizes chosen to stay legible after the figure is scaled down into a 6.1-inch text column" and sets xtick.labelsize 11 / legend.fontsize 11 — and also widens figsize from the rcParam default (12, 6) to (14, 6), deepening the reduction.

**Düzeltme:** In the cell at notebooks/en/04_runtime_prediction_models.ipynb: change line 9500 to `plt.subplots(figsize=(9, 5.5))` and delete the three downward font overrides so the rcParam sizes apply — drop `fontsize=10` on line 9505, `fontsize=9` on line 9508, and raise `fontsize=9` to 10 on lines 9520 and 9524. At 9 in wide the scale becomes 0.68, putting value labels at ~6.8 pt and ticks at ~7.5 pt.

---

### `NB04_Table01.html NB04_Table02.html NB04_Table03.html NB04_Table04.html` — completeness

**Ne:** NB04_Table02.html reports five per-bucket MAE columns with no sample size for any bucket, even though notebook 04 cell 40 computes `n {bucket}` for each and then excludes them from `_display_cols`. The buckets are wildly unequal in the test set (16,437 jobs): <1 min = 1,983; 1-10 min = 5,761; 10 min-1 h = 3,579; 1-24 h = 5,008; >24 h = 106 (0.65%). The ">24 h" column therefore carries the table's largest numbers (110570.15-136965.80 s) while resting on 106 jobs, and the reader has no way to see that. The claim's framing that this column "drives the whole tail argument" is overstated: the tail argument is carried by "MAE excl. top 5%" and "Top-5% share of total |error|", whose slice size is self-evident from the label; the >24 h bucket is a separate, far smaller slice. Related gap in the same artefact: the top-5% runtime threshold (28,357 s) that defines the last two columns is printed only in the notebook and never appears in the exported HTML.

**Kanıt:** Headers present: "MAE, &lt;1 min (s)", "MAE, 1-10 min (s)", "MAE, 10 min-1 h (s)", "MAE, 1-24 h (s)", "MAE, &gt;24 h (s)" — no n column of any kind. The "MAE, &gt;24 h (s)" column carries the largest numbers in the table (129020.20, 131620.31, 136944.97, 112590.05, 136965.80, 131683.28, 110570.15) and drives the whole tail argument, yet the reader cannot tell whether it is a mean over ten jobs or a thousand. Notebook cell 40 computes `_row[f"n {_bl}"] = int(_mask.sum())` for every bucket, but `_display_cols = ["Model"] + [f"MAE, {_bl} (s)" for _bl in _RUNTIME_BIN_LABELS] + ["MAE, all (s)", "MAE excl. top 5% (s)", "Top-5% share of total |error|"]` omits every `n {_bl}` column.

**Düzeltme:** Add the counts to `_display_cols` in cell 40 (they are identical across model rows, so either interleave `n {_bl}` after each MAE column or put the bucket sizes in the caption as "n = ... / ... / ... / ... / ..."), and state the total test n (16,437) in the caption.

---

### `NB04_Table05.html NB04_Table06.html NB04_Table07.html NB04_Table08.html` — completeness

**Ne:** NB04_Table08.html reports 3-seed means (seeds 42/1337/2024, aggregated in src/tuning.py finalize_dl_model) under the header set "Test MAE (s) / Test MdAE (s) / Test RMSE (s) / Test MAPE (%) / Test R²" — the same headers NB04_Table01.html uses for genuinely single-run ML fits — with no std column, no n, and no footnote, and sorts the rows ascending (LSTM 6688.74, CNN 7040.17, CNN-LSTM 7230.19) so they read as a ranking. The adjacent gaps are smaller than the seed noise the thesis reports elsewhere: CNN vs CNN-LSTM is 190.02 s against stds of 268.47 s and 653.18 s (NB04_Table13.html, Seeds (n) = 3), and LSTM vs CNN is 351.43 s against 61.02 s and 268.47 s. The same defect applies to NB04_Table09/10/11.html (Experiments D, E, F), which are built the same way. The dispersion is not missing from the work as a whole — NB04_Table13.html has std, saved-seed and n columns, and NB04-Figure05.png draws overlapping error bars — but neither that figure's title nor Tables 08-11 state the seed count, so a reader of the per-experiment tables alone cannot tell these are means over 3 runs.

**Kanıt:** NB04_Table08.html rows, column "Test MAE (s)": "LSTM" = 6688.74, "CNN" = 7040.17, "CNN-LSTM" = 7230.19, displayed in that ascending order with no dispersion column and no n. The identical means appear in NB04_Table13.html for experiment "C — Numeric" with their dispersion attached: "CNN | 7040.17 | 268.47 | 6733.31 ... | 3" and "CNN-LSTM | 7230.19 | 653.18 | 7691.65 ... | 3" under headers "MAE (s)", "MAE (s) — std", "MAE (s) — saved seed", "Seeds (n)". So Table08's numbers are means over 3 seeds, not test metrics of one run as "Test MAE (s)" implies, and 7230.19 − 7040.17 = 190.02 s is smaller than either architecture's own seed std. The notebook confirms the sort is asserted: cell 57 ends with `display(dl_num_results.sort_values("Test MAE (s)").reset_index(drop=True).round(2))`.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 57, build `dl_num_results` from the same multi-seed record that feeds Table13 rather than from `cnn_metrics_num["mae"]` etc. alone: add "Test MAE (s) — std", the matching std columns for MdAE/RMSE/MAPE/R², and a "Seeds (n)" column, and retitle the mean columns "Mean Test MAE (s)" so the header states what the cell holds. Keep the ascending sort but do not let the caption claim an architecture ranking, since the CNN/CNN-LSTM difference is inside one seed std.

---

### `NB04_Table05.html NB04_Table06.html NB04_Table07.html NB04_Table08.html` — completeness

**Ne:** NB04_Table07.html — the primary Experiment-B comparison of five learned models against seven baselines — reports every metric as a bare point estimate to two decimals with no uncertainty quantification of any kind, and NB04 contains no bootstrap CI and no paired significance test anywhere to supply it (grep for wilcoxon|bootstrap hits only a comment in cell 45 noting that every comparison rests on a single chronological split). The table is NOT sorted, so "adjacent" must be read as adjacent in MAE order, not adjacent table rows: by MAE the middle cluster is Profile Median 4389.67, LightGBM (Native Categorical) 4973.16, Per-User Median 5191.50, LightGBM (One-Hot) 5305.43 — gaps of 218–583 s that the notebook's own robustness runs show are not resolvable (user-grouped 5-fold CV for LightGBM-native: 4990.31 s ± 1615.83 s, cell 43; rolling-origin windows for the same model: 4964.36 / 4273.91 / 3456.07 s, NB04_Table06.html). The GroupKFold ± is not itself the error bar that belongs in the table: it is a different estimand and exists for only one of the twelve rows. What is actually missing is a paired bootstrap or signed-rank test on the fixed test set's per-job absolute errors, at margins (~218 s) that sit around 1–2 standard errors given the error spread implied by RMSE ~15,000 s over 16,437 jobs — precisely the regime where the reader cannot judge without it. The missing test-set n is NOT part of the defect: n = 16,437 is constant across all twelve rows and is stated twice in thesis/latex/chapters/4.prediction_models.tex (lines 7 and 141), which is standard placement. The same omission propagates to the printed table tab:predresults in thesis/latex/chapters/6.results_and_discussion.tex.

**Kanıt:** NB04_Table07.html has exactly six headers — "Model", "Test MAE (s)", "Test MdAE (s)", "Test RMSE (s)", "Test MAPE (%)", "Test R²" — and no dispersion or count column. Adjacent rows: "LightGBM (Native Categorical)" = 4973.16 vs "Per-User Median (baseline)" = 5191.50 (218.34 s apart, 4.4%), and "Profile Median (baseline)" = 4389.67 vs "LightGBM (Native Categorical)" = 4973.16. The notebook measures the relevant spread in cell 43 and keeps it out of the table: `print(f"[robustness-9] User-grouped 5-fold CV MAE: {np.mean(cv_fold_maes):.2f}s ± {np.std(cv_fold_maes):.2f}s vs. single chronological split: {lgb_nat_final_metrics['mae']:.2f}s")`. The test-set size is likewise only in prose in cell 43 ("16,437 independent jobs"), never in the table.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 47, add an "n (test jobs)" column (`len(y_test_nat)`) to `tuning_progress_categorical`, and add a "Test MAE (s) — 5-fold user-grouped CV" column carrying `mean ± std` from `cv_fold_maes` for the models where it was computed. At minimum, carry n and the CV std into the exported table so a reader can tell which of the 12 rows are actually separable.

---

### `NB04_Table09.html NB04_Table10.html NB04_Table11.html` — data-integrity

**Ne:** Real, but two details need correcting and the strongest instance is in Table 10, not Table 09. (1) The HTML files carry no caption at all, and the notebook markdown that acts as their caption does not use these means. NB04 markdown cell 69 (Exp D / Table 09) cites LSTM MAE 5836.32, CNN-LSTM 6618.09, CNN 6865.60 and R2 0.06/0.11/0.07; the table prints 6316.79, 6929.09, 6524.73 and 0.11/0.09/0.09. Cell 89 (Exp F / Table 11) names CNN best at MAE 6704.19 while the table ranks LSTM first at 6137.18. The captions do rank architectures against one another, but from a superseded single-seed run, so 'uses those means to rank' is inaccurate. (2) The sharpest reader-facing consequence is Table 10 (Experiment E), not the Table 09 R2 tie. Table 10 prints an ordered ranking CNN 6703.64 / CNN-LSTM 6716.67 / LSTM 6844.75. The top two differ by 13.03 s against per-seed stds of 213.00 and 266.46, and the entire three-way spread is 141.11 s against stds of 117.47-266.46 - the whole ordering is inside one standard deviation. The Table 09 R2 case is also real as described: 0.09083570 vs 0.08836087, a 0.0025 gap against stds 0.0484 and 0.0274. Accurate statement: NB04_Table09/10/11.html report three-seed mean test metrics to two decimals with no standard deviation and no sample size, even though every underlying checkpoint records n_seeds=3, a per-metric std, and test_size=16437. Each table is sorted into an architecture ranking whose margins are smaller than the seed dispersion the same run measured, and the printed thesis reproduces this pattern - chapter 6 reports DL point estimates and draws architecture conclusions from them without ever stating the seed count or dispersion, which appear only in NB04_Table13.html and never reach the thesis.

**Kanıt:** The header row of all three files is exactly '<th>Model</th><th>Test MAE (s)</th><th>Test MdAE (s)</th><th>Test RMSE (s)</th><th>Test MAPE (%)</th><th>Test R²</th>' — six columns, no dispersion column and no sample-size column. The same numbers appear in NB04_Table13.html with their dispersion: 'D — Categorical | LSTM | 6316.79 | 200.74 | ... | 0.11 | 0.02 | 0.12 | 3' under headers 'MAE (s) — std' and 'Seeds (n)'. results/checkpoints/exp_d_cnn.json gives r2=0.09083570142436419 with r2_std=0.04841276160712104 and n_seeds=3, and test_size=16437 for every one of the nine runs. So the seed count (3), the per-metric std, and the test-set size (16,437 jobs) all exist and are all omitted from Tables 09/10/11. In Table09 the CNN R² 0.09 and CNN-LSTM R² 0.09 differ by 0.0025 against stds of 0.048 and 0.027.

**Düzeltme:** In notebook cells 68, 78 and 88, build the DataFrame the way cell 101 already does — read the '<key>_std' entries and 'n_seeds' out of the metrics dicts — and emit paired columns ('Test MAE (s)', 'Test MAE (s) — std', ...) plus a 'Seeds (n)' column. State n = 16,437 test jobs in each table caption (available as test_size in the exp_d/e/f checkpoints).

---

### `NB04_Table12.html NB04_Table13.html NB04_Table14.html` — data-integrity

**Ne:** The caption in cell 104 misstates both Random Forest hyperparameters relative to Table 14: it claims "a maximum depth of 20" when the table shows max_depth 5 (Numeric, Exp A) and 15 (One-Hot, Exp B), and it attributes "max_features = 0.7" to "the Random Forest models" (plural) when only the One-Hot model uses 0.7 — the numeric-track model uses 'sqrt'. The claim's supporting argument that "depth 20 is not even reachable" because configs/models.yaml sets tuning.random_forest max_depth: [10, 15] is NOT correct: the pipeline uses run_gridsearch_iterative, which widens the grid around the random-search winner via _narrow_int in src/tuning.py, so the YAML list is not a hard bound — demonstrated by the numeric track landing on max_depth 5, also outside [10, 15]. The value 20 appears only as a docstring example at src/models/rf_runtime_predictor.py:46, which is the likely source of the stale caption number. Fix: change the caption to state the two actual depths (5 and 15) and note that max_features = 0.7 applies to the One-Hot Random Forest while the numeric-track model uses 'sqrt'.

**Kanıt:** NB04_Table14.html: Random Forest (Numeric) shows 'max_depth': 5, 'max_features': 'sqrt'; Random Forest (One-Hot) shows 'max_depth': 15, 'max_features': 0.7. The caption in notebooks/en/04_runtime_prediction_models.ipynb cell 104 reads: "the Random Forest models utilize a **maximum depth of 20** and **feature subsampling (max_features = 0.7)**". Depth 20 is not even reachable: configs/models.yaml tuning.random_forest sets max_depth: [10, 15].

**Düzeltme:** Replace the hand-written sentence with values read from champion_params (e.g. f-string rf_gs_best['max_depth'] and rf_oh_gs_best['max_depth']), and state the two Random Forest configurations separately since they differ.

---

### `NB04_Table12.html NB04_Table13.html NB04_Table14.html` — data-integrity

**Ne:** The caption for Table 12 (markdown cell 92 of notebooks/en/04_runtime_prediction_models.ipynb, and its Turkish counterpart) says "Among all 19 evaluated configurations" / "among all 19 evaluated models", but NB04_Table12.html lists 27 configurations. 19 also does not equal the 20 trained configurations that remain after excluding the seven non-learned baselines (Alibaba Estimate SJU/SJG/SJGG, Profile Median, Per-User Median, Constant Median, Constant Zero) — 20 being the count in NB04_Table14 — so the number is stale, not a deliberate subset. The claim's suggested reconciliation (27 minus eight rows including XGBoost (Native Categorical)) is not correct: XGBoost (Native Categorical) is a trained model with tuned hyperparameters listed in Table 14, so it cannot be the omitted item. The caption is stale in more than the count: it credits XGBoost (One-Hot) with MAE 3389.26, R² 0.51 and MdAE 1128.89, and LSTM (Experiment D) with MAPE 6.51%, whereas Table 12 reports 6053.96 / 0.18 / 2754.63 for XGBoost (One-Hot) and 1837.11% MAPE for LSTM (One-Hot), and the lowest MAE in the table belongs to Alibaba Estimate (SJGG) at 3151.10 — so the caption's "best overall model" claim also contradicts the table it captions.

**Kanıt:** NB04_Table12.html has 27 <tr> data rows (3 in "A — ML Numeric", 12 in "B — ML Categorical", 3 each in C, D, E, F). The caption (cell 92) says "Among all 19 evaluated configurations" and repeats "among all 19 evaluated models". The 12-row Experiment B block includes eight rows the count omits: Alibaba Estimate (SJGG), Alibaba Estimate (SJG), Alibaba Estimate (SJU), Profile Median, Per-User Median, Constant Median, Constant Zero, XGBoost (Native Categorical).

**Düzeltme:** Interpolate the count: f"Among all {len(df_all)} evaluated configurations".

---

### `NB04_Table12.html NB04_Table13.html NB04_Table14.html` — units

**Ne:** NB04_Table12.html reports 27 model configurations on a single MAE value per row with no std, CI or seed-count column, and no caption or footnote. The rows are not commensurable: the 12 deep-learning rows (Experiments C–F) are means over 3 seeds (DL_SEEDS = [42, 1337, 2024]), the 8 tree rows (RF/XGB/LightGBM) are single runs at random_state=42, and the remaining 7 rows are deterministic baselines (Alibaba SJGG/SJG/SJU estimates, Profile Median, Per-User Median, Constant Median, Constant Zero) for which a std does not apply. The table is not a single global ranking — it is grouped by Experiment A–F and sorted by MAE within each block — but those within-block orderings are what the reader will take at face value, and for the DL blocks they fall inside seed noise: Experiment E is ordered 6703.64 / 6716.67 / 6844.75 (141.11 s spread) against per-model seed stds of 213.00, 266.46 and 117.47 from NB04_Table13.html, and the largest seed std, 653.18 s for CNN-LSTM in Experiment C, exceeds the 190 s and 351 s gaps that order the C block. The dispersion and n exist only in NB04_Table13.html, and the notebook's own warning that comparing the two provenances at face value "treats two different levels of uncertainty as if they were the same" survives only as stdout of code cell 91; neither is carried into the exported Table 12 HTML, which has no note pointing readers to Table 13.

**Kanıt:** NB04_Table12.html has exactly these columns: <th>Experiment</th><th>Track</th><th>Model</th><th>MAE (s)</th><th>MdAE (s)</th><th>RMSE (s)</th><th>MAPE (%)</th><th>R²</th> — no std, CI or seed-count column. NB04_Table13.html gives the missing dispersion for the same DL runs: <td>C — Numeric</td><td>CNN-LSTM</td> MAE (s) 7230.19 with "MAE (s) — std" 653.18, and <td>D — Categorical</td><td>CNN-LSTM</td> 6929.09 ± 651.52. Table 12's Experiment E block is sorted 6703.64 / 6716.67 / 6844.75 — a 141 s spread ranked as if meaningful, against per-model stds of 213.00, 266.46 and 117.47 in Table 13. The notebook's own caveat exists only as stdout in cell 91 ("Comparing them at face value treats two different levels of uncertainty as if they were the same") and is not carried into the exported HTML.

**Düzeltme:** In cell 91, join the *_std and n_seeds fields that the DL metrics dicts already carry, add a "MAE (s) — std" and "Seeds (n)" column (blank or "1" for the single-run tree models), and print the caveat as an HTML footnote inside the exported table rather than to stdout.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** Two rows across the 256-GPU exports (none at 32-GPU) pair a "Yes (Holm p<0.05)" verdict with a 95% CI that contains zero, because the "Significant?" column and the CI columns measure different quantities and the exported tables never say so. The "p-value"/"p (Holm)"/"Significant?" columns come from stats.wilcoxon on signed ranks, while "95% CI low/high" come from _boot_ci, a 999-sample percentile bootstrap of the MEAN paired difference (both defined in cell 47 of notebooks/en/05_scheduler_evaluation_256_gpu.ipynb; cell 48 reuses _boot_ci for Table03). (1) NB05_256GPU_Table02.html, row 25, SJF-AlibabaEstimate-Group (baseline): JCT vs FIFO -364.800000, CI [-2363.200000, 1831.600000], effect size r +0.121, p 0.000000, "Yes (Holm p<0.05)" rendered green and bold. Table02's test is ONE-SIDED (alternative="greater", i.e. H1: FIFO slower than the policy) — proven by row 26 SJF-CNN (Numeric Sequence) reporting p = 1.000000 — so "Yes" here means "significantly BETTER than FIFO", not merely "significantly different". The same policy is reported at JCT Improvement % -0.26 in NB05_256GPU_Table01.html, so the two exported tables assert that a policy is 0.26% worse than FIFO and significantly better than FIFO simultaneously. (2) NB05_256GPU_Table03.html, last row, SJF-LSTM (Categorical) vs SJF-XGBoost (Categorical): Mean JCT Diff -648.0, CI [-1406.7, 36.1], r -0.042, Holm 2.36e-05, "Yes (Holm p<0.05)". This test is two-sided, so there is no direction contradiction here — only the CI/verdict mismatch, on the comparison cell 48's own comment names as the point of the table ("whether the two leading ML policies ... are actually distinguishable from each other rather than within noise"). The notebook's stdout banner ("Effect size: matched-pairs rank-biserial. CI: 999-sample percentile bootstrap", "H1: FIFO > Policy") does explain this, but scripts/export_thesis_results.py::extract_from_nb_dict copies only outputs carrying text/html, so the banner is dropped and the three exported artefacts carry the bare table with no caption, no note on the one-sided alternative, and no statement that the CI and the p-value describe different estimands.

**Kanıt:** Table02, `SJF-AlibabaEstimate-Group (baseline)`: JCT vs FIFO (s) `-364.800000`, 95% CI low `-2363.200000`, 95% CI high `1831.600000`, p-value `0.000000`, Significant? `Yes (Holm p<0.05)` — a policy Table01 reports as worse than FIFO (JCT Improvement % -0.26) is declared significantly better. Table03, `SJF-LSTM (Categorical)` vs `SJF-XGBoost (Categorical)`: Mean JCT Diff (s) `-648.0`, CI `[-1406.7, 36.1]`, Significant? `Yes (Holm p&lt;0.05)`. The two columns test different quantities — cell 47 runs `stats.wilcoxon(...)` on ranks while `_boot_ci` bootstraps the MEAN — but nothing in either exported table says so.

**Düzeltme:** Add a caption sentence stating that the Wilcoxon verdict concerns the paired rank/median shift while the CI concerns the mean difference, and that the two can disagree on heavy-tailed JCT data; or report a CI for the median shift (Hodges-Lehmann) so the verdict and interval refer to the same quantity.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — headers

**Ne:** Both NB05_256GPU_Table02.html and NB05_256GPU_Table03.html label their significance column with an unqualified `p-value`, but they come from different tests: Table02 from notebook cell 47's one-sided `stats.wilcoxon(..., alternative="greater")` (H1: FIFO > policy), Table03 from cell 48's `alternative="two-sided"`. Neither exported file contains any statement of direction — the only place the one-sidedness is recorded is a `print()` banner ("WILCOXON SIGNED-RANK — FIFO vs All Policies (H1: FIFO > Policy)") that goes to stdout, which scripts/export_thesis_results.py (line 178, `data["text/html"]` only) cannot export. The consequence is larger than a missing label. Table02 row 26, `SJF-CNN (Numeric Sequence)`, shows p-value 1.000000 and p (Holm) 1.000000 alongside JCT vs FIFO = -32,189.9 s, 95% CI [-34,447.9, -29,812.3] and effect size r = -0.239. Under the one-sided test a p of exactly 1 means the evidence is maximally against H1 — the policy is decisively WORSE than FIFO — yet thesis/latex/chapters/6.results_and_discussion.tex:243 reads this as "not statistically distinguishable from FIFO ordering", and the table caption at line 248 defines "(fail)" as "p >= 0.05". A two-sided test on the same pairs would yield a vanishingly small p and a strongly significant result in the negative direction, so the printed thesis currently states the opposite of what the data show. A secondary inconsistency: notebook cell 46's markdown declares "H1: The ML policy differs statistically significantly from FIFO" — a two-sided hypothesis — directly above the one-sided `alternative="greater"` code in cell 47. Fix: rename the columns to distinguish the tests (e.g. `p (one-sided, H1: FIFO > policy)` in Table02 vs `p (two-sided)` in Table03) or add the hypothesis as a caption row inside the exported HTML, and correct the p=1 interpretation in section 6 and the tab:wilcoxon caption.

**Kanıt:** Notebook cell 47: `stats.wilcoxon(paired["fifo_turnaround_time"], paired["turnaround_time"], alternative="greater")`; cell 48 `_paired_test(..., alternative="two-sided")`. Table02 row26 `SJF-CNN (Numeric Sequence)` shows p-value `1.000000` and p (Holm) `1.000000` — the one-sided artefact. The notebook's explanatory banner ("WILCOXON SIGNED-RANK — FIFO vs All Policies (H1: FIFO > Policy)") is a `print()` to stdout and is absent from the exported HTML.

**Düzeltme:** Rename the headers to `p (one-sided)` in Table02 and `p (two-sided)` in Table03, and carry the hypothesis direction into the caption of each exported table.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — data-integrity

**Ne:** The taxonomy markdown (cell 34, both the 256-GPU and 32-GPU English notebooks) says "The 14 evaluated policies are organised into three categories", but the simulation actually runs 28 policies (cell 26's POLICIES list and its own comment) and NB05_256GPU_Table01.html reports all 28 rows. The rows the three-category text does not cover are: SRF (Heuristic), SJF-UserMedian (baseline), SJF-ProfileMedian (baseline), THREE — not four — Alibaba-estimate baselines (SJF-AlibabaEstimate, -Group, -GroupGPU), SJF-XGBoost (Native Cat), SJF-LGBM (No Cluster-Load Features), and the SIX "Sequence" deep-learning variants. Category 3 does cover the six static CNN/LSTM/CNN-LSTM numeric+categorical policies, so 6 of the 12 DL rows are uncovered, not all twelve. Separately, the Category 2 sentence "6 models × 2 feature sets" is arithmetically self-inconsistent — RF, XGBoost and LightGBM are 3 models × 2 feature sets = 6 policies — and the code in fact runs 7 tree-based policies plus 1 feature-ablation. The Turkish counterpart notebook states yet another figure ("Değerlendirilen 20 politika dört kategoride"), so the three sources disagree with each other and with the exported table.

**Kanıt:** Notebook cell 34: "The 14 evaluated policies are organised into three categories" and "6 models × 2 feature sets. RF, XGBoost, and LightGBM use numeric and categorical version predictions as the ordering criterion." Table01 contains 28 data rows, including four Alibaba-estimate baselines (`SJF-AlibabaEstimate (baseline)`, `-Group`, `-GroupGPU`), `SJF-LGBM (No Cluster-Load Features)`, `SRF (Heuristic)` and twelve deep-learning variants that the three-category description does not cover.

**Düzeltme:** Rewrite cell 34 against the exported table: state 28 policies and list the categories actually present (FIFO, SJF-Oracle, SRF heuristic, median/Alibaba-estimate baselines, tree-based, deep learning static, deep learning sequence, ablation).

---

### `NB05_256GPU_Table04.html NB05_256GPU_Table05.html` — naming

**Ne:** The claim is accurate for the two named files but under-scopes the problem. Accurate statement: in all TEN exported NB05 tables (NB05_256GPU_Table01-05 and NB05_32GPU_Table01-05), the policy row is printed as 'SJF-LGBM (Categorical)' although it is driven by the native-categorical LightGBM model (lgbm_categorical_native.joblib predicting on X_test_cat_base) — the same encoding the neighbouring row spells '(Native Cat)' for XGBoost — while '(Categorical)' on the RF, XGBoost, CNN and LSTM rows means one-hot (X_test_cat_oh). None of the ten exports contains the string 'SJF-LGBM (Native Cat)'. Both NB05 notebook sources have already been renamed to 'SJF-LGBM (Native Cat)' in _PRED_COL_MAP, POLICIES and the prediction dict (7 source cells each; the old label survives only in stale cell outputs), so every NB05 HTML export must be regenerated. The reader-facing harm is sharpened by the upstream NB04 exports, which use the explicit pair 'LightGBM (One-Hot)' / 'LightGBM (Native Categorical)': a reader carrying that convention forward will read NB05's 'SJF-LGBM (Categorical)' as the one-hot LightGBM model, which it is not.

**Kanıt:** Both tables carry the rows 'SJF-LGBM (Categorical)' and 'SJF-XGBoost (Native Cat)' plus 'SJF-XGBoost (Categorical)' and 'SJF-RF (Categorical)'. In the notebook, preds_lgb_cat = lgb_cat_nat.predict(X_test_cat_base) with lgb_cat_nat = joblib.load(MODEL_DIR / "lgbm_categorical_native.joblib") (native categorical), whereas preds_xgb_cat = xgb_cat.predict(X_test_cat_oh) and preds_rf_cat = rf_cat.predict(X_test_cat_oh) (one-hot). The current source has already been renamed — _PRED_COL_MAP reads "SJF-LGBM (Native Cat)": "pred_lgb_cat" and POLICIES lists "SJF-LGBM (Native Cat)" — but the exported HTML still prints "SJF-LGBM (Categorical)", so the exports are stale relative to the notebook source.

**Düzeltme:** Re-execute notebooks/en/05_scheduler_evaluation_256_gpu.ipynb and re-run scripts/export_thesis_results.py so the label matches the source's "SJF-LGBM (Native Cat)"; better still, make the encoding suffix exhaustive and symmetric — "(One-Hot)" for RF/XGBoost/CNN/LSTM categorical rows, "(Native Cat)" for the LightGBM and XGBoost native rows.

---

### `NB05_256GPU_Table04.html NB05_256GPU_Table05.html` — data-integrity

**Ne:** Table04's "iid 95% CI low/high" and Table02's "95% CI low/high" are the same estimator (999-sample iid percentile bootstrap of the same FIFO-paired mean difference) applied to identical inputs from the same run, yet disagree on all 54 bounds while all 27 point estimates match exactly. Cause: notebook cell 47 defines `_boot_ci` over module-level `_rng = np.random.default_rng(42)`; cell 48 consumes ~37 further draws from it; cell 49 then re-calls `_boot_ci` for the iid column with the generator advanced. Two caveats to the original framing: (a) the divergence is not merely "a second call" — the intervening cell-16b calls are what shift the state; (b) the magnitude is pure Monte Carlo noise (max 285.9 s, at most 7.6% of a CI width) and no significance, sign, or zero-crossing conclusion changes, so this is a reproducibility/consistency defect rather than an error in any reported finding — severity is nearer minor/moderate than major. Also note the artefact list names NB05_256GPU_Table05.html, which is a tail-latency percentile table with no CI columns and is unrelated to this claim. Fix: reuse the lo/hi already computed in cell 16, or pass a freshly seeded generator per call. The same defect exists in the 32-GPU pair.

**Kanıt:** SJF-Oracle: Table04 'JCT vs FIFO (s)' = 109657.4, 'iid 95% CI low' = 108064.7, 'iid 95% CI high' = 111305.0; Table02 same policy 'JCT vs FIFO (s)' = 109657.400000, '95% CI low' = 108013.100000, '95% CI high' = 111232.000000. SJF-LSTM (Categorical): 85373.5/88976.7 vs 85450.300000/89002.000000. SJF-XGBoost (Native Cat): 84928.3/88300.2 vs 84991.700000/88351.500000. Both are the same estimator: cell 16 defines _boot_ci(diff) (999-sample percentile bootstrap) and cell 16c calls that very function again — but the generator is module-level shared state (_rng = np.random.default_rng(42)), so the second call draws a different resample.

**Düzeltme:** In cell 16, store each policy's (lo_iid, hi_iid) in a dict keyed by policy and have cell 16c reuse those stored values instead of calling _boot_ci again; or give _boot_ci its own generator (rng = np.random.default_rng(42) inside the function) so repeated calls are reproducible. Then re-export both tables.

---

### `NB05_256GPU_Table04.html NB05_256GPU_Table05.html` — units

**Ne:** NB05_256GPU_Table05.html (and identically NB05_32GPU_Table05.html) reports waiting-time percentiles but its value headers state only the statistic, not the quantity: 'Median (s)', 'P75 (s)', 'P90 (s)', 'P95 (s)', 'P99 (s)', 'Max (s)'. This is the sole break in an otherwise consistent convention — all 23 other exported tables name the measured quantity in the header — and the export script carries no caption. Because the sibling NB05_256GPU_Table01.html reports 'Median Wait (s)' / 'P95 Wait (s)' alongside 'Mean JCT (s)' / 'Max JCT (s)' for the same policies, a reader can take Table05's FIFO 'Max (s)' = 361340.4 for a maximum JCT and hit a false contradiction with Table01's 'Max JCT (s)' = 511762.3. Two details in the original claim need correcting: the notebook is not entirely context-free — markdown cell 53 immediately preceding the table is headed "Per-Policy Wait Time Percentiles" and cell 55 after it is captioned "Figure 5 — Wait Time Percentile Analysis ... waiting time distribution"; what is absent is a numbered table caption (the "**Table N –**" markdown cells stop at "Table 3 –", so tables 4 and 5 have no prepared caption text to transplant into the thesis). Also, NB05_256GPU_Table04.html is not affected — its header 'JCT vs FIFO (s)' does state the quantity. Fix: rename to 'Median Wait (s)', 'P75 Wait (s)', ..., 'Max Wait (s)' in notebook cell 54 (and the 32-GPU twin), or add a numbered caption naming the quantity.

**Kanıt:** Table05 headers are exactly 'Median (s)', 'P75 (s)', 'P90 (s)', 'P95 (s)', 'P99 (s)', 'Max (s)' — the word wait appears nowhere in the file. NB05_256GPU_Table01.html distinguishes 'Median Wait (s)', 'P95 Wait (s)', 'Mean JCT (s)' and 'Max JCT (s)'; for FIFO its 'Max JCT (s)' = 511762.3 while Table05's 'Max (s)' = 361340.4, so misreading the column produces a real numeric contradiction. scripts/export_thesis_results.py writes only the <table> markup (HTML_STYLE_HEADER + html_content + HTML_STYLE_FOOTER, lines 178-192) — no caption is carried across, and the notebook has no caption for this table at all (its markdown captions stop at "Table 3 –").

**Düzeltme:** In cell 54's pct_rows dict, rename the keys to 'P50 Wait (s)', 'P75 Wait (s)', 'P90 Wait (s)', 'P95 Wait (s)', 'P99 Wait (s)', 'Max Wait (s)', and add a caption for this table in the notebook so the export is self-describing.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** Table02's last row shows a 95% CI lying entirely below zero (the policy is reliably worse than FIFO) yet the "Significant?" column says "No", because the test is one-sided — a fact nowhere stated in the exported table.

**Kanıt:** Table02 row26 SJF-CNN (Numeric Sequence): JCT vs FIFO (s) "-208586.400000", 95% CI low "-230722.900000", 95% CI high "-187100.900000", p-value "1.000000", Significant? "No". Notebook cell 47 calls `stats.wilcoxon(..., alternative="greater")` (H1: FIFO > Policy), so a policy worse than FIFO can only ever return p→1.

**Düzeltme:** Either report the two-sided p for this table, or rename the column to "Beats FIFO? (one-sided Holm p<0.05)" and add a footnote stating the alternative is `greater`, so "No" cannot be misread as "no difference from FIFO".

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** In results/figures/thesis_export/html/NB05_32GPU_Table02.html, all 216 numeric cells (27 rows x columns 1-8) are rendered with exactly 6 decimal places by pandas Styler's default display.precision=6, because notebook 05_scheduler_evaluation_32_gpu.ipynb cell 47 calls sig_df.style.map(...) without a .format(). Two distinct problems follow, and the second is more serious than "trailing zeros carry no information": (a) Columns 1-6 (Mean JCT, JCT vs FIFO, 95% CI low/high, Effect size r, Wilcoxon W) were already rounded in cell 47 to 1 dp or 3 dp, so the appended zeros are not just padding, they are incorrect digits. Table01 of the same export reports SJF-Oracle Mean JCT (s) = 252353.75, whereas Table02 prints 252353.800000 for the same statistic; SJF-LSTM (Categorical) is 507394.68 vs 507394.700000. 25 of 27 policies disagree between Table01 and Table02, and Table02 -- the one displaying six decimals -- is the rounded, less accurate one. (b) Columns 7-8 (p-value, p (Holm)) are NOT rounded in cell 47; they are raw floats. The 6-decimal default truncates every one of them to "0.000000", printing the p-value as exactly zero rather than as a very small number. The sibling table NB05_32GPU_Table03.html, produced by cell 48 without a Styler, shows these correctly as 0.000000e+00, and its effect sizes as 0.907 and mean differences as 585438.8. The fix is to attach an explicit per-column .format() to the Styler in cell 47 (1 dp for the JCT/CI/W columns, 3 dp for effect size, scientific notation or a "< 1e-300" style floor for the two p-value columns), matching the precision the values were actually computed and rounded to.

**Kanıt:** Table02 cells: Mean JCT (s) "252353.800000", JCT vs FIFO (s) "1124356.500000", 95% CI low "1108812.600000", Effect size (r) "0.905000", Wilcoxon W "127787576.500000". A machine check of all 27 rows finds every one of columns 1-8 has exactly 6 decimal places. Notebook cell 47 stores these as `round(df_p["turnaround_time"].mean(), 1)`, `round(_rank_biserial(diff), 3)` and `round(stat, 1)`, so the trailing zeros carry no information.

**Düzeltme:** Apply an explicit per-column Styler format in cell 47, e.g. `.format({"Mean JCT (s)": "{:,.0f}", "JCT vs FIFO (s)": "{:,.0f}", "95% CI low": "{:,.0f}", "95% CI high": "{:,.0f}", "Effect size (r)": "{:.3f}", "Wilcoxon W": "{:,.0f}"})`.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** Table02 carries an extra unlabelled leading column of row numbers 0-26 that Table01 and Table03 do not have, because the export script's index-stripper only matches attribute-free <th> tags and silently gives up on pandas Styler output.

**Kanıt:** Table02 header begins `<th class="blank level0" >&nbsp;</th>` and every body row begins `<th id="T_1b4c9_level0_row0" class="row_heading level0 row0" >0</th>` … through `>26</th>`; pandas.read_html parses the columns as ['Unnamed: 0', 'Policy', 'Mean JCT (s)', ...]. Table01 and Table03 have no such column. Root cause in /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/scripts/export_thesis_results.py lines 201-202: `_LEADING_INT_TH_RE = re.compile(r"\s*<th>\s*(\d+)\s*</th>", re.S)` and `_LEADING_EMPTY_TH_RE = re.compile(r"\s*<th>\s*</th>", re.S)` — neither allows attributes, so `_strip_positional_index` returns the HTML unchanged for Styler tables.

**Düzeltme:** In scripts/export_thesis_results.py change the two patterns to allow attributes and a non-breaking-space header, e.g. `_LEADING_INT_TH_RE = re.compile(r"\s*<th[^>]*>\s*(\d+)\s*</th>", re.S)` and `_LEADING_EMPTY_TH_RE = re.compile(r"\s*<th[^>]*>\s*(?:&nbsp;|\s)*</th>", re.S)`, then re-export.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — units

**Ne:** Table01's "Slowdown" column reports the arithmetic mean of per-job slowdown (notebook cell 27: `mean_slowdown = df_p["slowdown"].mean()`), but the header omits the statistic name while every other aggregate column in the same row states it ("Mean Wait (s)", "Median Wait (s)", "P95 Wait (s)", "Mean JCT (s)", "Max JCT (s)"). The reader-facing harm is the mean/median ambiguity, not the missing unit: slowdown is dimensionless and conventionally takes no unit marker, and "Model R²" in the same header row carries none either. The ambiguity is load-bearing because the thesis reports slowdown both ways with order-of-magnitude different values - chapter 6 line 213 says median slowdowns for categorical models are "close to 100" at 32 GPUs while Table01 shows 1516-2392 for those policies, and the Figure 3 boxplot (notebook cells 41-42) is ordered by and discussed in terms of medians (FIFO near 5x10^2) against Table01's FIFO mean of 8275.39. Fix: rename the header to "Mean Slowdown". The identical bare header appears in NB05_256GPU_Table01.html and should be changed with it.

**Kanıt:** Table01 header row: "Mean Wait (s)", "Median Wait (s)", "P95 Wait (s)", "Mean JCT (s)", "Max JCT (s)", then bare "Slowdown". Notebook cell 27 computes `mean_slowdown = df_p["slowdown"].mean()` and stores it as `"Slowdown": mean_slowdown`. Column values run from "36.70" (SJF-Oracle) to "9478.28" (SJF-CNN (Numeric Sequence)) with nothing to say they are means of (wait+runtime)/runtime.

**Düzeltme:** Rename the key in cell 27 to "Mean Slowdown (×)" and add a one-line footnote giving the definition already written in the Figure 3 markdown: Slowdown = (Wait Time + Actual Runtime) / Actual Runtime, with 1.0 = zero wait.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** NB05_32GPU_Table03.html (and its 256-GPU twin) reports "Mean JCT Diff (s)" as Reference minus Policy, so a positive value means the Policy is BETTER (lower JCT) - but the exported table states no sign convention at all: no caption, footnote or direction key, and unlike Table02 there is no adjacent "Mean JCT (s)" column to decode it against. The header is ambiguous rather than strictly inverted (the column order "Reference | Policy" makes Reference-minus-Policy a plausible first reading, and both SJF-Oracle rows being positive gives an informed reader an anchor), but nothing in the printed table resolves it, and the sign decides the verdict for all 43 rows, 17 of which are negative. Fix: rename the column to "Mean JCT Diff (s) (Reference - Policy)" or add a "positive = policy faster" note to the exported table.

**Kanıt:** Table03 first row: Reference "SJF-UserMedian (baseline)", Policy "SJF-Oracle", Mean JCT Diff (s) "594142.4" — read literally this says the Oracle is 594,142 s worse, when it is in fact 594,142 s better. Notebook cell 48: `diff = (paired["ref_turnaround_time"] - paired["turnaround_time"]).to_numpy(dtype=float)`. Cross-check against Table01 Mean JCT (s): SJF-UserMedian 846496.17 − SJF-Oracle 252353.75 = 594142.42.

**Düzeltme:** Rename the column to "Mean JCT Diff, Reference − Policy (s)" or "Policy advantage over Reference (s)", and add "positive = Policy is faster" as a footnote.

---

### `NB05_32GPU_Table04.html NB05_32GPU_Table05.html` — naming

**Ne:** Inside the single "Policy" column of the NB05 export tables (all 10 NB05_*_Table0*.html, plus the NB05 Figure01 PNGs), the library-native categorical models are labelled inconsistently and the word "Categorical" is overloaded: "SJF-LGBM (Categorical)" is the LightGBM *native*-categorical model (lgbm_categorical_native.joblib), "SJF-XGBoost (Native Cat)" is the XGBoost *native*-categorical model (xgb_categorical_native.joblib), while "SJF-XGBoost (Categorical)" and "SJF-RF (Categorical)" are *one-hot* models. The tables carry no caption or legend, so "Cat" is undefined there (the thesis body does expand it once, as "LightGBM (Native Categorical)", at 6.results_and_discussion.tex line 59). The cited line 117 is a disambiguation note mapping tab:predresults names to tab:schedresults names ("LGBM (Native Cat.) = SJF-LGBM (Categorical)"), not a rule that "Native Cat." must always print as "Categorical" — but it does document the convention the XGBoost row departs from. The underlying cause is a stale export: the current notebook source (05_scheduler_evaluation_*_gpu.ipynb, _PRED_COL_MAP and POLICIES) already uses "SJF-LGBM (Native Cat)", so the exported HTML/PNG disagree with the code that produced them. Fix by re-exporting so both native-categorical rows read "(Native Cat.)", and expanding the abbreviation in the table caption.

**Kanıt:** NB05_32GPU_Table04.html and NB05_32GPU_Table05.html both contain the rows "SJF-XGBoost (Native Cat)" and "SJF-LGBM (Categorical)" in the same column, alongside "SJF-XGBoost (Categorical)" (the one-hot variant). The thesis body states the opposite convention explicitly — thesis/latex/chapters/6.results_and_discussion.tex line 117: "LGBM (Native Cat.) = SJF-LGBM (Categorical)" — i.e. "Native Cat." is meant to be printed as "Categorical", which the XGBoost row violates. Neither table defines "Cat".

**Düzeltme:** Pick one spelling for the native-categorical variant and apply it to every model. Since "SJF-XGBoost (Categorical)" is already taken by the one-hot model, use unambiguous full labels throughout — e.g. "SJF-XGBoost (One-Hot)" / "SJF-XGBoost (Native Categorical)" / "SJF-LGBM (Native Categorical)" — and update thesis/latex/chapters/6.results_and_discussion.tex's convention note to match.

---

### `NB05_32GPU_Table04.html NB05_32GPU_Table05.html` — data-integrity

**Ne:** The Figure 5 discussion in notebooks/en/05_scheduler_evaluation_32_gpu.ipynb (markdown cell 55) inverts BOTH halves of the median-vs-tail dichotomy it draws, and NB05_32GPU_Table05.html — the very table printed above it — refutes both. Half 1 (as the finding states): the text says "the tree-based models, specifically SJF-Random Forest (Categorical) and SJF-XGBoost (Categorical), achieve the absolute lowest median waiting times among practical policies." Table05's Median (s) column, ascending among the 27 non-Oracle policies: SJF-LSTM (Categorical) 297868.6 (1st), SJF-XGBoost (Categorical) 376438.2 (2nd), SJF-LSTM (Categorical Sequence) 378234.9 (3rd), SJF-XGBoost (Native Cat) 407717.9 (4th), SJF-RF (Categorical) 442588.2 (5th). The median leader among practical policies is a deep-learning model, and SJF-RF (Categorical) is 5th. Half 2 (not stated in the finding, same paragraph): the next bullet says "SJF-LSTM (Categorical) and SJF-CNN (Categorical) achieve the most significant reductions in P95 tail latency." Table05's P95 (s) column, ascending among non-Oracle policies: SJF-XGBoost (Native Cat) 1572324.5 (1st), SJF-RF (Categorical) 1619175.6 (2nd), SJF-CNN (Numeric) 1688474.3 (3rd), SJF-CNN (Categorical) 1699076.0 (4th), SJF-XGBoost (Categorical) 1709233.4 (5th), SJF-RF (Numeric) 1717296.3 (6th), SJF-LSTM (Categorical) 1754058.7 (7th). The tail leaders are the tree models; SJF-LSTM (Categorical) is 7th. Consequently the paragraph's closing conclusion — "Tree-based categorical models optimize typical (median) performance, while Deep Learning categorical models excel at minimizing extreme (tail) latency" — is exactly backwards against its own table: deep learning (LSTM Categorical) wins the median, tree models (XGBoost Native Cat, RF Categorical) win the P95 tail. The identical paragraph appears in notebooks/en/05_scheduler_evaluation_256_gpu.ipynb, where NB05_256GPU_Table05.html shows the same pattern (median leader SJF-LSTM (Categorical) 21599.4; P95 leaders SJF-XGBoost (Native Cat) 167963.3 and SJF-RF (Categorical) 174546.4), so the correction is needed at both cluster scales.

**Kanıt:** NB05_32GPU_Table05.html "Median (s)" values, ascending, excluding SJF-Oracle: SJF-LSTM (Categorical) 297868.6, SJF-XGBoost (Categorical) 376438.2, SJF-LSTM (Categorical Sequence) 378234.9, SJF-XGBoost (Native Cat) 407717.9, SJF-RF (Categorical) 442588.2. The Figure 5 discussion in notebooks/en/05_scheduler_evaluation_32_gpu.ipynb states: "the tree-based models, specifically **SJF-Random Forest (Categorical)** and **SJF-XGBoost (Categorical)**, achieve the absolute lowest median waiting times among practical policies." SJF-RF (Categorical) ranks 5th of the 27 non-Oracle policies, not 1st or 2nd.

**Düzeltme:** Rewrite the claim to match the column: among practical policies the lowest median wait is SJF-LSTM (Categorical) at 297868.6 s, with SJF-XGBoost (Categorical) second at 376438.2 s; SJF-RF (Categorical) is 5th at 442588.2 s.

---

### `NB05_32GPU_Table04.html NB05_32GPU_Table05.html` — data-integrity

**Ne:** The Figure 5 discussion (notebooks/en/05_scheduler_evaluation_32_gpu.ipynb, markdown cell 55; also reports/html/en/05_scheduler_evaluation_32_gpu.html line 10186) is stale relative to the cell-54 output that NB05_32GPU_Table05.html was exported from, and BOTH headline bullets are inverted, not just the P95 one. P95 bullet: it claims "While tree-based models win the median, SJF-LSTM (Categorical) and SJF-CNN (Categorical) achieve the most significant reductions in P95 tail latency." In Table05 the two lowest P95 values are tree-based — SJF-XGBoost (Native Cat) 1,572,324.5 s and SJF-RF (Categorical) 1,619,175.6 s — while SJF-LSTM (Categorical) 1,754,058.7 s ranks 7th of the 27 practical policies, behind FOUR tree-based models (XGBoost Native Cat, RF Categorical, XGBoost Categorical, RF Numeric) and both CNN variants. The claim's headline phrase "behind two tree-based models" understates this; only the "two lowest P95 values are tree-based" part is exact. Median bullet (missed by the claim): the preceding bullet asserts "SJF-Random Forest (Categorical) and SJF-XGBoost (Categorical) achieve the absolute lowest median waiting times among practical policies." In Table05 the lowest practical median is SJF-LSTM (Categorical) at 297,868.6 s — a deep-learning model — with SJF-XGBoost (Categorical) 376,438.2 s second, SJF-LSTM (Categorical Sequence) 378,234.9 s third, and SJF-RF (Categorical) 442,588.2 s only fourth. So the whole tree-vs-deep-learning dichotomy the section is built on is reversed on both axes simultaneously. A third bullet is also voided: "FIFO ... pushing extreme wait times beyond the 10^6 seconds mark" no longer distinguishes FIFO, since in this run every policy including SJF-Oracle (P95 1,265,561.8 s) exceeds 10^6 s. Root cause and blast radius: NB05_32GPU_Table05.html (exported 6 Sep) comes from a re-run whose wait times are roughly 2.5x those still shown in reports/html/en/05_scheduler_evaluation_32_gpu.html and in the printed thesis table tab:waitpercentile (thesis/latex/chapters/6.results_and_discussion.tex lines ~318-341), both of which carry the older run (SJF-Oracle median 11,564 / P95 489,044; SJF-LSTM (Categorical) P95 678,449 = best practical). Under that older run the prose was correct, so Chapter 6's Tail-Latency Analysis is currently self-consistent with itself — the contradiction is visible in the notebook and in the exported table, not yet in the compiled PDF. The exposure is that the thesis artefacts have diverged: the printed percentile table and the printed figure thesis/latex/figures/nb05-fig05-wait-percentile_32gpu.png are both dated 31 Aug while the HTML table exports are dated 6 Sep, and the current export run produced only NB05_32GPU-Figure01.png (the notebook holds just one figure output), so scripts/export_thesis_results.py skipped the figure sync. Re-running the notebook end to end and re-exporting — which that script exists to do automatically — would push the inverted numbers, and hence the now-false conclusion, straight into Chapter 6 and Figure 5. Fix: rewrite cell 55's bullets against the current numbers (or re-run and regenerate all NB05 artefacts together), and reconcile the LaTeX table and figure with whichever run is authoritative.

**Kanıt:** NB05_32GPU_Table05.html "P95 (s)" values, ascending, excluding SJF-Oracle: SJF-XGBoost (Native Cat) 1572324.5, SJF-RF (Categorical) 1619175.6, SJF-CNN (Numeric) 1688474.3, SJF-CNN (Categorical) 1699076.0, SJF-XGBoost (Categorical) 1709233.4, SJF-RF (Numeric) 1717296.3, SJF-LSTM (Categorical) 1754058.7. The Figure 5 discussion asserts: "While tree-based models win the median, **SJF-LSTM (Categorical)** and **SJF-CNN (Categorical)** achieve the most significant reductions in **P95 tail latency**." The table shows the reverse on both counts — the two lowest P95 values belong to tree-based models (SJF-XGBoost (Native Cat), SJF-RF (Categorical)), while SJF-LSTM (Categorical) is 7th.

**Düzeltme:** Rewrite the P95 claim to match the column: the lowest practical P95 is SJF-XGBoost (Native Cat) at 1572324.5 s, followed by SJF-RF (Categorical) at 1619175.6 s; SJF-LSTM (Categorical) ranks 7th at 1754058.7 s. The paired 'trees win the median, DL wins the tail' framing is not supported by this table and must be dropped or reversed.

---

## MINOR (13)

### `NB01-Figure01.png` — data-integrity

**Ne:** The figure's second mode sits at log₁₀ ≈ 3.62 (≈4,200 s), but the thesis text and caption describe it as log₁₀ ≈ 4.0 (≈10,000 s) — the figure is correct and the prose overstates the mode by a factor of ~2.4.

**Kanıt:** Measured from the PNG using the right panel's own gridlines (x: 1 -> px 1682, 6 -> px 2737, 211 px per decade; y: 0 -> px 865, 3500 -> px 193, 96 px per 500 counts). Tallest bar: centre log₁₀ = 2.351, top row 191 -> ≈3,510 counts. Secondary local maximum: centre log₁₀ = 3.621, top row 342 -> ≈2,724 counts; neighbours are 3.536 -> ≈2,146, 3.706 -> ≈2,469, 3.872 -> ≈2,344, 3.957 -> ≈1,974, 4.043 -> ≈1,453, so at log₁₀ = 4.0 the histogram is already well past the peak. 10^3.621 - 1 = 4,178 s. thesis/latex/chapters/3.dataset_and_workload.tex:40 states "a second, albeit smaller peak exists at log\textsubscript{10}~$\approx$~4.0 ($\approx$10,000\,s)" and the caption at line 45 states "those in excess of 10,000\,s".

**Düzeltme:** Correct the prose, not the plot: in thesis/latex/chapters/3.dataset_and_workload.tex:40 replace "log_{10} ≈ 4.0 (≈10,000 s)" with "log_{10} ≈ 3.6 (≈4,000 s)", and in the caption at line 45 replace "those in excess of 10,000\,s" with "those around 4,000\,s". Align the notebook markdown cell 16 ("log₁₀≈3.5–4.0 (~3,000–10,000 s)") to the same numbers.

---

### `NB03-Figure01.png` — naming

**Ne:** cluster_load_gpu is labelled "Background GPU Demand (GPUs)" in the cluster-load time-series figure (nb03-fig01-cluster-load.png, Figure ref fig:cluster-load) but "Background GPU load" in the correlation heatmap on the facing pages (nb03-fig02-correlation.png, Figure ref fig:correlation). The figure label is the outlier, not one side of a symmetric disagreement: the feature-definition table between the two figures (3.dataset_and_workload.tex line 206, tab:features) defines the column as "Background GPU load at job arrival", matching the heatmap. The body text introduces two further variants for the same quantity, "cumulative GPU allocation" and "total GPU demand" (lines 164 and 169). An additional collision the original claim did not note: the heatmap labels the distinct per-job column gpu_demand as "GPU demand (GPUs)", the same head noun and unit as the cluster figure's "Background GPU Demand (GPUs)", so a reader can conflate a per-job GPU request (max 8) with cluster-wide background occupancy (peaks near 800). The fix is to relabel cell 15's ylabel and title to the thesis's defined term ("Background GPU load"). The claim's secondary point about "Background Job Count" vs "Background job count" does not stand: that is a pure Title-Case vs sentence-case difference, each figure internally consistent, with no reader-facing consequence.

**Kanıt:** This figure's lower y-axis title reads "Background GPU Demand (GPUs)" and its panel title "Cluster State: Background GPU Demand Over Time". NB03-Figure02.png (the correlation heatmap from the same notebook) shows the row/column label "Background GPU load". Both come from the identical column: cell 15 plots job_df["cluster_load_gpu"], and cell 21's _FEATURE_LABELS maps "cluster_load_gpu": "Background GPU load". The job-count label also differs in case: "Background Job Count" here vs "Background job count" in Figure02 (active_job_count in both).

**Düzeltme:** Pick one wording and use it in both cells of /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/notebooks/en/03_feature_engineering.ipynb. Recommended: keep the unit-bearing form, so in cell 21 set "cluster_load_gpu": "Background GPU demand (GPUs)" and "active_job_count": "Background job count", and in cell 15 change set_ylabel("Background Job Count") to set_ylabel("Background job count") and the titles to match the same casing.

---

### `NB03-Figure01.png` — data-integrity

**Ne:** Both panels of NB03-Figure01.png begin at exactly 0 at day 0 and ramp up, which is a trace-window artefact rather than a genuinely idle cluster: add_cluster_utilization_features in src/feature_engineering.py cumsums arrival/departure events only, so jobs already running before the window contribute nothing. The claim's magnitudes need two refinements. First, the blue job-count trace reaches about 420 (not 440) by day 0.1. Second, "climbs for roughly the first half day" holds for the lower panel only - the orange GPU-demand trace stays below its ~400-600 band until about day 0.6 - whereas the upper job-count panel recovers into its normal range by about day 0.1, after which it dips to ~300 around day 0.15-0.3 for ordinary diurnal reasons. Neither the figure, the LaTeX caption (thesis/latex/chapters/3.dataset_and_workload.tex:169) nor the body text (line 164) marks the opening stretch as an artefact; the body instead calls ~400 the baseline.

**Kanıt:** In the upper panel the blue trace touches the y=0 gridline at the x=0 gridline and rises to about 440 by day 0.1 and about 600 by day 0.5; in the lower panel the orange trace likewise starts at 0 at x=0 and reaches about 340 by day 0.5, against a steady band of roughly 400-600 GPUs for the remaining seven days. The cause is confirmed in /Users/hasanugurcelebi/Thesis/alibaba-gpu-runtime-prediction-and-scheduling/src/feature_engineering.py: add_cluster_utilization_features builds only arrival/departure events from the trace and takes a cumulative sum, so the sweep starts from an empty cluster and jobs already running before the trace window contribute nothing. No shading, annotation or caption text in the figure flags this.

**Düzeltme:** In cell 15, shade and label the warm-up on both axes, e.g. inside the existing for _ax in axes loop add _ax.axvspan(0, 0.5, color="0.85", alpha=0.5, zorder=0) plus a single axes[0].text(0.5, 0.95, "sweep warm-up", transform=..., fontsize=9, color="0.4"), or clip the view with _ax.set_xlim(0.5, None) if the warm-up region is not needed in the thesis.

---

### `NB04-Figure01.png` — naming

**Ne:** The Experiment D and F deep-learning models are suffixed "(One-Hot)" / "(One-Hot Sequence)" in the figure but "(Categorical)" / "(Categorical Sequence)" in the hyperparameter table and the experiment headings.

**Kanıt:** Figure labels "LSTM (One-Hot Sequence)", "CNN (One-Hot Sequence)", "CNN-LSTM (One-Hot Sequence)", "LSTM (One-Hot)", "CNN (One-Hot)", "CNN-LSTM (One-Hot)". NB04_Table14.html lists the same six as "LSTM (Categorical Sequence)", "CNN (Categorical Sequence)", "CNN-LSTM (Categorical Sequence)", "LSTM (Categorical)", "CNN (Categorical)", "CNN-LSTM (Categorical)"; NB04_Table13.html heads the experiments "D — Categorical" and "F — Categorical (Sequence)".

**Düzeltme:** Standardise on one suffix in the Model strings in cell 91 and in cell 47/Table 14's source. "(One-Hot)" is the more precise term for these models and is what the figure already uses, so rename the table entries to match rather than the reverse.

---

### `NB04-Figure01.png` — axis

**Ne:** NB04-Figure01.png has 27 rows, of which seven are non-learned baselines: Constant Zero, Constant Median, Profile Median, Per-User Median, Alibaba Estimate (SJU), Alibaba Estimate (SJG) and Alibaba Estimate (SJGG). All seven are tagged "(baseline)" in NB04_Table07.html ("Constant Zero (baseline)", "Alibaba Estimate - GroupGPU / SJGG (baseline)", etc.); none carries the tag in the figure. The left panel's y-axis is titled "Model Architecture" and the shared legend classifies every row by "Feature set" (Numeric/Categorical), so a constant predictor is presented as a categorical-featured model architecture and no visual cue separates baselines from trained models. Profile Median is third from the top of the MAE panel, not at the top: the two bars above it are the Alibaba Estimate (SJGG) (~3,151 s) and Alibaba Estimate (SJG) (~3,313 s) baselines, so three of the four lowest-MAE bars are non-learned baselines while the best learned model, LightGBM (Native Categorical) at ~4,973 s, is only fourth. In the printed thesis the figure carries the caption "Performance of all models across Experiments A to F." (thesis/latex/chapters/6.results_and_discussion.tex:53), which reinforces the impression that the leading bars are models produced by those experiments. Fix by appending "(baseline)" to the seven baseline rows (or hatching/greying them) so the figure matches the naming already used in NB04_Table07.html.

**Kanıt:** Left panel y-axis title reads "Model Architecture"; the rows include "Constant Zero", "Constant Median", "Profile Median" and "Per-User Median". NB04_Table07.html names these same four "Constant Zero (baseline)", "Constant Median (baseline)", "Profile Median (baseline)", "Per-User Median (baseline)". A constant predictor has no architecture, and "Profile Median" currently sits at the top of a panel titled "Test MAE by Model".

**Düzeltme:** In cell 93 change axes[0].set_ylabel("Model Architecture") to axes[0].set_ylabel("Model / baseline"), and in cell 91 restore the "(baseline)" suffix on the four Model strings so the figure labels read "Profile Median (baseline)" etc., matching Table 7.

---

### `NB04-Figure02.png` — units

**Ne:** The finding is real but understated. The XGBoost axis label is not merely missing a "(sums to 1)" note — it misnames the quantity actually plotted. The bars are xgb_final.feature_importances_, which XGBoost's sklearn wrapper normalises to sum to 1 (verified: 1.0000002), so each bar is a share of total gain. The unnormalised quantity the label names, booster.get_score(importance_type='gain'), is 103,338,696,704 for gpu_demand (raw sum 2.56e11) — about eleven orders of magnitude away from the 0.00-0.40 axis shown. So the label should read something like "Average gain per split (normalised, sums to 1)" or "Share of total gain"; as printed it describes a quantity whose real values are nothing like those on the axis, and the neighbouring panel's explicit "(sums to 1)" makes the omission read as a positive signal that this panel is unnormalised.

**Kanıt:** XGBoost bar lengths measured at 823, 424, 308, 132, 81, 76, 75, 58, 33 px, which against the panel's 0.00-0.40 tick scale are 0.404, 0.208, 0.151, 0.065, 0.040, 0.037, 0.037, 0.029, 0.016 and sum to 0.99. The neighbouring panel's identical-looking axis reads "Mean decrease in impurity (sums to 1)" while this one reads only "Average gain per split", so a reader takes 0.40 as an absolute per-split gain in objective units rather than a 40 % share of total gain.

**Düzeltme:** In the _imp_units dict in cell 95, change "XGBoost (Numeric)": "Average gain per split" to "XGBoost (Numeric)": "Average gain per split (normalised, sums to 1)".

---

### `NB04_Table05.html NB04_Table06.html NB04_Table07.html NB04_Table08.html` — headers

**Ne:** Table06's "Best in window?" column never says by which metric, and in Window 1 the row flagged "Yes" carries the worst R² of the three — LightGBM is marked best with Test R² "-0.041" while XGBoost, marked "No", has "0.042".

**Kanıt:** NB04_Table06.html, Window 1: "LightGBM (Native Categorical) | 4964.36 | 848.82 | -0.041 | Yes" and "XGBoost (Native Categorical) | 6754.52 | 4336.14 | 0.042 | No" under headers "Test MAE (s)", "Test MdAE (s)", "Test R²", "Best in window?". A negative R² means worse than predicting the test mean, so within one printed row the "Yes" flag and the R² cell point opposite ways. The criterion is MAE-only, stated only in notebook cell 45: `_best_model_ro = min(_window_metrics_ro, key=lambda k: _window_metrics_ro[k]["mae"])`.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 45, rename the dict key `"Best in window?"` to `"Lowest MAE in window?"` when appending to `rolling_origin_rows`, so the column header states its own criterion and no longer reads as an overall verdict that the R² column contradicts.

---

### `NB04_Table05.html NB04_Table06.html NB04_Table07.html NB04_Table08.html` — naming

**Ne:** Real, but two details in the description need correcting. (1) Cell attribution: the print statement "[Exp B / AlibabaEstimate-SJU]" and the comment "SJU/SJG/SJGG are the trace's own three duration-estimator variants" are both in cell 38 of notebooks/en/04_runtime_prediction_models.ipynb (the computation cell), not cell 47; cell 47 is only where the Table07 label strings are assembled. (2) "Neither SJG nor SJGG is expanded anywhere in the table" is loose - the labels do carry "Group" and "GroupGPU" next to their codes, which conveys the granularity; what is absent is any expansion of the acronym itself. Accurate statement: in NB04_Table07.html the first of the three Alibaba duration-estimator baselines is labelled "Alibaba Estimate (baseline)", omitting both the trace's estimator code (SJU) and any granularity word, while its two siblings carry both ("- Group / SJG", "- GroupGPU / SJGG"). The same notebook exports NB04_Table12.html, where all three are named consistently as "Alibaba Estimate (SJU)/(SJG)/(SJGG)", so the same model appears under two different names across tables and must be matched by its MAE (5641.82) rather than by name. The codes SJU/SJG/SJGG are also undefined in thesis/latex/frontmatter/abbreviations.tex, which lists SJF only. Fix: label the first row "Alibaba Estimate - User / SJU (baseline)" and add SJU/SJG/SJGG to the abbreviations list.

**Kanıt:** NB04_Table07.html "Model" column: "Alibaba Estimate (baseline)", "Alibaba Estimate - Group / SJG (baseline)", "Alibaba Estimate - GroupGPU / SJGG (baseline)". The first row is the SJU variant — notebook cell 47's own print statement calls it `[Exp B / AlibabaEstimate-SJU]`, and cell comment line reads "SJU/SJG/SJGG are the trace's own three duration-estimator variants" — but "SJU" appears nowhere in the exported table, so one member of a three-member family is named by a different convention than its siblings.

**Düzeltme:** In notebooks/en/04_runtime_prediction_models.ipynb cell 47, rename the first row to `"Alibaba Estimate - User / SJU (baseline)"` so all three follow one pattern, and define SJU/SJG/SJGG once in the table caption (per-user, per-group, per-group-and-GPU-count duration estimators from the trace).

---

### `NB04_Table09.html NB04_Table10.html NB04_Table11.html` — naming

**Ne:** The description over-attributes the problem to the tables and understates its spread. Accurate statement: (1) In the printed thesis, MdAE occurs exactly once — thesis/latex/chapters/4.prediction_models.tex line 156 — against 10 occurrences of MedAE across thesis/latex/ (abbreviations.tex:27, 1.introduction.tex:77, 4.prediction_models.tex:155, the 6.results_and_discussion.tex:20 results table, and six appendices.tex table headers). The typeset tables are all hand-written tabular environments (4 in chapter 6, 7 in the appendices) and already spell it MedAE, so the fix inside the document is the single word on line 156. (2) The HTML exports are not \input or \includegraphics'd anywhere in thesis/latex/ — no reference to thesis_export exists — so their header does not itself reach the printed page; they are the upstream notebook artefacts, and the spelling comes from notebooks/en/04_runtime_prediction_models.ipynb, which uses the key "Test MdAE (s)" and the metric name mdae throughout. (3) The MdAE header is not confined to Table09/10/11: 12 exported tables carry it — NB04_Table01, 03, 06, 07, 08, 09, 10, 11, 12, 13 plus NB05_32GPU_Table01 and NB05_256GPU_Table01 — so any later decision to paste an export into the thesis, or to show these tables in a defense deck, would reintroduce the mismatch in eleven more places.

**Kanıt:** All three files use the header string 'Test MdAE (s)'. thesis/latex/frontmatter/abbreviations.tex line 27 reads '\abbline{MedAE}{Median absolute error}' — the only entry for this metric, and it spells it MedAE. thesis/latex/chapters/4.prediction_models.tex line 156 uses the third-party spelling in prose: 'MAE and MdAE are therefore used as the primary accuracy metrics throughout this thesis'. 'MdAE' therefore appears in the tables and in chapter 4 but is never defined in the abbreviations list.

**Düzeltme:** Pick one spelling. Either change '\abbline{MedAE}' to '\abbline{MdAE}' in thesis/latex/frontmatter/abbreviations.tex, or rename the column to 'Test MedAE (s)' in notebook cells 68/78/88 and update chapter 4 line 156 — and apply the same choice to NB04_Table01/02/03/06/07/08/12/13, which all use 'MdAE'.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — headers

**Ne:** Table03's `Mean JCT Diff (s)` column (and its `95% CI low`/`high` and `Effect size (r)`, which share the same sign) is computed as Reference minus Policy, so a positive value means the Policy finished jobs faster than the Reference. The exported HTML states this nowhere, and 20 of the 43 rows are negative, so the sign determines the verdict in every row. Unlike Table02, whose analogous `JCT vs FIFO (s)` column uses green/red conditional styling to signal direction, Table03 is unstyled and offers no cue at all. The claim's added assertion that this is "the opposite of the reading a Diff column normally invites" is arguable framing rather than established fact — the Reference-then-Policy column order arguably does imply that subtraction order — but the underlying defect (an undeclared sign convention on an outcome column) stands.

**Kanıt:** Table03 headers `Reference`, `Policy`, `Mean JCT Diff (s)`. Row `SJF-UserMedian (baseline)` / `SJF-Oracle` shows `57101.5`, which is Reference minus Policy (Table01: 89759.42 − 32657.89 = 57101.53), so positive means the Policy is faster — the opposite of the reading a "Diff" column normally invites. Cell 48 confirms: `diff = (paired["ref_turnaround_time"] - paired["turnaround_time"])`.

**Düzeltme:** Rename to `Mean JCT: Reference − Policy (s)` and add "positive = Policy is faster" to the caption. Table02's `JCT vs FIFO (s)` needs the same treatment.

---

### `NB05_256GPU_Table01.html NB05_256GPU_Table02.html NB05_256GPU_Table03.html` — headers

**Ne:** Table02 exports the pandas index as an unlabelled first column of row numbers 0-26.

**Kanıt:** Table02 header row begins `<th class="blank level0" >&nbsp;</th>` followed by `Policy`; each body row opens with `<th id="T_cd3a9_level0_row0" class="row_heading level0 row0" >0</th>` through `>26<`. Table01 and Table03 have no such column, so the three tables in one chapter disagree on whether rows are numbered.

**Düzeltme:** Add `.hide(axis='index')` to the Styler in cell 47 before export, matching Table01 and Table03.

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — naming

**Ne:** The header "Model MdAE (s)" in NB05_32GPU_Table01.html (line 27, between "Model MAE (s)" and "Model RMSE (s)") is not defective for being unexpanded — no header in that table is expanded, and tables properly defer to the front matter. The defect is that it contradicts the thesis's own abbreviation convention: thesis/latex/frontmatter/abbreviations.tex line 27 registers only "MedAE — Median absolute error" (no MdAE entry), 1.introduction.tex:77 expands "median absolute error (MedAE)", 4.prediction_models.tex:155 defines the metric as "MedAE", and every printed LaTeX table header uses "MedAE (s)" (6.results_and_discussion.tex:20 and six header rows in appendices.tex). MedAE appears 10 times in the LaTeX source, MdAE exactly once (4.prediction_models.tex:156, "MAE and MdAE are therefore used as the primary accuracy metrics", one line below the MedAE definition), and MdAE is absent from the List of Abbreviations, so a reader cannot look it up. The same mismatch appears in NB05_256GPU_Table01.html and the NB04 tables (e.g. NB04_Table01.html). Fix: rename the column to "Model MedAE (s)" wherever the notebooks emit it (notebooks/en/05_scheduler_evaluation_32_gpu.ipynb line 1704 and the 256-GPU and NB04 equivalents), and change "MdAE" to "MedAE" in 4.prediction_models.tex:156. NB05_32GPU_Table02.html and NB05_32GPU_Table03.html do not carry this header (Table03's headers are Policy, Reference, Mean JCT Diff (s), p-value, p (Holm), Effect size (r), 95% CI low/high, Significant?), so they are unaffected.

**Kanıt:** Table01 header "Model MdAE (s)" sits between "Model MAE (s)" and "Model RMSE (s)" — MAE, RMSE, MAPE and R² are standard, MdAE is not. Notebook cell 27 computes it as `mdae = median_absolute_error(y_true, y_pred)`.

**Düzeltme:** Rename to "Model Median AE (s)" in the cell-27 `summary_rows` dict, or add a footnote "MdAE = median absolute error".

---

### `NB05_32GPU_Table01.html NB05_32GPU_Table02.html NB05_32GPU_Table03.html` — data-integrity

**Ne:** Table01 prints the literal string "NaN" in 15 cells where a model metric does not apply, which is Python output rather than typeset table notation.

**Kanıt:** Table01 rows SJF-Oracle, SRF (Heuristic) and FIFO each show "NaN" in all five of Model MAE (s), Model MdAE (s), Model RMSE (s), Model MAPE (%) and Model R². These three policies use no predictor (cell 27: `pred_col = _PRED_COL_MAP.get(policy)` returns None), so the blank is semantically correct.

**Düzeltme:** Before display, `eval_sorted.fillna("—")` on the five Model columns, and footnote "— = policy uses no runtime predictor".

---

## NIT (1)

### `NB02-Figure02.png` — data-integrity

**Ne:** The caption places the second surge at "Day 5.5", but the ~1,080 jobs/hr spike is plotted at day 5.70 — visibly right of the midpoint between the day-5 and day-6 gridlines.

**Kanıt:** notebooks/en/02_workload_analysis.ipynb cell 13: "surging again to **~1,070 jobs/hr** near Day 5.5". Measuring the curve against the same calibration (day-0 tick x=285.5 px, 306.7 px/day; y=0 gridline row 813, 105 px per 200 jobs), the second-tallest spike peaks at x=2034 px, y=244 px = day 5.70, 1,084 jobs/hr. The day-5.5 position would be x=1972 px, and the spike is clearly to the right of it. (The first spike, quoted as "~1,350 jobs/hr around Day 1.5", measures day 1.45 / 1,356 jobs/hr and is correct.)

**Düzeltme:** In cell 13 change "surging again to **~1,070 jobs/hr** near Day 5.5" to "surging again to **~1,080 jobs/hr** near Day 5.7".

---
